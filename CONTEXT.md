# 만세력 (Saju)

생년월일시에서 사주를 도출하고, 두 사람 사이에 성립하는 관계를 **판정이 아니라 사실로**
내는 서비스. 이 문서는 용어집이다 — 구현도 스펙도 여기 적지 않는다.

## Language

### 사람과 입력

**Person**:
사용자가 사주를 보고 싶어 저장해 둔 한 사람 — 자기 자신을 포함한다.
_Avoid_: 프로필, 상대방, 유저

**User**:
로그인한 계정 주체. 여러 **Person** 을 소유한다.
_Avoid_: 회원, 사용자(문맥에 따라 Person 과 헷갈림)

**입력**:
**명식**을 도출하는 데 필요한 값 한 벌 — 생년월일시, 출생지, 성별, 그리고 결과가
갈리는 **옵션**까지 포함한다.
_Avoid_: 생년월일, 사주 정보

**옵션**:
학파에 따라 갈리는 지점의 선택값(자시 규칙, 시간 기준 등). 빼면 명식이 달라지므로
**입력**의 일부이지 설정이 아니다.
_Avoid_: 설정, 환경값

### 계산 결과

**명식**:
한 사람의 **입력**에서 도출된 사주 여덟 글자와 그로부터 나온 구조화된 사실.
**저장하지 않는다** — 볼 때마다 **입력**에서 다시 계산되는 파생 뷰다.
_Avoid_: SajuChart(엔티티로 오해됨), 사주, 팔자

**원국**:
태어난 시각으로 고정된 **명식** — 운(대운·세운·월운)과 대비되는 말.
_Avoid_: 본명, 사주 원판

**근거**:
AI 결과를 만들기 전에 허용된 자료를 구조화한 묶음. 엔진이 낸 사실과 claim 단계를 담고,
**`match-v0` 는 안 담는다**(`match-v0-not-in-prompt`). 사용자에게 보이는 최종 점수·해석은
근거가 아니라 **Reading**의 출력이다 — 점수도 모델이 낸다.
_Avoid_: 데이터, 분석 결과

**발화**:
**근거**를 재료로 계약이 허락한 상한 안에서 세운 한국어 문장.
_Avoid_: 해석, 설명

**분석 표**:
관계 표와 거기서 세운 **발화** — 프롬프트와 **근거**를 손보면서 무엇이 나왔는지 대조하는
**엔진 중간 결과**다. **우리끼리 쓰는 말이다** — 화면에서는 **「두 원국을 맞대어 본 표」**로
부르고, 궁합 화면에서 접어 둔다(ADR 0035). 저장한 두 사람 화면에는 아예 안 선다(ADR 0025).
_Avoid_: 원자료, 사실 표, 검산 자리

**해석**:
**근거**를 받은 AI 가 일반 사용자를 위해 쓴 글. **발화**와 다른 층이다.
**우리끼리 쓰는 말이다** — 사용자 화면에서 이것을 부르는 이름은 **풀이**다.
_Avoid_: 리딩

**풀이**:
`Reading` 을 사용자가 부르는 이름. 한 사람이면 **사주풀이**, 두 사람이면 **궁합 풀이**다.
「해석」은 내부 화면(`/me/reading/inspect`)에만 남긴다 — 같은 것을 세 낱말로 부르면
알림 한 줄과 화면 제목이 서로 다른 기능처럼 읽힌다(ADR 0026).
_Avoid_: 해석, 리포트, 상세 궁합 리포트

## 화면 문구 규칙

용어집이 **무엇을 부르는 이름**을 정한다면 여기는 **어떻게 적는가**를 정한다. 낱말이
한 벌이어도 표기가 셋이면 사용자는 같은 것인지 확인하는 데 눈을 쓴다(ADR 0027).

**마침표는 자리가 정한다.**

| 자리 | 마침표 | 보기 |
| --- | --- | --- |
| 제목 · 라벨 · 딱지 · 버튼 | 없음 | `출생 시각 모름` · `중지된 계정입니다` · `저장했습니다` |
| 짧은 상태 표시 | 없음 | `복사했습니다` · `풀이 만드는 중…` |
| 설명문 · 안내문 · 경고문 | 있음 | `저장된 값은 그대로 있습니다. 지금 화면이 그 값을 읽지 못하는 것입니다.` |
| 여러 문장 | 문장마다 | 위와 같다 |

**진행 중은 「~ 중…」 하나다.** `저장하는 중…` · `보내는 중…` · `풀이 만드는 중…`.
「~ 중이에요」·「~ 하고 있어요」·「~ 합니다…」로 적지 않는다 — 짧은 상태 라벨에 말투를
싣기 시작하면 화면마다 온도가 달라진다.

**버튼은 해라체를 쓰지 않는다.** 확인 버튼은 `삭제를 요청합니다`·`차단합니다`·
`신고합니다`·`뺍니다` 처럼 합쇼체로 적는다.

**한 사실에는 한 표기.** 「출생 시각 모름」(`HOUR_UNKNOWN_LABEL`)·「출생 정보」가 그
예다. 화면마다 줄이거나 늘리지 않는다 — 표가 좁으면 줄바꿈하지, 낱말을 바꾸지 않는다.

**한 화면에는 한 이름.** `/me/people` 은 **「저장한 사람」**이다 — 탭 제목도, h1 도, 궁합에서
고르는 자리도. 「등록한 사람」은 관리·DB 쪽 말이고 「내 사람」은 관계의 뜻이 너무 세다.
「등록하다」는 **동사로만** 남는다(「사람 등록하기」) — 목록의 이름과 목록에 넣는 동작은
같은 낱말일 이유가 없다.

**늘 참인 문장은 늘 세우지 않는다.** 그 화면에서 한 번도 틀린 적이 없는 안내는 배경이
되고, 배경이 두꺼워질수록 옆에 선 **실제로 갈리는** 한 줄이 같이 안 읽힌다. 그리고
**경고는 되돌릴 수 없는 누름 직전에 선다** — 버튼 옆에 늘 적힌 경고는 누르지 않을
사람에게 하는 말이다(ADR 0028).

**기능을 쓸 수 있다고 말할 때는 「이용할 수 있습니다」.** 「쓰실 수 있습니다」와 뜻이 같지만
처리방침이 쓰는 말투가 이쪽이고, 같은 문단 안에서 둘이 갈리면 다른 약속처럼 읽힌다.

## Relationships

**기본 객체는 셋이다** — `User` · `Person` · `Reading`. 이것은 제품의 엔티티가 셋이라는
뜻이 아니라, **사주를 저장하고 해석하는 일**의 기본 객체가 셋이고 **명식**은 그 위의
파생 뷰라는 뜻이다. 권한과 매칭 흐름은 별도 엔티티를 갖는다 — `UserPersonAccess` ·
`PersonChartRevision` · `MatchRequest` · `Match` · `Notification`.

- 한 **User** 는 여러 **Person** 을 소유한다
- 한 **Person** 은 정확히 하나의 **입력**을 든다
- **입력** → (계산) → **명식**. 저장되는 것은 왼쪽뿐이다
- **명식** → **근거** → **발화** / **해석**

## Flagged ambiguities

- "사주 저장"이 **입력** 저장인지 **명식** 저장인지 갈렸다 — 해소: **입력**만 저장한다.
  **명식**은 엔티티가 아니라 순수 함수의 결과이므로, 저장하면 진실이 두 벌이 되고
  엔진을 고칠 때 저장된 값이 조용히 어긋난다.

## Language (이어서) — 계정과 저장

**Reading**:
한 사람의 자기 풀이 또는 두 **Person**의 궁합에 대해 AI가 만든 **현재 결과**. 자기 풀이는
해석을, 두 사람 궁합은 점수와 해석을 들며, 실제 사용한 근거·프롬프트·모델 설정을 같은
한 벌로 둔다. 같은 대상에 대한 **결과 생성 요청**이 성공하면 한 벌 전체를 교체하며 이전
결과는 제품에서 보존·참조하지 않는다.
_Avoid_: Reading 이력, 해석 사건, Compatibility(궁합 사실 자체)

**결과 생성 요청**:
사용자가 자기 풀이 또는 이미 볼 권한이 있는 궁합을 AI로 새로 보겠다고 직접 실행하는 동작.
화면 조회도, 상대의 동의를 구하는 **MatchRequest**도 아니다. 성공하면 현재 **Reading**을
교체하고 실패하면 직전 성공 결과를 유지한다.
**누름과 완성이 같은 요청 안에 있지 않다** — 누르면 시도가 열리고, 만드는 일은 요청
수명 밖에서 돌다가 결과로 돌아온다(ADR 0016·0020). 화면은 시도의 상태를 물어 본다.
_Avoid_: 재계산, MatchRequest, 페이지 새로고침

**궁합**:
두 **Person** 사이에 성립하는 사실 그 자체. 늘 참이고 저장되지 않는다 —
사용자에게 보이는 실험 점수와 글은 현재 **Reading** 에 함께 저장된다.
_Avoid_: 매칭 점수, 상성

**자리 대칭**:
어느 쪽을 `a` 로 넣든 같은 사실 집합이 나온다는 뜻. **값 대칭이 아니다** —
십성·오행 보완·억부는 방향을 갖되 양방향이 언제나 함께 실린다.

**selfPerson**:
한 **User** 가 자기 자신이라고 지정한 **Person**. User↔Person 사이의 관계이지
Person 의 속성이 아니다.
_Avoid_: isSelf, 내 사주, 본인 프로필

**localLabel**:
한 **User** 가 어떤 **Person** 을 부르는 이름(「엄마」, 「전여친」). 같은 Person 이
다른 User 에게는 다르게 불리므로 Person 이 아니라 **엣지**가 든다. **닉네임과 다르다** —
이것은 내 목록 안에서만 쓰는 말이고 남에게 안 보인다.
_Avoid_: relationship, Person.name, 닉네임

**닉네임**:
**User** 가 앱 안에서 불리는 **하나뿐인 이름**. 가입할 때 짓고 2~8자이며 다른 사람과
같을 수 없다. 매칭·요청·소식 어디서나 이 이름이 선다. 참여의 부속물이 아니라 **계정의
것**이라 `app_user` 가 든다 — 인연 찾기에 참여하지 않는 사람도 이름이 있다.
_Avoid_: 별명, 공개용 별명, 표시 이름

**프로필 사진**:
**닉네임** 옆에 서는 선택 값. 올린 사람만 서고 없으면 이름의 첫 글자가 선다. 바이트는
Postgres 안에 있고(`profile_photo`) 계정에 cascade 로 매여 있다 — 지우는 일이 **열쇠를
따라가게** 하려는 것이다(ADR 0023). 볼 수 있는 조건은 「지금 그 사람의 이름이 내게
보이는가」와 같다.
_Avoid_: 아바타, 사진첩

## Relationships (이어서)

- 한 **User** 는 정확히 하나의 **selfPerson** 을 가리킨다
- **User** ↔ **Person** 은 다대다다. 그 엣지가 **localLabel** 과 권한을 든다
- 한 **Reading** 은 kind에 따라 한 **Person**, 두 **Person**, 또는 **Match**를 가리킨다
- **Reading** 은 현재 결과에 사용한 **근거**를 통째로 든다 — 입력을 따로 베끼지 않는다

## Flagged ambiguities (이어서)

- 「나」가 **Person** 의 속성인지 **User** 의 지정인지 갈렸다 — 해소: User 의 지정
  (`selfPersonId`). 같은 사람이 다른 계정에서는 「나」가 아니기 때문이다.
- 「엄마」가 **Person** 의 속성인지 관계의 라벨인지 갈렸다 — 해소: 라벨
  (**localLabel**). 한 Person 은 누군가에겐 엄마고 누군가에겐 배우자다.
- 「궁합을 저장한다」가 관계 사실 저장인지 사용자 결과 저장인지 갈렸다 — 해소: 관계
  사실 자체가 아니라 현재 점수·해석과 그 생성 자료 한 벌(**Reading**)을 저장한다.
- 「독자의 관점」은 제품에 넣지 않는다 — 궁합은 언제나 중립 문체 하나이고,
  A/B 입장 차이는 관점이 아니라 **양방향 사실**로 이미 글 안에 들어간다.

## Language (이어서) — 달력

**원본 생일**:
사용자가 실제로 넣은 형식 그대로의 생년월일 — 양력이거나, 음력 평달이거나, 음력 윤달.
**Person 이 이것과 변환된 양력을 둘 다 든다.**
_Avoid_: birthDate(어느 달력인지 안 말함)

**윤달**:
음력에서 같은 달 번호가 두 번 오는 쪽. 평달과 다른 날이므로 형식이 셋이지 둘이 아니다.

## Language (이어서) — 매칭

**기존 실험 지표** (`match-v0`):
현재 코드에 있는 결정론적 베타 계산. **프롬프트에 안 들어간다**(`match-v0-not-in-prompt`) —
근거만 넘기고 최종 점수도 모델이 낸다. 지금 이 값이 사는 자리는 `discovery-v0` 의 두 축이
거기서 왔다는 것 하나이고, 그 뒤로는 따로 산다. 내부 버전은 사용자에게 보이지 않는다.
_Avoid_: 최종 궁합 점수, 저장된 Match 점수

**현재 결과 점수**:
AI 해석과 같은 생성 건에서 나온 사용자용 실험 점수. 같은 대상의 현재 **Reading**에 해석과
함께 저장되며, **결과 생성 요청**이 성공하면 새 점수·해석으로 함께 교체된다. 이전 점수는
UI·AI·엔진이 참조하지 않고 사용자 이력으로 제공하지 않는다.
_Avoid_: 영구 점수, Match 고정 점수, 최신 버전 점수

**노출 순서** (`discovery-v0`):
아직 선택되지 않은 후보를 어떤 차례로 보여줄지 정하는, 별개 버전의 정책.
**정렬만 하고 사람을 제외하지 않는다.**
_Avoid_: 필터, 매칭 알고리즘, 추천 점수

**후보**:
매칭 참여에 동의한 다른 User 의 **selfPerson**. 대신 등록한 Person(엄마·친구)은
후보가 되지 않는다.
_Avoid_: 상대, 매칭 대상

**탐색 후보**:
`discovery-v0` 상위가 아닌데도 일부러 섞어 넣는 후보. 정책이 틀렸을 때 신호를 얻는
유일한 자리다.

**하드 제외**:
사주와 무관하게 후보에서 반드시 빼는 조건 — 미동의·차단·제재·연령·사용자 설정 조건.
**사주 점수는 여기 들어가지 않는다.**

## Flagged ambiguities (이어서)

- 「점수」가 현재 Reading의 결과 점수인지 후보의 노출 순서(`discovery-v0`)인지 갈렸다 —
  해소: 전자는 **결과 생성 요청**의 사용자 결과이고 후자는 후보 정렬용 내부 정책이다.
  후보 순서 숫자를 궁합 점수로 재사용하지 않는다.
- `match-v0`가 최종 점수 공식인지 갈렸다 — 해소: 아니다. **프롬프트에 안 들어가고**
  최종 점수는 모델이 근거만 보고 낸다(`READING_POLICY`).

## Language (이어서) — 신원과 이력

**claim**:
어떤 **Person** 이 자기 자신임을 **User** 가 승인받는 절차. claim 이 끝나면 그 Person 의
출생 정보 편집권이 그 User 에게만 남는다.
_Avoid_: 연결, 병합(merge 는 두 Person 을 하나로 합치는 별개의 일)

**revision**:
출생 정보 한 벌의 한 판본. 조회할 때가 아니라 실제로 다른 입력을 저장할 때만 생긴다.
현재 판본과 살아 있는 pending 요청·Match·Reading 이 참조하는 판본은 보존하고, 미참조
이전 판본은 최근 두 개까지만 둔다(ADR 0011).
**화면에는 「판본」이라고 적지 않는다** — 사용자가 보는 이름은 「저장된 출생 정보」이고,
Match 에 매인 것은 「동의하신 그때의 출생 정보」다(ADR 0026). 코드·시험·문서는 계속 판본으로
부른다. 사용자 문장에 그 낱말이 새는지는 `consent.test.ts` 가 전부 훑는다.
_Avoid_: 수정 이력, 버전(정책 버전과 헷갈림)

**pending 요청**:
한쪽이 「상세 궁합을 함께 보자」고 요청했고 상대가 아직 수락도 거절도 하지 않은 상태.
Evidence 를 바꾸는 수정이 일어나면 무효화된다.
_Avoid_: 매칭 신청, 대기

**본인확인**(`UserAgeVerification`):
매칭 참여 자격을 정하는 별개의 값. **`Person.birthDate` 로 대신하지 않는다** —
사주 입력은 고칠 수 있고 남의 것을 넣는 것이 정상 사용이다.

## Flagged ambiguities (이어서)

- 「생년월일」이 사주 입력인지 신원 정보인지 갈렸다 — 해소: 사주 입력이다. 연령 제한은
  별개의 **본인확인**을 근거로 한다.
- 「연결」이 claim 인지 merge 인지 갈렸다 — claim 은 Person↔User(내가 그 사람이다),
  merge 는 Person↔Person(이 둘은 같은 사람이다).

## Language (이어서) — 출시 범위

**테스트 코드**:
운영자가 만들어 전하는 문자열 하나. **성년인증이 아니라 범위 통제**다 — 인증이 없는
동안 공개 매칭을 열지 않기 위한 장치(ADR 0005). 코드마다 **사는 하루**와 **최대 인원**이
붙는다. 이메일 명단을 대신한다(ADR 0042) — 명단은 운영자가 주소를 알아야 했고, 코드는
받은 사람이 스스로 들어온다.
_Avoid_: 초대 코드(초대라는 말은 이제 사람이 하는 일이지 관문이 아니다), 초대장, 베타 신청

**가입 완료**:
코드로 들어와 **닉네임**을 짓고 **지금** 처리 안내를 확인한 상태. 셋이 한 문에서 함께
적히므로 값 하나가 든다(`signed_up_at`). 그 앞은 **구글 로그인만 한 계정**이고, 그
계정에는 되돌릴 수 없는 첫 쓰기가 하나도 안 열린다.
_Avoid_: 온보딩 완료(내 명식 등록과 헷갈림 — 그것은 가입 다음이다), 인증

**앱 내 알림**:
로그인해서 앱을 열었을 때만 보이는 통보. 외부 통보(이메일·카카오·푸시)는 공개 매칭과
함께 온다.

## Language (이어서) — 접근과 한도

**Match**:
양쪽이 궁합을 함께 보기로 합의해 성립한 관계. **그 자체가 상대 Person 에 대한 접근
근거**이며, `UserPersonAccess` 와는 다른 갈래다.
_Avoid_: 매칭(동사와 헷갈림), 연결

**Person 한도**:
사용자가 직접 만들어 관리하는 가족·친구 Person 에만 걸리는 수(10). **후보와 Match
상대는 세지 않는다** — `UserPersonAccess` 에 들어가지 않기 때문이다. 이 수는 언제나
**풀이권 총량보다 크다**(10 > 8) — 자리가 풀이권보다 적으면 풀이권을 가지고도 쓸 데가
없는 사람이 생긴다(ADR 0032).

## Flagged ambiguities (이어서)

- 「이 Person 을 왜 볼 수 있나」의 답이 둘이다 — **내가 등록했다**(`UserPersonAccess`)와
  **우리가 합의했다**(`Match`). 값으로 갈라 두지 않으면 나중에 되짚을 수 없다.

## Flagged ambiguities (이어서)

- **Match 가 주는 것**이 상대 Person 전체에 대한 접근인지 궁합 범위에 대한 접근인지
  갈렸다 — 해소: 범위가 정해진 접근이다. 그 범위에는 서로의 여덟 글자가 전부 드러날
  가능성이 포함되지만 정확한 출생 원문·출생지와 상대 원국 전체 판정은 포함되지 않는다
  (ADR 0008·0012).
- **근거**의 구실이 「명식을 보여주는 것」인지 「AI 의 주장을 되짚는 것」인지 갈렸다 —
  해소: 후자다. 그래서 근거는 모델에 넘긴 것과 정확히 같고, 더도 덜도 아니다.

## Language (이어서) — 후보 노출

**매칭 참여**:
자기 **selfPerson** 을 **후보**로 내놓겠다는 User 의 결정. **기본으로 켜져 있다** —
사주를 저장한 사람은 자동으로 후보 풀에 들고, 그 고지는 **가입 폼**이 읽힌다.
켠 시각과 **끈 시각**을 함께 드는 **사건**이지 설정값이 아니다. 끄면 내놓은
**오행 요약**도 거두고, **끈 사람은 자동으로 다시 켜지지 않는다.**
_Avoid_: 공개, 활성화

**DiscoveryProfile**:
**매칭 참여**에 관한 한 벌 — 참여 상태, 내놓은 **오행 요약**, 그리고 사주와 무관한
명시적 조건. **이름과 소개는 여기 없다** — 그 둘은 계정의 것이고 참여를 꺼도 살아 있다
(**닉네임**). 참여를 끄면 여기 있는 것만 거둬진다.
_Avoid_: 프로필(Person·프로필 화면과 헷갈림), 공개 정보

**오행 요약**:
참여할 때 매칭 풀에 내놓는 다섯 수 — 오행별 개수와 비중, 센 글자 수. **명식이 아니다**
(저장하지 않는다는 약속은 그대로다). 어느 **revision** 에서 나왔는지를 함께 들고,
지금 판본의 것이 아니면 **후보**가 아니다.
_Avoid_: 오행 벡터(자료구조 이름), 명식 요약

**노출 기록**:
누구를, 어떤 정책 버전으로, 몇 번째 자리에, **탐색 후보**로 보여줬는가의 기록.
운영자가 **노출 순서**를 평가하는 유일한 근거이고 사용자에게는 보이지 않는다.
_Avoid_: 로그, 조회수

**다시 보지 않기**:
이 사람을 내 후보 목록에서 빼겠다는 한 방향 표시. **차단이 아니다** — 차단은 양방향으로
접촉을 막고 되돌리지 않는다.
_Avoid_: 차단, 숨김

## Relationships (이어서)

- 한 **User** 는 **DiscoveryProfile** 을 하나 갖거나 갖지 않는다
- **매칭 참여** 중인 User 의 **selfPerson** 만 **후보**가 된다 — 대신 등록한 Person 은
  아니다. 참여가 실제로 열리는 자리는 **홈이 목록을 여는 순간**이다. 요약은 앱만 만들 수
  있어서, 앱이 넣지 않으면 켜질 수 없다
- **노출 순서**(`discovery-v0`)는 두 **오행 요약** 사이에서 난다. 명식도 **궁합**도
  보지 않는다

## Flagged ambiguities (이어서)

- 「그만 보기」가 **다시 보지 않기**인지 **차단**인지 갈렸다 — 해소: 둘은 다른 일이다.
  다시 보지 않기는 후보 목록에서만 빼고 되돌릴 수 있으며, 차단은 접촉을 막는다.

## Language (이어서) — 요청과 동의

**차단**:
그 사람과의 접촉을 끊는 결정. **양방향이고 되돌리지 않는다** — 서로의 후보 목록에서
사라지고, 살아 있던 **pending 요청**도 그 자리에서 거둬진다. 이미 성립한 **Match** 는
목록에서 내려가되 기록은 남는다.
_Avoid_: 다시 보지 않기, 숨김, 신고

**무효**(`invalidated`):
어느 한쪽의 **revision** 이 바뀌어 **pending 요청**이 성립할 수 없게 된 상태. 사람이
거둔 것이 아니다.
_Avoid_: 취소, 만료

**거둠**(`cancelled`):
요청한 쪽이 스스로 물린 상태. 받는 쪽에게는 알리지 않고, 그 요청의 통보도 서지 않는다.
_Avoid_: 무효, 삭제

**동의 화면**:
수락하면 서로에게 무엇이 열리고 무엇이 열리지 않는지를 **누르기 전에** 적어 둔 자리.
보내는 쪽과 받는 쪽이 **같은 한 벌**을 읽는다(ADR 0008).
_Avoid_: 약관, 안내

## Relationships (이어서)

- **후보**를 본 적이 있어야 **pending 요청**을 만들 수 있다 — 요청은 **노출 기록**에
  매인다(ADR 0009)
- 두 **User** 사이에 살아 있는 결정은 하나다 — pending·수락·거절 중 하나뿐이고,
  **무효**와 **거둠**은 다시 청할 수 있다
- 수락은 **Match** 를 만들고 **UserPersonAccess** 는 만들지 않는다 — 두 갈래로 남는다

## Flagged ambiguities (이어서)

- 「요청이 사라졌다」가 **무효**인지 **거둠**인지 갈렸다 — 해소: 값으로 가른다. 앞은
  입력이 바뀌어서고 뒤는 요청자가 스스로다. 합치면 「왜 사라졌는지」에 답할 수 없다.
- 「거절」이 한 번의 답인지 되돌릴 수 없는 결정인지 갈렸다 — 해소: 되돌리지 않는다.
  거절한 사람은 서로의 **후보** 목록에서도 내려간다(ADR 0009).
- 「참여를 끈다」가 자료 삭제인지 노출 중단인지 갈렸다 — 해소: 노출 중단이다. 내놓은
  **오행 요약**은 거두지만 Person·Match·Reading 은 그대로다. 다만 아무 Match·Reading 도
  참조하지 않는 이전 revision 은 ADR 0011 의 일반 보존 상한을 따른다.

## Language (이어서) — 공유 결과

**공유 결과**:
성립한 **Match** 위에 서는 화면. 두 원국 **사이**의 허용된 근거와 현재 **Reading**의
점수·중립적 해석이 함께 선다. 명식 표도 전체 근거 패널도 여기 없다 — 명식은 각자의 화면이
이미 들고, 근거 패널은 사용자 화면 어디에도 서지 않는다(`/evidence`, ADR 0025).
화면을 여는 것만으로 AI를 다시 부르지 않고 양쪽이 같은 현재 결과를 읽는다.
_Avoid_: 상세 궁합 리포트, 해석, Reading

**매인 판본**:
**Match** 가 잡아 둔 두 `chartRevisionId`. 공유 결과는 언제나 이것으로 난다. 뒤에
어느 쪽이 입력을 고쳐도 이 결과는 움직이지 않는다 — 동의한 대상이 그 판본이라서다.
_Avoid_: 현재 판본, 최신 입력

**열쇠**:
매인 판본의 계산 입력을 읽는 자리 하나(`service_role`). **볼 자격을 묻지 않는다** —
그 답은 `auth.uid()` 를 든 함수가 먼저 낸다. 열쇠가 하는 일은 그 답이 가리킨 두
판본을 읽는 것뿐이다(ADR 0010).
_Avoid_: 관리자 권한, 백도어

## Relationships (이어서)

- **Match** → **공유 결과**. 하나의 Match 에 화면 하나이고, 그 화면은 **매인 판본**
  으로만 난다
- **공유 결과**는 **동의 화면**과 **같은 한 벌**을 읽는다 — 열린 것과 열리지 않은
  것의 목록이 하나다(ADR 0008·0010)
- 사용자에게 보이는 점수·해석은 현재 **Reading**에서 읽는다. L3 조립기와 `match-v0` 는
  화면 흐름을 시험하던 선행 구현이고 사용자 결과의 출처가 아니다

## Flagged ambiguities (이어서)

- 「결과가 낡았다」가 고장인지 약속인지 갈렸다 — 해소: 약속이다. **매인 판본**으로
  나므로 뒤의 수정이 값을 바꾸지 않는다. 새 입력으로 보려면 새 요청이 필요하다.
- 「상대의 글자가 보인다」가 여덟 글자 전부 공개까지 포함하는지 갈렸다 — 해소: 포함한다.
  관계 여러 줄을 합치면 년주·월주·일주·시주가 모두 드러날 수 있고 Match 동의가 그것을
  연다. 다만 정확한 출생 원문과 그 글자 위의 상대 원국 전체 판정은 열리지 않는다
  (ADR 0012).

## Relationships (이어서)

- **revision** → **참조**. 무엇이 참조인지는 표 이름을 적어 둔 목록이 아니라
  `person_chart_revision` 을 가리키는 **FK** 가 정한다. terminal 요청이 판본을 놓는 것이
  곧 「그 요청은 더 이상 참조가 아니다」라는 선언이다(ADR 0011)

## Flagged ambiguities (이어서)

- 「고친 기록은 덮어쓰지 않고 쌓입니다」가 화면 약속인지 저장 규칙인지 갈렸다 — 해소:
  저장 규칙이고, 지키는 것은 **참조된 판본**이다. 아무 기록도 가리키지 않는 이전 입력은
  최근 둘까지만 남는다. 그래서 `/me` 의 판본 이력 목록을 내렸다 — 목록을 세워 두면
  정리되는 입력이 화면에서 하나씩 사라지고, 그때 화면이 자기 약속을 어긴 것처럼 보인다.

## Flagged ambiguities (이어서)

- 「지문만 남긴다」가 원문 폐기인지 갈렸다 — 해소: **아니다.** 출생 입력은 후보 공간이
  작아서 열쇠 없는 해시는 원문의 다른 표기일 뿐이다(재어 봤다: 시각만 모르면 1 밀리초,
  아무것도 몰라도 두 시간이 안 걸린다). terminal 요청이 드는 것은 그때 그 판본을 가리키던
  **무작위 식별자**이고, 그 값은 입력을 담지 않는다. 판본 행 자체의 지문은 그대로다 —
  그 행은 원문을 들고 있어 지문이 더 여는 것이 없다.

## Language (이어서) — 결과 만들기

**redacted 근거**:
모델에 넘기려고 **잘라 낸** 근거. 출생 원문·출생지·분 단위 정밀도가 빠져 있고, 무엇을
왜 뺐는지를 값으로 함께 든다. 자르지 않은 **근거**와 다른 것이며, `Reading` 이 저장하는
것은 언제나 이쪽이다(ADR 0008·0013).
_Avoid_: 익명화, 마스킹

**공유 범위 근거**:
Match 공유 결과에 쓰려고 **한 번 더** 자른 redacted 근거. 여덟 글자와 두 원국 **사이의**
사실만 남고 상대 원국 하나에 대한 판정은 빠진다. 자르는 이유가 앞의 것과 다르다 — 앞은
모델에 안 넘기기로 한 것이고 이것은 **두 사람이 서로 열지 않기로 한 것**이다(ADR 0012).
_Avoid_: 요약본, 축약 근거

**시도**(`reading_run`):
결과 생성 요청 한 번의 기록 — 언제 누가 무엇을 만들려 했고 어떻게 됐는가. **결과가
아니다**: 본문도 근거도 판본 참조도 들지 않는다. 실패한 시도 하나 때문에 과거 출생
입력이 영구 보존되지 않게 하려는 것이다(ADR 0011·0013).
_Avoid_: 이력, 로그(운영 로그와 헷갈림)

## Flagged ambiguities (이어서)

- 「점수가 어디 서는가」가 화면마다 갈렸다 — 해소: **한 화면에 하나**다. 로그인 화면에서는
  현재 **Reading** 의 점수만 서고 `match-v0` 는 내려갔다. 둘을 함께 두면 무엇을 믿을지
  사용자가 정해야 하는데, 그 물음에 우리가 답을 갖고 있지 않다. `match-v0` 는 익명 화면과
  내부 실험에 남는다(ADR 0003·0013).
- 「같은 요청 한 번」을 무엇이 지키는지 갈렸다 — 해소: **도는 시도 잠금**이다. 열쇠를
  서버가 지으면 매번 달라 아무것도 막지 못하고, 브라우저가 지으면 손으로 적을 자리가
  열린다. 열쇠는 나중에 안정된 값을 들고 올 쪽을 위해 자리만 둔다(ADR 0013).

## Glossary (이어서)

**근거 밀착성**:
AI 해석의 결론이 실제로 그 Evidence 의 내용과 조합 때문에 나왔는가. 구체성·실용성과 함께
품질 게이트의 세 축이다. 재는 것은 **문체의 독창성이 아니다** — 같은 명식 구조를 가진
사람은 여럿이므로 「한 사람에게만 맞아야 한다」는 목표가 아니고, 비슷한 Evidence 끼리
문장이 겹치는 것은 실패가 아니다.
_Avoid_: 비뻔함(무엇을 재는지 말하지 않고 실패만 가리킨다), 독창성, 개인화 정도

**체감 적합성**:
사용자가 자기 해석을 읽고 실제와 맞는다고 느끼는 정도. **운영 지표이지 품질 게이트가
아니다** — 내부 평가자는 Evidence 만 보므로 대신 매길 수 없고, 사용자에게 보여준 뒤에만
얻어진다. 반드시 **근거 밀착성과 함께** 본다: 바넘 문장은 근거 없이도 「내 얘기 같다」를
만들므로, 체감만 오르고 밀착성이 떨어지면 그것은 개선이 아니라 바넘화다.
_Avoid_: 만족도, 정확도

**Reading kind**:
현재 AI 결과가 무엇에 대한 것인가. `self`(내 명식 하나) · `person`(내가 관리하는 저장된
사람 하나) · `private`(내가 접근 가능한 두 사람) · `match`(성립한 Match). 넷은 같은
파이프라인과 같은 교체 규칙을 쓰되 근거 범위와 접근 판정이 다르다. **한 kind 의 권한
판정이 다른 kind 를 열지 않는다.**

`self` 와 `person` 은 **한 사람짜리 계열**이라 자료·프롬프트·검사가 같고 궁합 점수를
내지 않는다. 갈리는 것은 접근 판정 하나뿐이다 — `self` 는 부른 사람의 selfPerson 을
스스로 찾고, `person` 은 대상을 받아 엣지를 확인한다. 그래도 한 낱말로 합치지 않는다:
내 명식을 넘긴 것과 남의 명식을 넘긴 것은 동의 범위가 다른 일이고, 합치면 그 둘이
기록에서 같아진다.

**내 selfPerson 은 `person` 의 대상이 아니다.** 그것도 내 엣지에 있으므로 안 막으면 같은
명식에 결과가 둘 서고 같은 자료로 풀이권이 두 번 나간다. 이 판정은 접근 판정 함수에 있고
화면 필터에 있지 않다 — 화면에만 두면 주소로 열린다. 화면이 이 값을 견줄 때는 주소에 적힌
글자가 아니라 **DB 가 정규화한 id** 로 견딘다: `uuid` 비교는 대소문자를 안 가리는데
문자열 비교는 가려서, 대문자로 적은 주소가 화면 판정만 지나간다.

**`person` 본문은 대상 Person 에게 직접 말한다.** `self` 의 프롬프트를 그대로 쓰므로
「당신은」이 그 사람을 가리키고, 읽는 사람은 그 옆에서 읽는다. 궁합 두 kind 의 중립
제3자 문체와 갈리는 자리이니 바꾸려면 여기부터 고친다.

_Avoid_: visibility, 공개 범위(그것은 kind 가 아니라 접근 근거가 답한다)

**용어 판**(`terminology`):
사주 용어의 **이름을 사용자 본문에 부를 것인가**를 정하는 프롬프트 조립 값. `annotated`
는 뜻을 먼저 놓고 필요하면 이름을 뒤에 달고, `plain` 은 이름을 부르지 않고 그 말이
가리키는 성향·관계·상황을 바로 쓴다. **규칙 한 줄이 아니라 절과 본보기가 함께 갈린다** —
「이름을 쓰지 마라」와 「이름을 숨기지 말고」가 한 프롬프트에 함께 서 있으면 이기는 쪽은
눈앞의 본보기다.

**`plain` 은 치환 사전이 아니다.** 한 이름을 정해진 한 마디로 옮겨 두고 갈아 끼우면 그
옮긴 말이 새 전문용어가 되고, 같은 이름이 자리마다 다르게 나타난다는 사실이 사라진다.
**오행(목·화·토·금·수)은 여기서 빠진다** — 「금이 셋이에요」는 한국어에서 자연스러운
말이고, 그것을 세는 것이 이 엔진이 하는 일이다(ADR 0029).
_Avoid_: 쉬운 말 모드(무엇이 쉬워지는지 말하지 않는다), 용어 금지

## Flagged ambiguities (이어서)

- 「뻔한 글을 막는다」가 무엇을 재는 것인지 갈렸다 — 해소: **결론이 Evidence 에 의해
  결정됐는가**이지 문장이 독창적인가가 아니다. 그래서 이름을 `근거 밀착성` 으로 바꿨다.
  만점 조건을 「서로 충돌하는 근거를 찾았는가」로 두려다 물렀다 — 그렇게 두면 모델이 없는
  긴장을 만들어 점수를 얻고, claim 상한 규율과 정면으로 싸운다. 만점은 **반사실 민감도**다:
  근거가 바뀌면 해석도 실질적으로 다시 써야 하는가.
- 「AI 해석을 붙인다」가 완성인지 실험인지 갈렸다 — 해소: **실험 인프라**다. 프롬프트와
  엔진·AI 책임 경계는 계속 비교할 수 있다. 화면은 프롬프트의 출력 구조를 알지 않고
  (원문 Markdown 그대로), 현재 점수와 해석은 같은 Reading에서 읽는다. 결과 생성 요청이
  성공하면 둘과 근거·생성 설정을 통째로 교체하고 이전 결과는 참조하지 않는다.
  JSON·프롬프트·검사 결과는 내부 테스트 화면에서만 본다.

## Glossary (이어서) — 안전 운영

**신고**:
운영자가 봐야 할 일이 있었다고 남기는 **기록**. **차단이 아니다** — 신고는 상대에게
알리지 않고, 후보 목록에서 사라지게 하지도 않는다. 되돌릴 수 없는 개인적 결정인 차단과
달리 **사건이라 여러 번 쌓인다**: 같은 사람에게 두 번 일어난 일은 두 건이다. 마주친 적
있는 사람만 신고할 수 있다 — 안 그러면 이 문이 「그 계정이 있는가」를 묻는 문이 된다.
_Avoid_: 차단, 숨김, 제재(제재는 운영자가 하는 것이고 신고는 그 근거일 뿐이다)

**제재**:
운영자가 계정에 거는 상태(`suspended`). **신고의 결론이지 신고 자체가 아니다.** 어느
표에도 처분을 적지 않는다 — 제재는 `app_user.status` 한 자리가 들고, 신고 기록에는
「봤다」만 남는다.
_Avoid_: 신고, 정지(정지는 되돌릴 수 있는지 말하지 않는다)

**계정 삭제 요청**:
사용자가 그만 쓰겠다고 낸 요청(`deletion_requested`). **삭제가 아니다** — 요청이
접수되면 후보 노출이 꺼지고 살아 있던 요청이 정리되고 바깥으로 나가는 길이 다 막히지만,
저장된 자료는 그 자리에서 지워지지 않는다. 실제 삭제는 운영자가 하고, 무엇을
삭제·익명화·보존할지는 공개 출시 전에 정한다. 이미 성립한 **Match** 처럼 두 사람의 것인
기록은 한쪽이 지울 수 없다.
_Avoid_: 탈퇴, 삭제(둘 다 그 자리에서 사라진다고 읽힌다)

## Flagged ambiguities (이어서)

- 「삭제를 요청한 계정」과 「중지된 계정」이 같은 상태여야 하는지 갈렸다 — 해소: **막는
  것은 같고 이유가 다르다.** 값을 하나로 합치면 화면이 「중지되었습니다」라고만 말하게
  되고, 자기가 요청해서 그렇게 된 사람에게 그 문장은 거짓이다. 그래서 `status` 에 값을
  하나 더하되 **새 관문은 두지 않았다** — 이미 모든 문이 `status = 'active'` 를 묻고
  있으므로 고칠 문이 없다. 좁힘을 두 자리에 적으면 바깥쪽 문이 하나를 빠뜨린다.
