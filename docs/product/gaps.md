# 간극 대장 — 아직 없는 것 · 안 정한 것 · 어긋난 것

`docs/prd.md` 가 지금 모양을 적는다면 이 문서는 **그 모양과 다음 사이의 틈**을 든다. §7(하기로
정한 것) · §8(안 정한 것) · 코드가 문서와 어긋난 자리 · 이슈 #88 · 세션 메모에 흩어져 있던 것을
2026-09-22 에 한 장으로 모았다(ADR 0089). 요구사항이 아니다 — 무엇을 만들지는 PRD 가 말하고,
여기는 **상태 · 끝났다고 말할 조건 · 언제 닫는가**만 든다.

**상태는 다섯뿐이다.** `정했다`(무엇을 만들지 정했고 아직 없다) · `미정`(무엇을 할지 안 정했다) ·
`어긋남`(코드와 문서가 다르다) · `보류`(사용자가 써 본 뒤 정한다, 2026-09-15) · `결정 대기`(사람의
답이 필요하다). 닫히면 줄을 지우고 `docs/product/prd-changelog.md` 에 날짜와 함께 적는다.

**띠는 넷이다** — 언제 닫는가. 운영 베타(지금) · 채팅 안전 베타 · 공개 출시(PRD §7.0) · 정비(위임
준비). 「써 보고 정한다」는 띠가 아니라 상태(`보류`)다.

**§ 표시는 `docs/prd.md` 의 절**이고, 그 절이 실제로 있는지는 `scripts/code-rules.test.ts` 가 잰다.

## 운영 베타에서 닫는다

지금 줄이 없다 — G-60(프로필 사진 여러 장)은 2026-09-26 에 카드에서 넘겨 보기까지 운영에 서서 닫혔다.

## 채팅 안전 베타

지금 줄이 없다 — G-14(실시간 접속 표시)는 2026-09-23 에 「안 한다」로 닫혔다(§7.2).

## 공개 출시

| # | 무엇 | 출처 | 상태 | 끝났다고 말할 조건 | 메모 |
| --- | --- | --- | --- | --- | --- |
| G-20 | 본인인증이 가입의 문이다 — PASS · 만 19세 · CI 로 한 사람 한 계정 · 무료 몫 평생 한 번 | §7.3 · ADR 0101 | 정했다 | 인증 없이 가입이 안 끝나고, 같은 CI 의 두 번째 계정이 막히고, 탈퇴 뒤 재가입한 계정의 무료 몫이 0 이다 | 베타 계정은 공개 출시 때 인증만 받는다. 떠난 뒤 남는 HMAC 식별값의 보관 적정성은 G-25. 인증은 G-21 과 같은 PortOne 연동에 KCP 휴대폰 본인확인이 후보다 — PortOne 은 PG 가 아니라 연동 층이라 계약은 PG · 본인확인 기관과 따로 한다. **CI 제공을 계약 조건으로 확인한다**(KCP 는 DI 는 늘 주고 CI 는 가맹점 설정에 따른다). **운영자 승인(2026-09-24) — 최소 저장안:** 본인확인에서 남기는 것은 만 19세 이상 여부 · 확인 시각 · CI HMAC 셋뿐이다 — 생년월일은 나이를 판정한 뒤 버리고 이름 · 휴대폰 번호 · 통신사 · 성별 · DI 는 남기지 않는다. CI HMAC 도 개인정보로 다룬다(앱 역할 · 화면이 못 읽는 자리, 키는 서버 비밀에만, 새면 교체 — 교체 뒤 옛 값의 처분은 이 줄의 구현 PR 이 G-25 ⑧ 의 답과 함께 정한다, ADR 0101 추기). 처리방침 문장은 `docs/legal/privacy-v8-draft.md` 2절. **변호사 검토(G-25)가 끝날 때까지 본인확인을 실제로 열지 않는다** — 가입의 문으로 켜는 것과 운영 키를 넣는 것이 그 걸음이다. 구현 · 샌드박스 연동 · 시험은 막지 않는다(`docs/legal/README.md`) |
| G-21 | PG 결제 — 풀이권 묶음 넷(1 · 3 · 5 · 20회)을 판다 | §7.0 · ADR 0100 · 0102 | 정했다 | 산 몫이 한도 위에 얹혀 잔액에 서고, 무료 몫이 1개가 되고, 저장 자리 10명이 걷히고, 국내 카드로 묶음이 팔린다 | 가격은 정했다(§7.0 표, 2026-09-23). **PG 는 후보다** — PortOne V2 + NHN KCP, 점술 · 매칭 업종 입점과 CI 제공을 확인한 뒤 확정한다. 판매는 G-25 의 철회 기준과 결제 전 고지 뒤에 연다. **변호사 검토(G-25)가 끝날 때까지 판매를 실제로 열지 않는다** — 판매 스위치를 켜는 마이그레이션과 PG 운영 키가 그 걸음이다. 장부 · 웹훅 · 화면 구현은 막지 않는다(`docs/legal/README.md`). 자리 10명을 걷기 전에 궁합의 두 사람 고르는 칸이 찾아 고르는 모양이 된다(ADR 0102) — **만들었다(2026-09-24, #193)**: `select` 둘을 이름을 입력하거나 목록에서 고르는 combobox(APG 자동완성 방식)로 바꿨다(`app/person-combobox.tsx`, 판단은 `src/lib/people/pick.ts`). 증거: 단위 `pick.test.ts`(좁히기 · 옮기기 14건), e2e `signed-in.spec.ts` 「저장한 사람이 많아도 이름을 쳐서 좁히고 키보드로 고른다」(스물여섯 명 · 주소로 미리 골라져 들어오는 길 · 다섯 줄 반 높이와 스크롤 · 결과 없음의 `aria-expanded` · status) · 「저장한 사람이 없으면 두 칸 다 적는 칸으로 선다」 · 「저장한 사람이 하나면 첫 칸에 앉고 두 번째는 적는 칸이다」, 두 기기. **사람 목록(`/me/people`)도 같은 물음을 받는다** — **쟀고 고쳤다(2026-09-24)**: 0 · 1 · 10 · 26 · 100명을 넣고 프로덕션 빌드로 쟀다. 카드가 데스크톱 254px · 휴대폰 334px 이라 첫 화면에 온전히 서는 카드는 수와 상관없이 하나였고, 끝의 사람까지 화면을 내리는 수가 데스크톱 4 · 10 · 37, 휴대폰 5 · 13 · 48(10 · 26 · 100명)이었다. 서버 읽기는 수와 상관없이 같았다 — 문 넷(자리 · 목록 · 풀이 · 사람 표 `in` 한 번)에 N+1 없음, 100명의 응답 첫 바이트 약 0.3초 · HTML 1.1MB(전송 32KB). 그래서 페이지 나누기 · 가상 스크롤 · 정렬은 더하지 않고 **여섯부터 목록 위에 찾는 칸**을 세웠다(`app/me/people/finder.tsx`, 판단은 `pick.ts` 의 `findsInList` · `findStatus`) — 칸을 누르고 이름 세 글자면 백 명 중 끝의 사람이 스크롤 없이 첫 화면에 선다. 증거: 단위 `pick.test.ts` 「사람 목록에서 찾기」 셋, e2e `signed-in.spec.ts` 「저장한 사람이 스물여섯이면 목록 위의 칸에 이름을 쳐서 좁힌다」 · 「저장한 사람이 백이어도 끝의 사람이 이름을 치면 첫 화면에 선다」 · 「저장한 사람이 없거나 하나면 찾는 칸이 안 선다」, 두 기기. 칸이 서면 휴대폰의 첫 카드가 60px 내려가 첫 화면에 온전히 서는 카드가 0이 된다(걸침 1). **정한 것(2026-09-23):** ① 공개 출시 전에 가입한 계정은 무료 총 5를 지킨다 — 이미 준 몫을 소급해 빼지 않는다. 가입 시각 경계로 가르는 것을 권한다 — 전원에게 얹는 +4 행은 예외 표(ADR 0043)를 정책으로 쓰는 일이 되고, `app_user.signup_code` 는 코드 전 계정이 비어 있어 표지가 못 된다 ② 무료 1개로 인연 요청을 보낼 때는 누르기 전 확인 창이 그 1개가 답이 올 때까지 묶인다는 것과 사는 길을 함께 보인다(문구는 표로 따로 승인) ③ 쓴 순서는 무료 몫 → 오래된 묶음으로 본다 — 잔액 셈에는 안 들고 환불 셈에만 든다. **환불은 산 몫에만 있다** — 무료 몫과 예외 몫(ADR 0043)은 산 것이 아니므로 어떤 경우에도 돌려주지 않는다. 돌려줄 수 있는 것은 「그 묶음에서 아직 안 쓴 것」이고, 무료 몫을 먼저 썼다고 보므로 안 쓴 무료 몫이 환불로 바뀌는 길이 없다 ④ 일부 쓴 묶음의 자동 환불은 G-25 에서 철회 기준을 정한 뒤 만든다. 결제와 쓰임은 묶음별로 따라갈 수 있어야 한다 ⑤ 산 몫은 `reading_credit_grant` 와 다른 표가 들고 결제 주문과 이어진다. **⑤ ④ 의 장부가 섰다 — 판매는 닫힌 채(2026-09-24, ADR 0106)**: `20261011090000` 이 PG 에 중립인 표 다섯을 세웠다 — 결제 주문(`reading_order`, 대기 → 승인 → 부분 · 전액 환불 · 대기 → 취소, 멱등 열쇠) · 산 묶음(`reading_bundle`, 주문 1:1 · 가격 스냅샷) · 환불(`reading_order_refund`) · 제공자 알림(`payment_event`, 같은 알림 한 번 — G-23 ⑥ 이 서명 검증을 얹을 자리) · 사용 이력(`reading_credit_use`, 어느 시도 · 요청이 무료 · 예외 · 어느 묶음 · 몫 밖을 썼나, 예약 → 확정 · 풀림). 한도는 `무료 + 예외 + Σ(산 수 − 걷은 수)` 이고 잔액은 여전히 센다 — 산 몫이 0 인 지금 모든 계정의 한도 · 잔액이 그대로다. 쓴 순서(③)는 자리를 잡는 순간 트리거가 정하고, 수락은 요청의 예약을 그대로 이어 받는다. 승인 · 취소 · 환불 문은 `service_role` 만, 주문을 여는 문은 판매 스위치(`reading_sale_is_open()` = false)가 막는다 — 같은 열쇠로 나란히 열어도 하나를 돌려주고 같은 열쇠를 다른 묶음에 쓰면 거절한다(`20261013090000`). 환불은 안 쓴 회차만 걷고(예약 중은 풀릴 때까지 아님) 금액은 사람이 넣는다 — 산식은 G-25 ⑥ 뒤. 같은 PR 이 `request_match` 가 셈 전에 풀이 시작과 같은 사람 자물쇠를 잡게 고쳤다 — 전에는 둘이 나란히 마지막 한 번을 읽으면 둘 다 지나갔다. 증거: pgTAP `48_reading_bundle_ledger`(52 — 전 계정 한도 불변 · 권한 · 쓴 순서 · 예약/풀림/이어 받기 · 환불 한계 · 같은 자물쇠 · 떠날 때 옮김 · 5년 파기; 자물쇠 한 줄을 빼면 · 이어 받기를 빼면 · 한도에 1 을 더하면 각각 붉다) · `25_reading_credit_grant`(상수를 읽는 한 자리가 `reading_credit_shares` 로) · `13_reading`(서버 문 열여덟), 단위 `src/lib/reading/bundle.test.ts`, runbook 「산 풀이권」 · `51_payment_event_retry`(같은 알림 · 환불 번호의 재시도는 같은 요청일 때만, `20261015090000`). **판매를 열 때 남은 것:** PG 웹훅(G-23 ⑥)이 `approve_reading_order` 를 부른다 · `/ops/**` 의 환불 셈 화면(문 `operator_reading_refund_basis` 는 섰다) · 결제 화면과 탈퇴 판의 남은 수량(G-25 ⑦) · 스위치를 켜는 마이그레이션 · **상품 화면 · 결제 전 고지의 할인율이 `discountAgainstSingles`(정수 내림)와 같은 기준인가 — 판매를 여는 PR 의 체크리스트에 둔다.** **할인율 표시 — 운영자 결정(2026-09-24):** 가격은 그대로, 표시는 보수적 정수 내림 — 3회 12% · 5회 18% · 20회 39%(실제 12.2 · 18.8 · 39.8%, 반올림 12 · 19 · 40 은 5회 · 20회에서 실제보다 컸다). 값은 `src/lib/reading/bundle.ts` 의 `discountAgainstSingles` 하나이고 PRD §7.0 표 · 약관 초안 제12조 · ADR 0106 이 같은 값이다. 증거: 단위 `bundle.test.ts` 「정수 내림으로 0 · 12 · 18 · 39」 · 「표시는 어느 묶음에서도 실제보다 크지 않다」 — `Math.round` 로 되돌리면 둘 다 붉다. **운영자 승인(2026-09-24):** ① 쓴 순서는 무료 → 예외 → 오래된 유료 묶음(③ 에 예외 몫의 자리를 더한다, ADR 0106 결정 4) ② 결제 보존에 로그인 이메일을 넣지 않고 PG 거래 번호 · 내부 주문 번호로 추적한다(ADR 0106 결정 11) ④ `/me/people` 의 찾는 칸은 여섯부터(ADR 0102 추기) |
| G-23 | 공개 모집에 견딜 보안 점검 — **자체 점검표** | §7.0 · ADR 0102 | 정했다 | 아래 열둘이 각각 **완료 조건을 넘고 증거(커밋 · 시험 이름 · runbook 절)를 이 줄에 남긴다** | **정한 것(2026-09-23):** 외부 모의해킹은 결제가 운영에 들어간 뒤 따로 한다. 빗장은 앱 메모리가 아니라 **DB 에서 원자적으로** 센다. **점검표** — ① 운영 의존성 취약점: `npm audit --omit=dev` 가 high 이상 0 이고 CI 가 그것을 잰다 — **닫힘(2026-09-23)**: Next 16.3.1 → 16.3.6(critical 셋 · sharp high)은 `1e1f6c8`, 2026-09-23 값은 운영 46개에 0. CI 차선 `audit`(`npm audit --omit=dev --audit-level=high`)이 **의존성 목록을 바꾼 PR** 의 머지를 막고, 새 advisory 는 main 푸시 · 하루 한 번의 일정이 잡아 `ci-main-red` 가 「`audit` 만 붉다」고 적는다(ADR 0104). 증거: `ci-plan.test.ts` 「운영 의존성 감사」 · `main-red.test.ts`, `1e1f6c8` 전 잠금 파일에서 차선 명령이 1 로 끝남. 절차: runbook 「운영 의존성 취약점」 ② 보안 헤더 — **CSP 강제(2026-09-23)**: 한 정책 `Content-Security-Policy` 가 `default-src 'self'` · Supabase 하나에만 연 `connect-src` · `frame-src`/`object-src 'none'` · `base-uri`/`form-action 'self'` · `frame-ancestors 'none'` 을 막고, `X-Frame-Options: DENY` · `nosniff` · `Permissions-Policy` 가 함께 선다(`next.config.ts`). `Report-Only` 는 걷었다. 운영 화면이 부르는 출처는 브라우저로 재어 전부 제 출처였다(Vercel Analytics · 툴바 없음). 어긴 자리를 받는 `report-uri` 는 두지 않았다 — 되돌리는 법은 runbook 「CSP 가 화면을 막을 때」. 증거: e2e `security-headers.spec.ts`(강제 헤더, 바깥 이미지를 `enforce` 로 막는 것)와 CSP 자동 손잡이 — 익명 spec 은 `anon.ts`, 로그인 · 관문 spec 은 `session.ts` 를 지나 **시험 전부**에서 위반 0, dev 와 운영 빌드 둘 다. **`script-src` · `style-src` 의 `'unsafe-inline'` 을 뺄 것인가 — 정했다(2026-09-24, 운영자): ⓒ 지금은 유지하고, 결제 · 본인인증(PortOne V2 + KCP)을 샌드박스로 연동한 뒤 nonce 전환을 다시 검토한다** — 아래 출처와 `form-action` 이 그때 재어져야 모양이 정해진다. `next.config.ts` 는 그대로다. 잰 값: ⓐ nonce: 돈다(운영 빌드 e2e 전부 위반 0). 정적 넷(`/` · `/_not-found` · `/auth/denied` · `/me/discovery`)이 전부 동적이 되고 proxy 가 모든 화면 요청에 돈다 — 운영에서 잰 첫 바이트 정적 63ms · 동적 118ms(중앙값), 콜드 1.35초 ⓑ SRI(`experimental.sri`): 정적은 지키나 Next 의 인라인 조각에 해시가 없어 홈만 열어도 위반 4, 화면이 안 선다 — 대안이 못 된다 ⓒ 지금처럼 둔다. PG · 본인인증(PortOne V2 + KCP)은 `cdn.portone.io`(script) · `checkout-service.prod.iamport.co`(frame · connect) 외 `connect-src` 넷을 더 연다 — 샌드박스에서 재야 확정. 값과 목록은 `docs/notes/csp-nonce-2026-09-23.md` ③ `security definer` 함수의 `search_path` — **닫힘(2026-09-23)**: 이미 129개 전부 `""` 였다. 증거: pgTAP `42_definer_search_path`(빠진 함수를 넣으면 이름으로 잡는 것을 흔들어 확인) ④ 사람 저장 빗장 시간 20명 · 하루 100명, 총량 없음 — **닫힘(2026-09-23)**: `20261007090000` 의 `person_save_rate` 트리거. 증거: pgTAP `43_save_and_report_brakes` ⑤ 신고 빗장 하루 20건 + 검토 전 같은 대상 · 같은 사유 중복 막음 — **닫힘(2026-09-23)**: 같은 마이그레이션의 `hold_report_quota` 와 두 신고 문, `20261008090000` 이 중복을 하루 수보다 먼저 판정하고 조회 인덱스 셋을 더했다. 증거: pgTAP `43_save_and_report_brakes` · `34_chat`. **「검토 전」은 처리 필요다(2026-09-24, `20261017090000`, ADR 0107 정정)**: 두 문의 중복 판단이 목록과 같은 `report_is_open` 을 불러 추가 확인 필요로 보류한 동안에도 같은 신고를 거절한다(전에는 `reviewed_at is null` 이라 보류 중에 또 쌓였다). 조치 없음 · 경고 · 이용 정지 결정으로 끝나면 다시 낼 수 있다. 받치는 인덱스는 술어 없는 `report_by_pair` 로 바꿨다. 증거: pgTAP `53_report_duplicate_is_open`(고치기 전 12 중 6 붉음). 두 문은 가입 완료도 묻는다 — G-20 이 서면 가입이 인증을 품어 「인증을 마친 계정만」이 된다 ⑥ PG 웹훅: 서명 검증 · 금액을 서버의 주문과 대조 · 같은 알림이 두 번 와도 한 번만 반영 — 시험. **문과 시험이 섰다, 가맹 샌드박스에서 한 번 재면 닫힌다(2026-09-24)**: `POST /api/portone/webhook`(`app/api/portone/webhook/`)이 PortOne V2 의 알림을 받아 ⓐ Standard Webhooks 서명(`webhook-id` · `webhook-timestamp` · `webhook-signature`, 앞뒤 5분 — SDK 없이 `node:crypto`)을 검증하고 ⓑ `Transaction.Paid` 만 다루며 ⓒ 금액은 알림 본문이 아니라 PortOne 결제 단건 조회(`GET /payments/{paymentId}`)에서 받아 상태 `PAID` · 우리 상점 · `KRW` 를 본 뒤 ⓓ 결제 번호(`rdo_…`)에서 주문을 되짚어 `approve_reading_order(주문, transactionId, 받은 금액, webhook-id)` 를 부른다 — 금액 대조와 「같은 알림은 한 번」은 DB 가 든다. 다시 보내도 같은 답(서명 · 금액 거절 · 닫힌 주문 · 다른 요청의 알림 번호)은 2xx · 401 · 400 으로 끝내고 까닭은 기록에만, 설정 · PortOne · DB 에 못 닿으면 503 으로 재전송을 부른다. 켜는 값 셋(`PORTONE_WEBHOOK_SECRET` · `PORTONE_API_SECRET` 비밀, `PORTONE_STORE_ID` 설정)이 없으면 503 이고 판매 스위치는 그대로 꺼져 있다. 증거: 단위 `signature.test.ts`(규격의 공개 시험 벡터 · 본문 한 바이트 · 5분 창 · 빈 비밀) · `settle.test.ts`(금액은 PortOne 에서 · 같은 알림 번호 · 401 이면 아무것도 안 부름 · 거절 코드별 답 · 결제 번호 모양이 마지막 `open_reading_order` 정의와 같음 — 시각 검사 · 서명 비교 · `PAID` 검사를 빼면 붉다), DB 쪽은 pgTAP `48_reading_bundle_ledger` · `51_payment_event_retry`, `secret-env.test.ts`(새 이름의 갈래 · 잠금 · runbook 줄), runbook 「결제 알림 — PortOne 웹훅」 · 「비밀이 새면」의 두 줄. **닫는 조건:** 가맹 뒤 샌드박스 결제 하나로 묶음이 서고, 알림 본문 · 결제 조회의 모양이 문서대로인 것을 본다(#218 의 4). 그때 정할 것 — 거래 번호로 `transactionId` 와 `pgTxId` 중 무엇을 남기나, 환불 번호가 주문 사이에 겹치나. **DB 쪽이 먼저 고쳐졌다(2026-09-24, `20261013090000`)**: `approve_reading_order` 는 금액이 다르면 던지지 않고 `(refused, null)` 을 돌려줘 거절한 알림이 `payment_event` 에 남는다(전에는 되감겼다). 웹훅은 이 결과를 읽는다. **떠날 때 승인된 적 없는 주문의 거절 알림도 5년 — 최소 칸만(2026-09-24, 운영자 결정, `20261018090000`, ADR 0106 추기)**: 전에는 승인된 적 있는 주문만 옮겨 거절만 받은 주문의 알림이 계정과 함께 사라졌다. 이제 거절 알림이 하나라도 있으면 `retention.refused_reading_payment` 에 주문 번호 · 계정 내부 번호 · 제공자 · 가맹점 주문 번호 · 주문 금액 · 주문 시각 · 거절 알림(번호 · 종류 · 받은 금액 · 받은 시각 · 결과)만 옮겨 마지막 거절 알림부터 5년 둔다 — 이메일 · 주문 열쇠 · 묶음 · 상태는 없다. 같은 크론(`payment-retention-purge`)의 같은 문이 두 표를 함께 지우고, 앱은 못 읽는다. 증거: pgTAP `54_refused_payment_retention`(고치기 전 13 중 9 붉음 — 칸을 하나 더하거나 두 번째 표의 지우기를 빼면 붉다). 법적 범위는 변호사 검토 B-8(`docs/legal/counsel-request.md`). **같은 알림 번호의 재시도는 같은 요청일 때만(2026-09-24, `20261015090000`)**: 부딪힌 알림의 주문 · 종류 · 금액(승인된 알림이면 주문의 거래 번호까지)이 이번 부름과 모두 같을 때만 처음의 결과를 돌려주고, 하나라도 다르면 `22023` 으로 던진다 — 전에는 주문 A 에서 승인된 알림 번호로 주문 B 를 부르면 B 가 묶음 없이 `applied` 를 받았다. 던져도 처음의 알림 줄이 그대로라 잃는 기록이 없다. 환불도 같은 모양이었다 — 같은 환불 번호를 다른 금액 · 회차 · 사유로 부르면 이제 `22023`. 증거: pgTAP `51_payment_event_retry`(고치기 전 20 중 9 붉음). 제공자가 환불 번호를 주문 사이에 겹쳐 쓰는지는 PG 가 정해지면 본다 ⑦ PASS 결과는 서버가 인증사에서 다시 받아 확인하고 화면이 보낸 값을 믿지 않는다 — 시험 ⑧ 비밀키(무료 지급 HMAC · PG · 본인확인)는 서버 환경변수에만 있고, 새는 경우의 절차가 runbook 에 선다 — **닫힘(2026-09-24)**: 지금의 비밀(Supabase 비밀 키 · OpenAI 키 · webhook 서명 비밀 · `CRON_SECRET`, 로컬 갈래 하나)은 Vercel 서버 환경변수에, 나머지는 Vault 에만 있고 읽는 모듈 셋이 `import 'server-only'` 로 잠겼다. HMAC · PG · 본인확인 키는 아직 코드에 없다 — 들어오는 PR 은 `scripts/secret-env.mjs` 의 갈래와 runbook 표에 줄이 서야 초록이다. 증거: `scripts/secret-env.test.ts`(갈래 · 잠금 · runbook 줄 — 잠금을 지우고 새 이름을 넣으면 붉다), `npm run build` 끝의 `scripts/secret-env.mjs`(브라우저로 가는 파일의 비밀 이름 · 값 — 가짜 값을 일부러 싣은 빌드가 섰다, Vercel 배포 빌드도 진짜 값으로 지난다), runbook 「비밀이 새면 — 교체가 먼저다」 ⑨ 운영자 계정(Supabase · Vercel · GitHub · PortOne · **AWS** — 접속기록 반출 버킷, ADR 0105) 전부 MFA — runbook 에 확인 날짜 ⑩ 운영자 접속기록 1년 이상(G-25 ③) — **구조가 섰다, AWS 계정이 켜지면 닫힌다(2026-09-24, ADR 0105)**: 개인정보 열람은 `/ops/**` 화면으로만, 원격 SQL 은 개인을 가리키지 않는 것만, 개인정보 직접 SQL 은 사람만 break-glass 로(에이전트는 예외 없이 금지). DB 안의 추가만 되는 표 `audit.operator_access` 에 운영자 문 셋이 읽을 때마다 한 줄(운영자 id · 시각 · 동작 · 대상 신고 id 또는 거른 조건 · 성공/거절)을 같은 트랜잭션에서 적고, 거절은 앱의 문이 `note_operator_denial` 로 따로 적는다(던진 트랜잭션은 되감긴다). `npm run db:remote` 는 목적 없이 안 돌고 목적 · SQL sha256 · 실행자만 적는다. 크론 `/api/cron/audit-export` 가 매일 지난 반출 뒤의 범위를 JSON Lines(머리 = 행 수 · 첫/마지막 번호 · sha256)로 S3(Versioning + Object Lock, Governance 로 재고 Compliance 400일)에 올리고 범위를 이어 적는다 — 켜는 값 넷이 없으면 「설정 안 됨」으로 조용히 끝난다. 증거: pgTAP `46_operator_access_log`(적힘 · 거절 · 누구도 못 고침 · 운영자도 표 직접 못 읽음 · 반출 이어 붙임) · `47_review_and_signup_pause`, 단위 `app/api/cron/audit-export/export.test.ts`(가짜 S3) · `scripts/db-remote.test.ts`, runbook 「운영자 접속기록」「운영 주기」「개인정보는 화면으로만」. **닫는 조건:** 사람이 AWS 계정을 열어 runbook 「반출」의 1 ~ 7 을 밟고, 첫 반출 객체가 S3 에 서고 `audit.operator_access_export` 에 범위가 적힌 것을 본다. **사람 몫:** 그 AWS 절차 · Supabase Account Audit 의 가장 오래된 날짜 확인 · GitHub 보안 로그(90일) 분기 내보내기 · PortOne 가맹 때 보존 기간 문의. **남는 틈:** 대시보드 SQL Editor 는 기록이 안 남는다(규약으로 막는다) · 운영자가 아닌 사람이 PostgREST 로 문을 직접 두드리고 거절 기록 문을 안 부르면 거절이 안 남는다 · 반출 실패는 Vercel 로그와 월 점검만 본다. **정정(2026-09-24, `20261013090000`)**: 10분 창은 커밋 안 된 줄을 못 봐 늦게 커밋된 낮은 번호가 영영 안 나갈 수 있었다 — 반출과 쓰기가 advisory 자물쇠 하나를 배타 · 공유로 나눠 쥐고(쓰기는 번호를 받기 전 문장 트리거에서) 창을 걷었다. 거절 기록 한 시간 서른 줄은 그 사람의 자물쇠 안에서 센다(나란히 두 번이면 31 이었다). 증거: 흐름 `scripts/check-db-races.mjs`(두 세션 경합 — 고치기 전 14 중 7 붉음, 뒤 14) · pgTAP `46_operator_access_log`(같은 자물쇠를 쥐는가 · 문장 트리거 — 트리거를 걷거나 자물쇠 줄을 빼면 붉다). **반출 실행의 둘레가 섰다(2026-09-24, `20261014090000`)**: 겹쳐 돌면 뒤는 「도는 중」(5분 임대) · 같은 범위의 두 번째 완료는 조용히 · 실행마다 결과 한 줄(오류는 분류만)과 집계 `audit.export_status()`(마지막 성공 · 연속 실패 · 밀린 행) · 실패 즉시와 이틀 무시도 · 무성공 알림(`notify_ops`) · 켜는 값 **모두 없음(조용히)과 일부만(오설정 — 실패 · 알림)**을 가름 · 자격은 Vercel OIDC → IAM 역할이 기본안(접근 키는 대안) · S3 객체를 내려받아 DB 기록과 견주는 `npm run audit:verify` · CLI 질의의 성공/실패가 새 줄 `cli.result` 로 남음(한 질의에 한 줄은 부분 유일 인덱스가 든다 — 나란히 적어도 한 줄, 같은 결과면 그 줄의 번호, 다른 결과는 거절, `20261015090000`, 흐름 `check-db-races` 5 가 고치기 전 2줄로 붉었다). 「반출 실패는 Vercel 로그와 월 점검만 본다」는 틈은 닫혔다. 증거: pgTAP `50_audit_export_runs` · 흐름 `check-db-races`(두 반출 실행 — 시작의 자물쇠를 빼면 붉다) · 단위 `export.test.ts`(겹침 · 꺼짐/오설정 · 실패 분류 · 성공 범위) · `audit-verify.test.ts`(가짜 S3) · `db-remote.test.ts`, runbook 「반출」 ⑪ Supabase 보안 advisor 경고 0 — 결과를 남긴다 — **닫힘(2026-09-24), 남은 것은 까닭과 함께**: 운영 WARN 87 → 71(같은 날 G-24 의 운영자 문 셋이 0029 에 더해졌다). `20261009120000` 이 상수 함수 열다섯의 `search_path`(0011 15 → 0)와 밖에서 아무도 안 부르는 definer 셋(`claimed_by` · `may_edit_person_input` · `beta_is_over`, 0028 3 → 2 · 0029 68 → 65, G-24 셋이 더해져 68)을 닫았다. 증거: pgTAP `44_advisor_lints` · `33_function_shape`, runbook 「보안 advisor」(다시 부르는 명령). 남은 70 은 앱이 부르라고 연 RPC 문이고(규칙째 끄면 새 문도 숨어 끄지 않는다), 유출 비밀번호 검사 1 은 **Pro 플랜부터**라 남겼다. 인증 설정은 바꾼 것이 없다. 이메일 공급자가 켜져 있다(앱은 구글만 쓰고, e2e · 운영 확인 계정이 비밀번호로 든다) — 끌지는 PASS 연동(G-20) 때 함께 본다. performance advisor — **정리(2026-09-24)**: INFO `unindexed_foreign_keys` 24 → 1 · `unused_index` 12 → 35. `20261012090000` 이 탈퇴 처분의 cascade 사슬 위 외래키 스물셋에 인덱스를 세웠고, 남은 하나(`app_user.notice_schedule_id`, 부모를 손으로 넣는다)는 까닭과 함께 남겼다. 안 쓰인 인덱스는 완전히 겹치는 것이 없어 하나도 안 지웠다 — 늘어난 스물셋은 새 인덱스가 아직 안 쓰인 것이다. 증거: pgTAP `49_foreign_key_indexes`, runbook 「성능 advisor」(각 인덱스가 받치는 질의) ⑫ 개발 의존성 감사(`@vitest/mocker` moderate · `js-yaml` high, 2026-09-23 기준)는 운영에 안 실리지만 정리한다 — **닫힘(2026-09-23)**: vitest 4.1.10 → 4.1.11(`@vitest/mocker` 함께) · js-yaml 4.3.1 → 4.3.2, 둘 다 같은 메이저라 `overrides` 없이. `npm audit` 전체 3(high 1 · moderate 2) → 0. CI 는 개발 의존성을 안 막는다 — 새로 뜨면 runbook 「운영 의존성 취약점」 끝 줄대로 손으로. **IP 보조 제한은 두지 않는다** — 새 계정으로 빗장을 넘는 길은 PASS · CI 한 사람 한 계정(ADR 0101)이 막고, 이용자 IP 수집은 G-25 ⑨ 가 아직 안 정했다 |
| G-24 | 관리자 화면 1차판 — 신고 증거 열람대 | §7.0 · ADR 0103 | 정했다 | 운영자는 현재 계정에 연결된 신고를 목록에서 찾고, 신고 당시 저장된 불변 대화 스냅샷에서 사용자가 고른 메시지와 앞뒤 문맥을 화면으로 읽는다. 탈퇴한 사람이 든 분리 보관 신고는 기존 runbook SQL로 읽는다 | **화면이 섰다(2026-09-24)**: `/ops/reports` · `/ops/reports/[reportId]`, 운영자 전용 문 셋(`20261009090000`), 읽기 전용. 증거: pgTAP `45_operator_reports`(운영자만 · 반환형에 이메일 · 출생정보 · 방 실마리 없음 · 떠난 사람의 신고 안 나옴) · e2e `chat.spec.ts` 「운영자는 신고 목록에서 신고를 열어 …」(비운영자 404 · 누름 0) · runbook 「신고와 차단」. **남은 것: 프로덕션 운영 검증 한 번** — 아래. **검토 기록과 열람 기록이 섰다(2026-09-24, ADR 0105)**: 신고에 `reviewed_by` · 결과(조치 없음 · 경고 · 이용 정지 · 추가 확인) · 판단 근거 · 제재를 받은 쪽 · 실행한 사람이 붙고 runbook 의 검토 SQL 이 채운다. 상세가 그 칸을 읽는다(신고한 사람에게는 안 열린다). 화면을 여는 것마다 접속기록(G-23 ⑩)에 남는다. **닫는 검증 — 누구나 그대로 밟는 열네 걸음(2026-09-24):** runbook 「신고 열람대의 운영 검증 — G-24 를 닫는 열네 걸음」이 걸음마다 SQL 과 통과 조건을 든다. 전제 — 관련 마이그레이션(`20261009090000` · `20261010090000` · `20261010100000` · `20261010110000`)이 운영에 있다 · 최신 main 이 Production Ready 다(runbook 「묶음 배포 — 최신 main 을 Production 으로 한 번」) · 실제 개인정보 없는 전용 테스트 계정이다 · 테스트 메시지에도 실제 이름 · 연락처 · 출생정보를 안 쓴다. 걸음 — ① 테스트 계정 A · B ② 둘 사이의 테스트 대화와 신고할 메시지 ③ 신고 1건 ④ `/ops/reports` 목록에 보임 ⑤ 처리 상태 · 신고 사유 · 대화 근거 거르기 각각 ⑥ 상세의 신고 내용 · 스냅샷 ⑦ 고른 메시지가 정확하고 앞뒤 문맥이 차례대로 ⑧ 접속기록에 목록 · 상세 · 스냅샷 열람이 각각 ⑨ runbook 의 검토 문(`review_report`)으로 `조치 없음`(또는 시험용 결과 — 추가 확인 필요는 처리 필요에 남고, 이용 정지 결정은 같은 트랜잭션에서 계정을 정지한다) ⑩ 상세의 처리 상태 · 검토 결과 · 검토 운영자 · 판단 근거 · 당시 제재 대상이 DB 값과 같음 ⑪ 비운영자는 같은 주소에서 404 이고 거절 기록이 남음 ⑫ 배포 SHA · 신고 id · 실행 시각 · 접속기록 id · 검토 결과 · 화면 확인 결과를 이슈에(틀 `.github/ISSUE_TEMPLATE/ops-verification.md`) ⑬ 테스트 자료 정리 또는 보존 방식 기록(신고는 `retention.report` 에서 6개월 뒤 자동 파기, 접속기록 줄은 지울 수 없다) ⑭ **열셋이 모두 통과일 때만 닫는다.** 메시지 본문이 나오는 ⑦ 의 질의는 사람이 break-glass 로 돈다 — 에이전트는 질의를 건네기만 한다. **배포 한도 — 운영자 결정(2026-09-24, 2026-09-25 에 뒤집음):** `ignoreCommand` 는 빌드만 건너뛰고 배포 횟수(Hobby 24시간 100)는 줄이지 못한다. 2026-09-24 에는 Preview 편의를 우선해 Git 배포를 켜 두었으나, 2026-09-25 에 한도에 두 번 닿아 머지된 수정이 운영에 못 들었다 — Git 배포를 끄고 묶음마다 손으로 한 번 올린다(ADR 0110). 검증 전에 「묶음 배포」로 최신 main 을 올려 전제를 세운다. **검토는 문 하나가 적는다(2026-09-24, ADR 0107)**: runbook 의 UPDATE 둘을 `review_report` 하나로 모았다 — 검토한 사람이 운영자인지 묻고, 이용 정지 결정은 기록과 계정 정지를 한 트랜잭션으로 한다(적는 순간에만 — 해제해도 기록은 남는다). 결과와 제재 대상이 표의 제약으로 맞물리고(`sanction_follows_the_outcome`, 떠난 사람의 신고에도 논리 제약 다섯), 처리 필요 = 안 봤거나 추가 확인 필요(`report_is_open`)로 목록 · runbook 질의 · 월 점검이 같은 정의를 쓴다. 3영업일은 접수부터. 화면 글자는 처리 필요 / 처리 완료 · 이용 정지 결정 · 추가 확인 필요 · 당시 제재 대상. 증거: pgTAP `52_report_review_door` · e2e `chat.spec.ts` 「운영자 목록은 처리 필요와 처리 완료로 가르고 …」. 운영 DB 의 두 표는 그날 0줄이라 새 제약을 어긴 줄이 없었다. 경고를 이용자에게 알리는 길은 G-57. 1차판에서 뺀 것(검토 완료 누름 · 제재 누름 · 메모 · 운영 현황 집계 · 실행한 운영자 표시)은 화면에 쓰는 누름이 필요해지는 날 새 줄로 연다 |
| G-25 | 개인정보 정책 · 약관 · 고객 대응 | §7.0 | 정했다 | 이용약관이 서고, 처리방침이 아래 넷(본인확인 · 결제 · 무료 지급 기록 · 접속 기록)을 들어 내용 개정으로 오르고, **변호사 검토 1회**가 끝난다 | **정한 것(2026-09-23):** ① 메시지 보존은 §7.1 의 90일을 처리방침이 옮겨 적는다 ② 결제 · 청약철회 기록 **5년**, 소비자 불만 · 분쟁 처리 기록(환불 요청 · 중복결제 · 취소 이의 · 미제공 신고) **3년** — 탈퇴 3일 처분의 예외로 따로 두고 앱은 못 읽는다(전자상거래법 시행령 제6조) ③ 개인정보처리시스템에 접속한 운영자의 접속기록 **1년 이상**(안전성 확보조치 기준 제8조) — 이용자 5만 명 이상이 되거나 고유식별정보 · 민감정보를 받게 되면 2년으로 다시 본다 ④ 고객 문의는 이메일과 앱 안 문의, **3영업일 안 첫 답**. 1372 안내는 문의 대응 절차에 두지 않는다 ⑤ 결혼중개업법은 지금 비해당으로 판단하고 제품 구조를 바꾸지 않는다 — 요청 때 예약 · 수락 때 자동 생성 그대로(ADR 0038 · 0100). 지키는 경계: 결혼 상대 · 성혼 · 결혼 상담이라 광고하지 않는다 · 결혼 의사나 혼인 조건을 받지 않는다 · 교제나 성혼을 보장하지 않는다 · 약관의 서비스 정의에 결혼 알선이 아님을 적는다 **변호사 검토로 정하는 것:** ⑥ 환불 산식 — 「쓴 회차를 1회권 가격으로 공제」와 「7일 뒤 10% 공제」가 유효한지. 무료 · 예외 몫은 환불하지 않는다는 것과 예약 중인 몫은 풀릴 때까지 대상이 아니라는 것은 정했다(G-21 ③). 묶음별 결제 · 사용 이력은 어느 산식이든 필요하므로 먼저 만든다 — **섰다(2026-09-24, ADR 0106)**: 운영자 문 `operator_reading_refund_basis` 가 묶음마다 산 수 · 쓴 수 · 예약 중 수 · 안 쓴 수 · 결제 금액 · 결제 시각 · 환불한 것을 한 번에 내고 접속기록에 남긴다. 떠난 사람의 결제 기록은 `retention.reading_payment` 에 마지막 결제 사건부터 5년(②) — 로그인 이메일은 옮기지 않는다. 승인된 적 없는 주문의 거절 알림은 `retention.refused_reading_payment` 에 최소 칸으로 마지막 거절 알림부터 5년(G-23 ⑥, 2026-09-24 — 5년 대상인지는 변호사 검토 B-8) ⑦ 탈퇴 때 남은 유료 풀이권 — 바로 소멸시킬지는 환불 기준과 함께 본다. 그때까지는 탈퇴 판에 남은 수량과 환불 신청 길을 분명히 보인다 ⑧ 무료 지급 HMAC 식별값의 보관 기간 · 목적 · 근거(개인정보 보호법 제21조, ADR 0101) — 기한 없음이 어렵다면 5년, 기한이 생기면 ADR 0101 을 다시 연다 ⑨ 이용자 IP · 접속 시각 3개월 — 통신비밀보호법(시행령 제41조②) · 전기통신사업자 해당 여부를 본 뒤 확정한다(G-52 에서 넘어옴, ADR 0098) ⑩ 이용약관 전문 — 공정위 디지털콘텐츠 이용 표준약관을 바탕으로 쓰고, 분쟁 해결 조항에 1372 · 분쟁조정기관을 넣을지도 여기서 정한다 ⑪ 결혼중개업법 적용 여부 — 서비스 구조를 보이고 짧게 확인한다. 신고 · 보증보험을 전제로 문서를 만들지 않는다 ⑫ **운영자 접속기록 1년인가 2년인가**(ADR 0105) — 「개인정보의 안전성 확보조치 기준」 제8조는 원칙 1년, 정보주체 5만 명 이상 · 고유식별정보 · 민감정보 처리 등은 2년이다. **본인확인에서 받는 CI 가 고유식별정보에 해당하는지**(ADR 0101 은 CI 원문을 안 남기고 HMAC 식별값만 둔다)와 그래서 2년인지를 묻는다 — 단정하지 않았다(요청서 `docs/legal/counsel-request.md` 에 이미 든 물음). 2년이면 S3 기본 보존을 730일로 올리고 이미 있는 객체의 보존을 만료 전에 늘린다(runbook 「반출」 4) **선행:** 사업자등록 · 통신판매업 신고(PG 신청 조건, G-21). **변호사 검토는 공개 출시 전 운영자가 한 번에 처리한다 — 다른 구현을 막는 blocker 가 아니다. 다만 결제 판매(G-21)와 본인확인(G-20)을 실제로 여는 일은 검토가 끝날 때까지 하지 않는다.** 묶음의 입구는 `docs/legal/README.md`(요청서 · 약관 초안 · 처리방침 v8 · 미결 질문 열여덟 · 빈칸), 표지는 `[출시 전 운영자·변호사 확인]`. **운영자 승인(2026-09-24):** 운영자 CLI 기록은 SQL 원문이 아니라 목적 · SHA-256 이다(ADR 0105 추기) **초안이 섰다(2026-09-24):** 이용약관 초안 `docs/legal/terms-draft.md`(과기정통부 고시 제2022-66호 「디지털콘텐츠 표준약관」의 조 구성 — 공정위 판 원문은 못 찾았다) · 처리방침 v8 `docs/legal/privacy-v8-draft.md` — **2026-09-24 에 구현과 맞춘 완성본으로 다시 썼다**(법인 소재와 처리 위치를 가름 — Supabase 서울 `ap-northeast-2` · Vercel 서버 `icn1` · 전송망은 세계, AWS 반출 서울 기본안 · OpenAI 로 가는 것 · 본인확인의 받는 것과 남기는 것 · 결제 장부 칸 그대로 · 접속기록 칸 · 쿠키와 저장소 · 보존과 크론, 부록 A 구현 대조 · B v6 대조표 · C 재확인 지점 · D 빈칸) · 변호사 검토 요청서 `docs/legal/counsel-request.md`(⑥~⑪ + CI 고유식별정보 여부 · 접속기록 1년/2년 · 서울 리전의 국외 이전 여부 · 본인확인기관의 지위). **화면은 그대로 `notice-v6` 다** — v7 반영(`NOTICE_VERSION` · `NOTICE_ACK_FLOOR` 를 `notice-v8` 로, 확인한 사람 전원이 다시 읽는 내용 개정)과 가입 폼의 약관 확인 칸은 **변호사 검토와 한글 문구 승인 뒤**다. **남은 것:** ㉠ 변호사 검토 1회 ㉡ 빈칸 — **사업자등록 뒤**(상호 · 대표자 · 주소 · 전화 · 사업자등록번호 · 통신판매업 신고번호 · 개인정보 보호책임자 · 고객 문의 이메일 · 시행일)와 **계약 뒤**(PortOne · PG · 본인확인기관의 이름 · 소재 · 처리 위치 · 넘기는 항목)뿐이다 — AWS 리전은 서울(`ap-northeast-2`) 기본안으로 채웠다 ㉢ v6 이 구현과 어긋난 자리 — 안전성 확보조치 절 · 자동 수집 장치 절이 없다, Supabase · Vercel 을 「미국」으로만 적는다, **궁합풀이가 OpenAI 에 두 사람을 부르는 이름과 사이를 싣는데 「여덟 글자와 사실만」이라 적는다**, 닉네임 · 소개 · 사진 · 메시지 · 신고를 받는 항목으로 안 적는다, 닫힌 방 메시지를 「90일이 지나면 지웁니다」라 하나 지우는 손은 달마다 도는 SQL 이다 — 전부 v7 에서 바로잡았고 v6 은 따로 고치지 않는다(v6.1 없음, 운영자 결정 2026-09-24) ㉣ 출시 전 운영 결정 — 이전 방침의 열람 경로 · 선택 항목 「다음 테스트 소식 받기」의 이름 · 문의 메일함의 3년 보관 · 접속기록 DB 원본의 파기 시점 ㉤ 앱 안 문의 창구 · 결제 내역의 철회 신청 칸이 아직 없다. 이용 정지의 소명과 경고의 이의 제기는 고객 문의 이메일로 받는다(운영자 결정 2026-09-24, ADR 0108 추기) — 새 창구를 만들지 않는다 |
| G-26 | 외부 알림(이메일 · 카카오 · 푸시) | §7.0 · CONTEXT 「앱 내 알림」 | 정했다 | 새 메시지 통보부터 | 지금 통보는 앱 안에서만 보인다. 경고 이메일(G-57, ADR 0108)도 이 제공자를 기다린다 |
| G-57 | 경고를 이용자에게 알린다 — 앱 안 안내와 이메일, 사유의 갈래만 | §7.0 · ADR 0107 · ADR 0108 | 정했다 | 경고를 받은 이용자가 다음에 앱에 들어올 때 맨 위의 안내로 사유의 갈래 · 경고한 날 · 이의 제기의 길 · 안내번호를 보고 「확인했습니다」를 누르면 확인한 시각이 남고, 같은 넷이 이메일로 한 통 가며, 어느 쪽에도 판단 근거 · 신고한 사람 · 신고 시각 · 고른 메시지 · 경고 횟수가 없다(pgTAP 으로 잠근다). 운영자는 「알렸는가」(확인 · 발송)를 이메일 없이 센다. 문구가 표 승인을 받았다 | **앱 안 안내가 섰다(2026-09-24, ADR 0108 추기)** — `20261019090000`: 경고의 칸 다섯(갈래 · 안내번호 `W-` + 네 글자 · 이용자가 확인한 때 · 발송한 때 · 발송 결과)이 두 표(`public.report` · `retention.report`)에, 검토 문의 갈래 인자(기본은 신고 사유), 이용자의 문 둘(`my_warning_notice` 는 안내번호 · 갈래 · 날짜만 · 확인 전 가장 오래된 하나 · `active` 에만 / `acknowledge_warning`), 운영자 목록의 안내번호 찾기와 상세의 안내번호 · 경고 갈래 · 이용자 확인. 화면은 `/me` 아래 모든 화면의 맨 위(`app/me/layout.tsx`)에 서고 문구는 운영자 승인(2026-09-24) 그대로다. 이의 제기 인정(`no_action` 으로 다시 적기)이면 안내가 내려가고 안내번호는 남는다. 증거: pgTAP `55_warning_notice` · e2e `chat.spec.ts` 「경고받은 사람은 확인할 때까지 안내를 보고 …」 · 단위 `src/lib/account/warning.test.ts`. **운영자 결정(2026-09-24, ADR 0108):** ① 수단 — 앱 안 안내와 이메일 **둘 다** ② 이용자에게는 **사유의 갈래만** — 판단 근거(메모)는 공개하지 않는다 ③ **최근 12개월 경고 3회부터** 운영자가 이용 정지를 **검토**한다 — 자동 정지가 아니다. **중대한 위반은 횟수와 상관없이 즉시 검토**(보기는 약관 초안 제21조에 맞췄다) ④ 이의 제기 · 이용 정지의 소명 — 고객 문의 이메일(G-25 ④ · ㉤, 첫 답 3영업일 안) ⑤ **경고 횟수는 이용자에게 말하지 않는다** ⑥ 안내번호로 운영자가 이메일 없이 경고를 찾는다. **운영 절차** — runbook 「경고를 적기 전에 — 셈과 무게」(12개월 셈 · 안내번호로 찾기 · 「알렸는가」 집계). **남은 것:** ㉠ 이메일 — G-26 의 제공자(계정 · 비용은 사람). 발송 칸은 비어 있고 아무것도 안 보낸다. 승인된 이메일 문구: 제목 「[서비스명] 운영정책 위반 경고 안내」, 본문 「안녕하세요. {날짜}에 {갈래}에 해당하는 이용이 확인되어 경고를 드립니다. 같은 일이 반복되면 이용이 제한될 수 있습니다. 이 경고에 이의가 있으면 이 메일에 답장하시거나 {고객 문의 이메일}로 알려 주세요. 3영업일 안에 답을 드립니다. 문의하실 때 안내번호 {번호}를 함께 적어 주세요.」 — **`other` 갈래를 이 틀에 어떻게 넣는지(앱 안은 「운영정책에 어긋나는 이용」)와 [서비스명]은 발송 잡을 만들 때 표로 묻는다** — 조율자의 추천은 `other` 를 「그 밖의 운영정책 위반」으로 넣는 것이다(추천, 미승인 — 발송 잡을 만들 때 표로 확정한다, 2026-09-24). `[서비스명]` 은 서비스명이 정해지기를 기다린다(G-58) ㉡ 고객 문의 이메일 주소(G-25 ㉡, 사업자등록 뒤) — 지금은 `src/lib/account/warning.ts` 의 `SUPPORT_EMAIL` 자리 표시이고, 공개 출시로 옮기면 `scripts/code-rules.test.ts` 가 이메일 모양이 아닌 것을 붉힌다. 운영 DB 의 경고는 0건이다(2026-09-24). **정한 것 하나(ADR 0108 추기):** 신고한 사람이 떠나 `retention.report` 로 옮겨진 경고는 이용자에게 안 선다 — 떨어진 보관을 앱이 읽는 길을 내지 않는다. 셈에는 든다 **안내번호는 두 표를 합쳐 하나다(2026-09-24, ADR 0108 정정)** — `20261020090000`: 표마다의 유일 제약만으로는 한 번호가 `public.report` 와 `retention.report` 에 하나씩 설 수 있었다(같은 씨앗으로 두 표에 나란히 적으면 둘 다 같은 번호로 커밋 — `check-db-races` 6 의 붉음 셋, pgTAP `56_warning_ref_is_one` 아홉 중 여덟 붉음). 번호마다 한 줄인 대장 `warning_reference` 를 두 표가 `(번호, 신고 id)` 외래키로 가리키고, 뽑는 문이 번호를 대장에 먼저 잡는다 — 나란히 적는 두 경고는 뒤가 기다렸다가 다른 번호를 받는다. 떠날 때 옮겨도 · 이의 제기 인정 뒤에도 번호는 그대로이고, 파기된 경고의 번호도 다시 안 쓴다. 증거: pgTAP `56_warning_ref_is_one`(9) · `scripts/check-db-races.mjs` 6(다섯) |
| G-58 | 서비스명 · 로고가 모든 자리에 반영되지 않았다 | §7.4 · ADR 0109 · `docs/legal/README.md` | 정했다 | 이메일 · 약관 · 처리방침까지 「점점」이 한 상수에서 오고, 로고가 이메일 · 공유 그림에도 선다 | **이름을 정했다 — 「점점」(운영자 2026-09-24, ADR 0109)**: 점으로 만든 관계 지도와 맞고 점술의 「점」을 품는다. 상수는 `src/lib/brand`(`SERVICE_NAME` · `SERVICE_TAGLINE` · 제목 틀)이고 뿌리 메타데이터 · 머리글 · 화면 제목 틀 · 공유 미리보기 · 공유 없음 화면 · 가입 설명이 그것을 쓴다. 로고는 `app/ui/logo.tsx`(가운데 큰 점 · 점선 궤도 · 커지는 작은 점 둘)와 `app/icon.svg`(다크 짝). 기능 이름으로서의 「만세력」(명식 보기 · 엔진)은 그대로다. **남은 것:** ㉠ 경고 이메일 제목의 `[서비스명]`(G-57 ㉠ — 발송 잡을 만들 때) ㉡ 이용약관 초안의 「만세력 이용약관」 · 「만세력 서비스」와 처리방침 v8 의 「만세력 서비스」 — 본문 글자는 다음 판을 올릴 때 바꾼다(처리방침은 판을 올리면 안내 확인이 다시 돈다) ㉢ ~~공유 미리보기 그림(`opengraph`)의 로고~~ **닫았다(2026-09-24)** — 그림 셋은 **2026-09-26 에 v3 로 다시 그렸다**(`public/brand/{saju,reading,compat}-share-v3.jpg`, 40~45KB) — 옛 종이 공예 삽화를 버리고 화면의 부품(크림 바탕 · 오행 파스텔 · 다섯 상징 · 점선 궤도 · 고운돋움)으로 나를 가운데 둔 궤도 · 여덟 글자 판 · 두 사람의 궤도를 그린다. 글자는 로고 · `SERVICE_NAME` · `SERVICE_TAGLINE` 과 판 위의 한자뿐이다. 판을 바꾸면 파일 이름의 판도 올린다(메신저가 주소로 그림을 붙든다). 굽는 도구 `scripts/brand-share-images.mjs` 가 상수를 읽고, 이름을 바꾸면 `scripts/brand-share-images.test.ts` 가 붉힌다(다시 굽는다). 대체 글도 `이름 — 소개 한 줄` 이다 |

## 정비 — 위임 준비

지금 줄이 없다 — G-42(운영 관측)는 2026-09-23 에 크론 실패 알림으로 닫혔다.

## 닫힌 것은 여기 없다

끝난 것은 `docs/product/prd-changelog.md` 가 날짜와 함께 든다. 「써 보고 정한다」 띠(G-30~37)는
2026-09-23 에 사람이 전부 답해 비었다 — 닫힌 것(G-30 · 31 · 32 · 33 · 34 · 35 · 36 · 37)은 changelog 에 있다.
`보류` 상태는 다음 「써 보고 정한다」를 위해 남긴다.
