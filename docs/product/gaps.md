# 간극 대장 — 아직 없는 것 · 안 정한 것 · 어긋난 것

`docs/prd.md` 가 지금 모양을 적는다면 이 문서는 **그 모양과 다음 사이의 틈**을 든다. §7(하기로
정한 것) · §8(안 정한 것) · 코드가 문서와 어긋난 자리 · 이슈 #88 · 세션 메모에 흩어져 있던 것을
2026-09-22 에 한 장으로 모았다(ADR 0089). 요구사항이 아니다 — 무엇을 만들지는 PRD 가 말하고,
여기는 **상태 · 끝났다고 말할 조건 · 언제 닫는가**만 든다.

**상태는 다섯뿐이다.** `정했다`(무엇을 만들지 정했고 아직 없다) · `미정`(무엇을 할지 안 정했다) ·
`어긋남`(코드와 문서가 다르다) · `보류`(사용자가 써 본 뒤 정한다, 2026-09-15) · `결정 대기`(사람의
답이 필요하다). 닫히면 줄을 지우고 `docs/product/prd-changelog.md` 에 날짜와 함께 적는다.

**띠는 넷이다** — 언제 닫는가. 운영 베타(지금) · 채팅 안전 베타 · 공개 출시(PRD §7.0) · 정비(위임
준비). 「써 보고 정한다」는 띠가 아니라 상태(`보류`)다.

**§ 표시는 `docs/prd.md` 의 절**이고, 그 절이 실제로 있는지는 `scripts/code-rules.test.ts` 가 잰다.

## 운영 베타에서 닫는다

| # | 무엇 | 출처 | 상태 | 끝났다고 말할 조건 | 메모 |
| --- | --- | --- | --- | --- | --- |

## 채팅 안전 베타

지금 줄이 없다 — G-14(실시간 접속 표시)는 2026-09-23 에 「안 한다」로 닫혔다(§7.2).

## 공개 출시

| # | 무엇 | 출처 | 상태 | 끝났다고 말할 조건 | 메모 |
| --- | --- | --- | --- | --- | --- |
| G-20 | 본인인증이 가입의 문이다 — PASS · 만 19세 · CI 로 한 사람 한 계정 · 무료 몫 평생 한 번 | §7.3 · ADR 0101 | 정했다 | 인증 없이 가입이 안 끝나고, 같은 CI 의 두 번째 계정이 막히고, 탈퇴 뒤 재가입한 계정의 무료 몫이 0 이다 | 베타 계정은 공개 출시 때 인증만 받는다. 떠난 뒤 남는 HMAC 식별값의 보관 적정성은 G-25. 인증은 G-21 과 같은 PortOne 연동에 KCP 휴대폰 본인확인이 후보다 — PortOne 은 PG 가 아니라 연동 층이라 계약은 PG · 본인확인 기관과 따로 한다. **CI 제공을 계약 조건으로 확인한다**(KCP 는 DI 는 늘 주고 CI 는 가맹점 설정에 따른다) |
| G-21 | PG 결제 — 풀이권 묶음 넷(1 · 3 · 5 · 20회)을 판다 | §7.0 · ADR 0100 · 0102 | 정했다 | 산 몫이 한도 위에 얹혀 잔액에 서고, 무료 몫이 1개가 되고, 저장 자리 10명이 걷히고, 국내 카드로 묶음이 팔린다 | 가격은 정했다(§7.0 표, 2026-09-23). **PG 는 후보다** — PortOne V2 + NHN KCP, 점술 · 매칭 업종 입점과 CI 제공을 확인한 뒤 확정한다. 판매는 G-25 의 철회 기준과 결제 전 고지 뒤에 연다. 자리 10명을 걷기 전에 궁합의 두 사람 고르는 칸이 찾아 고르는 모양이 된다(ADR 0102). **정한 것(2026-09-23):** ① 공개 출시 전에 가입한 계정은 무료 총 5를 지킨다 — 이미 준 몫을 소급해 빼지 않는다. 가입 시각 경계로 가르는 것을 권한다 — 전원에게 얹는 +4 행은 예외 표(ADR 0043)를 정책으로 쓰는 일이 되고, `app_user.signup_code` 는 코드 전 계정이 비어 있어 표지가 못 된다 ② 무료 1개로 인연 요청을 보낼 때는 누르기 전 확인 창이 그 1개가 답이 올 때까지 묶인다는 것과 사는 길을 함께 보인다(문구는 표로 따로 승인) ③ 쓴 순서는 무료 몫 → 오래된 묶음으로 본다 — 잔액 셈에는 안 들고 환불 셈에만 든다. **환불은 산 몫에만 있다** — 무료 몫과 예외 몫(ADR 0043)은 산 것이 아니므로 어떤 경우에도 돌려주지 않는다. 돌려줄 수 있는 것은 「그 묶음에서 아직 안 쓴 것」이고, 무료 몫을 먼저 썼다고 보므로 안 쓴 무료 몫이 환불로 바뀌는 길이 없다 ④ 일부 쓴 묶음의 자동 환불은 G-25 에서 철회 기준을 정한 뒤 만든다. 결제와 쓰임은 묶음별로 따라갈 수 있어야 한다 ⑤ 산 몫은 `reading_credit_grant` 와 다른 표가 들고 결제 주문과 이어진다 |
| G-23 | 공개 모집에 견딜 보안 점검 — **자체 점검표** | §7.0 · ADR 0102 | 정했다 | 아래 열둘이 각각 **완료 조건을 넘고 증거(커밋 · 시험 이름 · runbook 절)를 이 줄에 남긴다** | **정한 것(2026-09-23):** 외부 모의해킹은 결제가 운영에 들어간 뒤 따로 한다. 빗장은 앱 메모리가 아니라 **DB 에서 원자적으로** 센다. **점검표** — ① 운영 의존성 취약점: `npm audit --omit=dev` 가 high 이상 0 이고 CI 가 그것을 잰다 — Next 16.3.1 → 16.3.6(critical 셋 · sharp high)은 `1e1f6c8` 에서 닫았고 CI 에 거는 일이 남았다 ② 보안 헤더 — **CSP 강제(2026-09-23)**: 한 정책 `Content-Security-Policy` 가 `default-src 'self'` · Supabase 하나에만 연 `connect-src` · `frame-src`/`object-src 'none'` · `base-uri`/`form-action 'self'` · `frame-ancestors 'none'` 을 막고, `X-Frame-Options: DENY` · `nosniff` · `Permissions-Policy` 가 함께 선다(`next.config.ts`). `Report-Only` 는 걷었다. 운영 화면이 부르는 출처는 브라우저로 재어 전부 제 출처였다(Vercel Analytics · 툴바 없음). 어긴 자리를 받는 `report-uri` 는 두지 않았다 — 되돌리는 법은 runbook 「CSP 가 화면을 막을 때」. 증거: e2e `security-headers.spec.ts`(강제 헤더, 바깥 이미지를 `enforce` 로 막는 것)와 CSP 자동 손잡이 — 익명 spec 은 `anon.ts`, 로그인 · 관문 spec 은 `session.ts` 를 지나 **시험 전부**에서 위반 0, dev 와 운영 빌드 둘 다. **결정할 자리: `script-src` · `style-src` 의 `'unsafe-inline'` 을 뺄 것인가** — ⓐ nonce: 돈다(운영 빌드 e2e 전부 위반 0). 정적 넷(`/` · `/_not-found` · `/auth/denied` · `/me/discovery`)이 전부 동적이 되고 proxy 가 모든 화면 요청에 돈다 — 운영에서 잰 첫 바이트 정적 63ms · 동적 118ms(중앙값), 콜드 1.35초 ⓑ SRI(`experimental.sri`): 정적은 지키나 Next 의 인라인 조각에 해시가 없어 홈만 열어도 위반 4, 화면이 안 선다 — 대안이 못 된다 ⓒ 지금처럼 둔다. PG · 본인인증(PortOne V2 + KCP)은 `cdn.portone.io`(script) · `checkout-service.prod.iamport.co`(frame · connect) 외 `connect-src` 넷을 더 연다 — 샌드박스에서 재야 확정. 값과 목록은 `docs/notes/csp-nonce-2026-09-23.md` ③ `security definer` 함수의 `search_path` — **닫힘(2026-09-23)**: 이미 129개 전부 `""` 였다. 증거: pgTAP `42_definer_search_path`(빠진 함수를 넣으면 이름으로 잡는 것을 흔들어 확인) ④ 사람 저장 빗장 시간 20명 · 하루 100명, 총량 없음 — **닫힘(2026-09-23)**: `20261007090000` 의 `person_save_rate` 트리거. 증거: pgTAP `43_save_and_report_brakes` ⑤ 신고 빗장 하루 20건 + 검토 전 같은 대상 · 같은 사유 중복 막음 — **닫힘(2026-09-23)**: 같은 마이그레이션의 `hold_report_quota` 와 두 신고 문, `20261008090000` 이 중복을 하루 수보다 먼저 판정하고 조회 인덱스 셋을 더했다. 증거: pgTAP `43_save_and_report_brakes` · `34_chat`. 두 문은 가입 완료도 묻는다 — G-20 이 서면 가입이 인증을 품어 「인증을 마친 계정만」이 된다 ⑥ PG 웹훅: 서명 검증 · 금액을 서버의 주문과 대조 · 같은 알림이 두 번 와도 한 번만 반영 — 시험 ⑦ PASS 결과는 서버가 인증사에서 다시 받아 확인하고 화면이 보낸 값을 믿지 않는다 — 시험 ⑧ 비밀키(무료 지급 HMAC · PG · 본인확인)는 서버 환경변수에만 있고, 새는 경우의 절차가 runbook 에 선다 ⑨ 운영자 계정(Supabase · Vercel · GitHub · PortOne) 전부 MFA — runbook 에 확인 날짜 ⑩ 운영자 접속기록 1년 이상(G-25 ②③) — 어디에 몇 일 남는지 runbook 에 선다 ⑪ Supabase 보안 advisor 경고 0 — 결과를 남긴다 ⑫ 개발 의존성 감사(`@vitest/mocker` moderate · `js-yaml` high, 2026-09-23 기준)는 운영에 안 실리지만 정리한다. **IP 보조 제한은 두지 않는다** — 새 계정으로 빗장을 넘는 길은 PASS · CI 한 사람 한 계정(ADR 0101)이 막고, 이용자 IP 수집은 G-25 ⑨ 가 아직 안 정했다 |
| G-24 | 확장된 관리자·제재 화면 | §7.0 | 정했다 | 신고 근거 스냅샷을 읽는 자리가 화면으로 선다 | 채팅 안전 베타까지는 runbook SQL |
| G-25 | 개인정보 정책 · 약관 · 고객 대응 | §7.0 | 정했다 | 이용약관이 서고, 처리방침이 아래 넷(본인확인 · 결제 · 무료 지급 기록 · 접속 기록)을 들어 내용 개정으로 오르고, **변호사 검토 1회**가 끝난다 | **정한 것(2026-09-23):** ① 메시지 보존은 §7.1 의 90일을 처리방침이 옮겨 적는다 ② 결제 · 청약철회 기록 **5년**, 소비자 불만 · 분쟁 처리 기록(환불 요청 · 중복결제 · 취소 이의 · 미제공 신고) **3년** — 탈퇴 3일 처분의 예외로 따로 두고 앱은 못 읽는다(전자상거래법 시행령 제6조) ③ 개인정보처리시스템에 접속한 운영자의 접속기록 **1년 이상**(안전성 확보조치 기준 제8조) — 이용자 5만 명 이상이 되거나 고유식별정보 · 민감정보를 받게 되면 2년으로 다시 본다 ④ 고객 문의는 이메일과 앱 안 문의, **3영업일 안 첫 답**. 1372 안내는 문의 대응 절차에 두지 않는다 ⑤ 결혼중개업법은 지금 비해당으로 판단하고 제품 구조를 바꾸지 않는다 — 요청 때 예약 · 수락 때 자동 생성 그대로(ADR 0038 · 0100). 지키는 경계: 결혼 상대 · 성혼 · 결혼 상담이라 광고하지 않는다 · 결혼 의사나 혼인 조건을 받지 않는다 · 교제나 성혼을 보장하지 않는다 · 약관의 서비스 정의에 결혼 알선이 아님을 적는다 **변호사 검토로 정하는 것:** ⑥ 환불 산식 — 「쓴 회차를 1회권 가격으로 공제」와 「7일 뒤 10% 공제」가 유효한지. 무료 · 예외 몫은 환불하지 않는다는 것과 예약 중인 몫은 풀릴 때까지 대상이 아니라는 것은 정했다(G-21 ③). 묶음별 결제 · 사용 이력은 어느 산식이든 필요하므로 먼저 만든다 ⑦ 탈퇴 때 남은 유료 풀이권 — 바로 소멸시킬지는 환불 기준과 함께 본다. 그때까지는 탈퇴 판에 남은 수량과 환불 신청 길을 분명히 보인다 ⑧ 무료 지급 HMAC 식별값의 보관 기간 · 목적 · 근거(개인정보 보호법 제21조, ADR 0101) — 기한 없음이 어렵다면 5년, 기한이 생기면 ADR 0101 을 다시 연다 ⑨ 이용자 IP · 접속 시각 3개월 — 통신비밀보호법(시행령 제41조②) · 전기통신사업자 해당 여부를 본 뒤 확정한다(G-52 에서 넘어옴, ADR 0098) ⑩ 이용약관 전문 — 공정위 디지털콘텐츠 이용 표준약관을 바탕으로 쓰고, 분쟁 해결 조항에 1372 · 분쟁조정기관을 넣을지도 여기서 정한다 ⑪ 결혼중개업법 적용 여부 — 서비스 구조를 보이고 짧게 확인한다. 신고 · 보증보험을 전제로 문서를 만들지 않는다 **선행:** 사업자등록 · 통신판매업 신고(PG 신청 조건, G-21) |
| G-26 | 외부 알림(이메일 · 카카오 · 푸시) | §7.0 · CONTEXT 「앱 내 알림」 | 정했다 | 새 메시지 통보부터 | 지금 통보는 앱 안에서만 보인다 |

## 정비 — 위임 준비

지금 줄이 없다 — G-42(운영 관측)는 2026-09-23 에 크론 실패 알림으로 닫혔다.

## 닫힌 것은 여기 없다

끝난 것은 `docs/product/prd-changelog.md` 가 날짜와 함께 든다. 「써 보고 정한다」 띠(G-30~37)는
2026-09-23 에 사람이 전부 답해 비었다 — 닫힌 것(G-30 · 31 · 32 · 33 · 34 · 35 · 36 · 37)은 changelog 에 있다.
`보류` 상태는 다음 「써 보고 정한다」를 위해 남긴다.
