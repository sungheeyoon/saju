# PRD 개정 기록

`docs/prd.md` 가 **무엇을 만드는가**라면 이 문서는 **그 문서가 어떻게 여기까지 왔는가**다.
요구사항이 아니다 — 어느 날 무엇을 재고 무엇을 고쳤는지의 기록이고, 2026-09-22 에 PRD 에서
갈라 냈다(ADR 0089). 새 개정은 여기에 날짜 순으로 쌓고, PRD 본체에는 지금 모양만 남긴다.
아직 없는 것 · 안 정한 것 · 어긋난 것은 `docs/product/gaps.md` 가 든다.

## 개정의 방법

이번 개정은 **2026-09-05 에 코드·마이그레이션·시험을 실제로 열어 보고 다시 썼다.** 같은 날
2차로 가입 흐름(ADR 0042)과 풀이권 총량과 인연 목록의 빈 자리를 고치고 여기 반영했다. 그래서
지난 판과 달리 줄마다 **지금 있는가**를 함께 적는다. 표시 셋(선다 · 없다 · 다르다)은 PRD
본체 머리에 있다.

## 재어 본 값

### 0.3 2026-09-05 에 재어 본 값

- 단위 시험 **1,526개 통과** (파일 66, 건너뛴 것 5)
- DB 시험 **659개 통과**(pgTAP 25파일) · 실스택 흐름 검사 **117개 통과** ·
  브라우저 시험 **125개 통과**(익명 49 · 로그인 76)
- ADR **0001~0042**
- 열린 티켓 **없다** — 이슈 #1(이 PRD)을 빼면 #2~#14 가 모두 닫혔다
- 화면 라우트 19개, 마이그레이션 49개, 서버 액션·RPC 로 여는 문 그 밖

**2026-09-09 에 다시 잰 값** — 단위 시험 **1,586개 통과**(파일 76, 건너뛴 것 6) ·
pgTAP **25파일** · ADR **0001~0055** · 마이그레이션 **54개**. 브라우저 시험은 이날 안 돌렸다
(스펙은 새 흐름으로 고쳤다) — **안 돌린 것을 통과로 적지 않는다.**

**2026-09-12 에 네 층을 다 돌려 잰 값**

- 단위 시험 **1,612개 통과**(파일 77, 건너뛴 것 6)
- DB 시험 **691개 통과**(pgTAP 26파일)
- 실스택 흐름 검사 **341개 통과**(스크립트 6개)
- 브라우저 시험 **익명 47개 통과**(건너뛴 것 1) · **로그인 79개 통과**(건너뛴 것 2)
- ADR **0001~0060** · 마이그레이션 **59개** · 화면 라우트 **19개**

**흐름 검사와 로그인 브라우저 시험이 이날 깨져 있었다.** 자기 사람을 닉네임으로 부르게
바꾼 뒤(`self_is_called_by_the_nickname`)와 공유 궁합을 풀이 쪽으로 옮긴 뒤(ADR 0058)
검사가 옛 화면을 재고 있었다 — 코드가 맞고 검사가 낡은 쪽이라 검사를 지금 화면으로
옮겼다. **다섯 파일에서 일곱 자리다**: 자기 이름(둘) · 옛 인연 설정 주소 · 후보 카드
문구 · 공유 궁합의 자리 이름 · 풀이 목록의 줄 이름 · 수락 뒤의 이동 · 답이 난 뒤의 차단
자리. 여기에 검사 자체의 버그 하나를 함께 고쳤다 — 후보 목록을 낱말로 잘라서 내 명식이
후보의 것으로 잡히고 있었다.

**흐름 검사는 첫 스크립트에서 멈춰 있었다.** 그래서 나머지 다섯이 한 번도 안 돌았다 —
깨진 것을 고치기 전까지 「통과」로 읽히던 자리가 아니라, **아무 값도 없던** 자리다.

**2026-09-14 에 잰 값** (PR #57 의 CI — 그날 로컬이 아니라 CI 로그에서 셌다)

- 단위 시험 **1,598개 통과**(파일 79, 건너뛴 것 8)
- DB 시험 **772개 통과**(pgTAP 30파일)
- 브라우저 시험 **익명 55개 통과**(건너뛴 것 1) · **로그인 95개 통과**(건너뛴 것 2)
- 실스택 흐름 검사는 이날 **안 돌렸다**
- ADR **0001~0066** · 마이그레이션 **68개**(PR #57 시점) · 화면 `page.tsx` **24개**

## 개정에서 문서를 고친 자리

### 0.4 이번 개정에서 **문서를 고친 자리** (§ 표시 「다르다」)

1. **차단은 소식이 아니다** (§6.4) — 지난 판은 「요청·수락·거절·무효·차단은 소식으로
   알린다」고 적었다. 코드는 차단당한 사실을 알리지 않는다. 알리면 그것이 상대를
   두드리는 도구가 된다. 대신 차단이 끊은 요청이 「거절」로 서고, **만료도 소식이다**
   (요청자에게만 — 풀이권이 돌아온 것을 알아야 한다)
2. **「탈퇴」라는 말은 화면에 없다** (§5.3) — 카드의 이름은 「계정 삭제」이고 되돌릴 수
   없는 마지막 누름이 「삭제를 요청합니다」다. 한 사실에는 한 표기라는 규칙(ADR 0027)을
   PRD 도 따른다
3. **시간당 상한은 20이다** (§2) — 지난 판은 수를 안 적었다. 값이 어디 있는지 모르면
   조일 때 조인 줄도 모른다
4. **회원 메뉴의 「궁합」과 공개 메뉴의 「궁합 보기」가 갈렸다** (§3) — 같은 화면인데
   로그인 전후로 다른 이름이 섰다. **이건 문서가 아니라 코드가 틀렸다.** 2026-09-10에
   회원 메뉴를 `사주·궁합`으로 합쳐 해결했다
5. **프로필은 메뉴가 아니라 설정 안에 있다** (§3) — 표에 화면으로만 적혀 있어서 어디로
   가는지 문서가 답을 못 했다

**그리고 문서가 아니라 코드에 구멍이 있던 것 하나** — `create_managed_person` 이,
곧 **남의 생년월일시가 들어오는 문**이 안내 확인을 한 번도 안 묻고 있었다. 화면 관문이
`/me` 를 지키니 닿을 수 없다고 여겼는데 RPC 는 브라우저에서 그대로 부를 수 있다.
2차 개정에서 막았다 (ADR 0042).
### 0.5 2026-09-09 개정에서 **문서를 고친 자리** — 궁합

**화면이 정답이고 문서가 낡았다.** 궁합을 손으로 걸어 보고 그 모양대로 다시 적었다.

1. **궁합은 걸음 둘이다** (§3.1·§4.5) — 지난 판은 「저장한 두 사람을 고르고 결과를 본다」와
   「직접 입력하고 그 자리에서 결과를 본다」 둘로 적혀 있었다. 탭이 없어졌다: 한 화면에서
   **칸마다** 저장한 사람/직접 입력을 고르고, 결과는 다음 화면이다 (ADR 0054)
2. **풀이를 받으려고 사람을 저장할 필요가 없다** (§2·§4.1) — 궁합 쪽 대상은 만들되
   **사람 목록에 안 서고 열 자리도 안 쓴다** (ADR 0053). 「저장 한도가 풀이권보다 커야
   한다」는 부등식의 까닭 절반이 여기서 사라졌다
3. **사이를 묻는 자리는 하나다** (§4.5) — 고르는 자리에서 묻고 결과에서는 안 묻는다.
   결과는 무엇으로 읽는지 한 줄로 적기만 한다
4. **분석 표는 궁합 결과에서도 접힌 채로 선다** (§4.5) — 「저장한 두 사람 화면에는 아예
   안 선다」였는데, 그 화면과 접이칸이 살던 화면이 하나로 합쳐졌다 (ADR 0035 재적용)
5. **§9 가 비었다** — 갈려 있던 메뉴 이름은 비로그인 메뉴가 비면서(§7.4.3) 어긋날 자리가
   없어졌다
### 0.6 2026-09-09 개정에서 **문서를 고친 자리** — 한 사람의 명식과 풀이

**한 화면 아래에 명식과 긴 풀이를 쌓지 않는다**(ADR 0055).

1. 내 명식과 저장한 사람의 명식에는 `사주`·`사주풀이` 탭이 선다. 둘은 숨김 상태가
   아니라 각각 주소를 가진 화면이라 사람 목록과 풀이 목록에서 원하는 자리로 바로 간다
2. 한 사람의 풀이 결과는 `/me/readings/[subject]`에 선다. 풀이 화면에는 풀이만 두고,
   명식은 나란히 선 `사주` 탭으로 이동해 확인한다
3. 풀이권을 쓰거나 예약하는 누름은 처음과 다시 만들기를 가리지 않고 바로 앞에서 확인한다.
   다시 만들 때에는 현재 풀이가 교체된다는 사실도 같은 창에서 말한다
4. 저장한 사람을 목록에서 빼는 일은 되돌릴 수 없으므로 실제로 빼기 전에 확인한다
### 0.7 2026-09-12 개정에서 **문서를 고친 자리** — 점수와 소식

**코드가 정답이고 문서가 낡았다.** 2026-09-10·11 에 나간 것들을 대조해 다시 적었다.

1. **점수는 이제 기준점에서 움직인다** (§1.4) — 지난 판은 「점수는 모델이 낸다」였다.
   근거만 넘기고 모델이 종합하던 판은 눈금이 안 생겨(62~68, ADR 0052) 접었고, 지금은
   후보 카드와 **같은 함수가 낸 기준점**에서 여덟 항목이 ±15 안에서 움직이고, 그 뒤에
   모델의 재량 ±10 이 따로 붙는다 (ADR 0059·0060·0065)
2. **후보 카드가 점수와 요청을 한 장으로 든다** (§6.1) — 균형 문장 하나만 서던 자리에
   점수·판정 한 줄·이유 한 줄이 서고, 그 아래가 「상세 궁합 요청하기」다. 오행 하나로 성격을
   말하던 뜻풀이는 걷었다
3. **성립한 궁합은 풀이 목록에 선다** (§6.3·§6.4) — 소식은 「새 소식이 있다」까지만 말하고,
   결과 화면은 한 문장 결론·점수·두 명식·본문 차례다. 「첫 번째 분」은 화면에서 사라지고
   읽는 사람 쪽의 **나와 상대 닉네임**으로 갈린다 (ADR 0058)
4. **자기 명식의 이름은 닉네임이다** (§4.3·§5.2) — 같은 사람에게 이름 둘이 생기지 않게
   등록·수정에서 따로 안 받는다
5. **소식은 탭이다** (§7.4.1) — 「종으로 간다」고 적혀 있던 것을 코드에 맞췄다
6. **`/evidence` 는 없는 주소다** (§3) — 검산 자리는 `/me/reading/inspect` 하나다

**그리고 문서가 아니라 검사가 낡아 있던 것 일곱.** 흐름 검사와 로그인 브라우저 시험이
옛 화면을 재고 있어 `npm run test:flow` 가 첫 스크립트에서 멈춰 있었다 — 나머지 다섯
스크립트가 한 번도 안 돌던 상태다(§0.3).

---
### 0.8 2026-09-18 — 오늘의 인연을 매칭으로 확장

기존 추천과 궁합 계산을 유지하면서, 인연을 탐색하는 주 화면을 **매칭**으로 확장한다.
“내 귀인은 내가 찾는다”는 사진과 소개로 상대를 알아보고, **나와의 예측 궁합과 그 이유**를
읽은 뒤 직접 선택한다는 뜻이다. 궁합 점수를 외모나 인기 순위처럼 사용하지 않는다.

초기 `febd015` 리뷰에서 남았던 보관함 복원·연속 되돌리기·실사진·20명 표시 제한을
후속 구현에서 연결했다. 홈에는 매칭 진입을 남기고 중복 추천 목록을 걷었다. 모바일
하단에도 매칭을 배치했다. 현재 코드와 검증 상태는 §9.1 및 후속 리뷰 기록에 둔다.
구현 판단은 ADR 0070을 따른다.

## 화면 정리 — 끝난 것

**7.4.3 비로그인 메뉴 — 끝났다(2026-09-07)**

궁합을 메뉴에서 내렸다. 눌러서 못 가는 줄은 메뉴가 아니라 광고다 — 그 길은 사주 보기 화면의
머리에 「로그인 필요」를 달고 선다.

그러고 남은 「사주 보기」 한 줄도 뺐다. **하나뿐인 탭이 언제나 지금 보고 있는 화면이었다.**
비로그인 메뉴는 이제 비어 있고 헤더에는 로고와 로그인만 선다(§3.1).

**7.4.4 회원 메뉴의 「궁합」 — 끝났다(2026-09-10)**

`궁합`은 사주 보기 화면의 `궁합 보기`에서 시작하는 것과 같은 흐름이다. 회원 메뉴의 독립
`궁합`을 없애고 기존 `사주 보기`를 **`사주·궁합`**으로 바꾼다. `/compat`과 `/me/compat`에서도
이 합쳐진 메뉴가 현재 위치로 표시된다. 화면과 주소는 합치지 않고 중복된 내비게이션만 합친다.

---

## 코드 쪽 어긋남을 고친 기록

### 9.1 매칭 확장 후속 수정 (2026-09-18)

초기 소스 리뷰의 여섯 항목을 수정했다: 보관함에서 실제 복원, 사람별 연속 되돌리기,
실패 후 재시도, 최신 20명 표시, 실제 사진과 예시 구분, 현재 공개 자격 검사.
보관함·제외 판정의 동률 순위도 맞췄다. 홈의 중복 목록은 걷고 모바일 하단에 매칭을 넣었다.

배포된 보관 정책은 §6.1.2대로 유지한다. 실제 검증 결과와 배포 상태는
[매칭 구현 리뷰](product/matching-review-2026-09-18.md)의 후속 수정 기록에 남긴다.

### 9.2 이전에 닫은 항목

**프로덕션 적용까지 닫았다 (2026-09-15).** 인연 궁합 풀이의 저장된
근거·프롬프트에는 상대의 억부 후보·성별·계산 옵션이 들고, `my_reading_artifacts` 가 두 당사자에게
그것을 내주고 있었다. 마이그레이션 `20260921120000` 이 인연 궁합 요청을 **운영자 + 기존 범위**로
좁혔다(ADR 0068).

그 「남은 길」도 닫았다 — `my_reading` 은 인연 궁합에서 **사용자 본문만** 내주고, 저장된 원문은
그대로 둔 채 운영자이면서 당사자일 때만 따로 연다(`match_reading_source`, ADR 0069). 검산 화면의
인연 궁합 근거 절도 그 문을 지난다.

그 전까지 비어 있었다. 2026-09-12 개정에서 어긋난 자리는 전부 **문서가 낡은 쪽**이었다 —
점수(§1.4), 후보 카드(§6.1), 소식과 결과가 사는 자리(§6.3·§6.4), 소식 탭(§7.4.1), 없는
주소 `/evidence`(§3). 코드를 고친 것은 **검사 여섯 자리**뿐이고, 그것은 제품 행동이 아니라
검사가 옛 화면을 재고 있던 자리다(§0.3).

**다만 값을 적어 둘 것이 하나 있다.** 공유 궁합의 본문은 화면에서 「나」와 상대 닉네임으로
그려지지만, 그리기 전의 원문은 응답 페이로드에 그대로 실려 온다 — 거기에는 「첫 번째 분」이
남아 있다. 내 글이라 새는 값은 아니고, 자리 이름을 사용자 앞에 세우지 않는다는 약속
(ADR 0058)이 **그리는 자리에서만** 지켜진다는 뜻이다.

---

## 10. 이 문서의 계보

- **2026-09-22** — **출시 범위를 세 단계로 이름 붙였다**(§7.0): 운영 베타(지금) → 채팅 안전 베타 →
  공개 출시. 채팅 안전 베타의 완료 조건 여섯을 지정 테스트 계정으로 밟는 일로 적었다. 접속 상태는
  최근 활동 구간이고(§7.2), 「방이 닫힌다」는 입력만 양쪽 다 막히고 열람은 닫은 이유별로 갈리는 것이다(§7.1).
  **끝난 것은 세 단계의 경계와 채팅 안전 베타의 정의다** — 공개 출시의 완료 조건은 아직 크기가 안 정해졌다.
  같은 날 검토로 여섯을 고쳤다 — 닫힌 방의 열람 규칙을 닫은 이유별로 갈랐고, 완료 조건에 중지와
  열람 검증을 더해 지정 테스트 계정으로 밟는다고 적었고, 채팅과 공개 출시의 접점 넷을 표로 적었고,
  신고 근거를 「고른 메시지 + 앞뒤 문맥 스냅샷」으로 정했고, 접속 상태를 구간 셋으로 좁혔고, 판매
  단위는 §8.7 미정으로 보냈다. §5.3 과 용어집의 「중지」가 어긋난 것을 찾아 §9.1.1 에 적었다. 9/15 의
  새 기능 보류는 채팅에 한해 풀렸다. 「운영 베타」 낱말은 CONTEXT.md 에 있다
- **2026-09-18** — 오늘의 인연을 매칭 카드 탐색으로 확장한 제품 방향·주소·보관 패널·20명과
  24시간 정책을 기록했다. `febd015` 소스와 대조해 복원·연속 되돌리기 등 미완료를 §9에
  분리했다. 같은 날 후속 요청으로 복원·사진·순위·실패 처리를 연결하고, 홈과 모바일
  진입을 매칭으로 정리했다(ADR 0070). 배포된 최근 20명 보관 정책을 유지한다.

- **2026-09-16** — **인연 궁합의 근거 절을 본문 조회에서 걷었다**(§9, ADR 0069). 비운영자 당사자가
  검산 화면과 RPC 로 그 절을 읽던 것을 로컬에서 재현하고 막았다. 저장된 원문은 그대로다
- **2026-09-15 (4차)** — 두 궁합 지시에서 고정 생활 예시와 근거 칸 본보기를 걷고 `reading-prompt-v12` 로
  올렸다(개인 풀이 v10 그대로). match·private 실호출로 저장까지 확인했다(§8.5)
- **2026-09-15 (3차)** — **두 궁합을 읽는 법 4판으로, 인연 궁합 입력을 제한형 A 로 올렸다**(§1.2·§8.6,
  ADR 0067, 궁합 `reading-prompt-v11` · 개인 풀이 v10 그대로). 고른 사이가 장면을 고른다. 인연 궁합 원문
  근거 제한을 프로덕션에 적용했다(§9, ADR 0068)
- **2026-09-15 (2차)** — **인연 궁합 입력 두 판을 견줄 수 있게 했다**(§1.2·§8.6, ADR 0067). 운영은 옛 컷
  그대로다. 인연 궁합 원문 근거를 운영자로 좁히는 마이그레이션을 썼다(§9, ADR 0068 — 프로덕션 미적용)
- **2026-09-15** — **모델 입력에 자리 색인을 더하지 않고 자리가 붙은 사실 목록으로 갔다**
  (§8.5, `reading-prompt-v11`). 실험 가지 `overlaps-v2` 는 지웠고 `now.overlaps` 는 둔다
- **2026-09-14** — 코드를 전수로 훑고 문서와 대조했다. **한 줄 요약은 비유가 아니라 직접
  요약**이다(§1.5·§4.2, ADR 0056 개정이 이미 그렇게 적고 있었다). `/me/compat` 은 인자가 없으면
  `/compat` 으로 간다(§3). 출생 정보를 바꾸기 직전의 확인 창이 섰다(§6.2). 그리고 **코드
  쪽 어긋남 하나를 고쳤다** — 요청을 받은 사람의 「채우는 오행」이 옛 규칙(0개)으로 세어지고
  있었다. 이제 후보 카드와 같은 `discovery-v1` 규칙(20% 미만)이다. 같은 날 옛 오행 축
  SQL 함수 넷(`discovery_complement` 등)을 지우고, §0.2·§0.3·§3·§4.8·§7.1 의 어긋남을 고쳤다

- **2026-09-13** — **사주풀이를 링크로 보낸다**(§4.8, ADR 0063·0064). 「현재 결과 하나」에
  예외가 하나 생겼고(§4.1), 로그인 없는 역할에게 문이 처음 하나 열렸다. 같은 날 궁합
  베타 카드가 옛 셈(`match-v0`)을 버리고 `discovery-v1` 로 옮겼다 — **§9 가 비어 있다고
  적은 동안 실제로 있던 어긋남**이고, 한 화면에 다른 축의 점수가 둘 서 있었다. 그리고
  점수 폭이 둘로 갈렸다 — 자료가 말하는 ±15 와 모델이 판단하는 ±10(ADR 0065)

- **2026-09-03** — 코드와 대조해 축을 다시 세우고 어긋난 일곱을 티켓으로 냈다 (ADR 0032~0038)
- **2026-09-04** — 비용 한도(0039)·가입 프로필(0040)·참여 기본 켜짐(0037 나머지 절반)이 섰다
- **2026-09-07** — 원국 결과를 훑고 아홉을 고쳤다: 발화 카드와 「지금의 운」을 걷고,
  용신을 탭 카드로 가르고, 신살을 여덟 글자 아래로 올렸다 (§3.1)
- **2026-09-07** — 첫 화면을 눈으로 훑고 넷을 고쳤다: 비로그인 메뉴를 비웠고(§7.4.3),
  결과 버튼의 **잠금을 제출 거절로 바꿨다**(§3.1), 히어로의 두 갈래와 오행 다섯을 좁은
  화면에서 한 줄로 세웠다
- **2026-09-08** — 자리 색인(`overlaps-v2`)을 다시 열어 보고 **보류를 유지했다.** 색인이
  같은 이름을 접으며 잃는 것을 확인했고, 보류의 근거·다시 여는 문턱·그때 먼저 할 것을
  §8.5 에 적었다. 블라인드 라운드의 수치가 남아 있지 않다는 사실도 함께 적었다
- **2026-09-05** — **이 개정.** 코드·마이그레이션·시험을 열어 보고 줄마다 상태를 붙였다.
  「선다/없다/다르다」 표시와 §0 대조표가 이번에 생겼고, §9 가 코드 쪽 어긋남을 든다
- **2026-09-05 (2차)** — 테스트를 열기 전에 셋을 고쳤다.
  1. **가입이 한 폼이 됐다**(ADR 0042) — 이메일 명단을 걷고 테스트 코드로 바꿨다.
     `/welcome` 이 없어지고 관문이 셋에서 둘로 줄었다. 그 과정에서
     `create_managed_person` 이 **안내를 한 번도 안 묻고 있던 것**을 찾아 막았다
  2. **풀이권** — 여덟으로 올렸다가 같은 날 **다섯으로 되돌렸다.** 사람이 정했다.
     여덟의 근거였던 셈(요청 둘이면 아무것도 못 만든다)은 §2 에 제약으로 남겨 뒀다
  3. **인연 목록의 빈 자리에 제 문구가 섰다** (§6.1)

- **2026-09-06** — 넷을 프로덕션까지 올리고 ADR 0043~0046 을 붙였다.
  1. **풀이권 예외**(0043) — 한도는 그대로 두고 한 사람에게만 얹는다. 한도를 묻는 자리
     셋이 함수 하나를 지나게 모았다 (§2)
  2. **용신 계열을 읽는 판**(0044) — 자료에 실리는데 프롬프트가 안 부르던 경로들을
     이미 있는 세 절이 읽는다. 절은 안 늘렸다
  3. **경로 유출 검사**(0045) — 자료 경로 이름이 사용자 본문에 새면 저장 전에 막는다
  4. **이름을 안 부르는 판**(0046) — 분류명을 본문에 안 쓴다(§1.1). ADR 0029 가
     「견줘 본 뒤에 정한다」고 미뤄 둔 판단인데 **블라인드 라운드가 아니라 제품 결정으로
     났다.** 글이 나아졌는지는 아직 모른다 — 이 판은 결정 시점까지 한 번도 실호출된 적이
     없고, 원문 판단은 사용자가 프로덕션에서 직접 한다

- **2026-09-07** — 프롬프트 판본이 `v7` 로 올라갔다 (ADR 0047~0052).
  1. **궁합이 절을 걷었다**(0051) — 절 열하나와 「성격을 읽는 순서」를 내리고 **다룰 것의
     목록**만 준다. 나누는 것·차례·어느 판정을 읽을지는 모델이 정한다. 그 전에 두 라운드를
     버렸고(용신 서열을 10절에 읽히는 판), **버린 까닭이 값으로 남아 있다** — 그 서열이
     답하는 물음을 궁합 프롬프트가 안 한다
  2. **점수 옆에 한 문장 비유**(0052, §1.5) — 점수가 늘 62~68 이고 재호출 흔들림이 다른
     짝과의 차이만큼 커서, **뜻을 문장으로 옮겼다.** 숫자는 남지만 답의 자리에서 내려온다
  3. **엔진이 모델에게 말을 걸지 않는다**(0047) · **진입 판정을 한 자리로**(0048) ·
     **옛 뼈대 삭제**(0049) · **로그인 뒤 화면도 CI 가 잰다**(0050)

  **실호출 없이 올렸고 그 대가를 두 번 치렀다** — 검사가 막는 값(비유 길이)을 프롬프트가
  말하지 않아 자기 풀이가 떨어졌고, 긴 비유 옆에서 화면 배치가 깨졌다. 둘 다 같은 날
  고쳤다. 풀이권은 안 깎였지만(실패는 안 센다) 토큰은 나갔다

- **2026-09-09** — **궁합을 손으로 걸어 보고 그 모양대로 다시 적었다**(§0.5). 코드 쪽으로는
  둘이다.
  1. **저장하지 않아도 궁합풀이가 난다**(ADR 0053) — 대상은 만들되 사람 목록에 안 세운다
     (`listed`). 열 자리도 안 쓴다. 그래서 「저장이 관문」이던 다리(「두 사람을 저장하고
     궁합풀이로 가기」)와 자리 부족 안내가 함께 없어졌다
  2. **탭 둘을 없애고 걸음 둘로**(ADR 0054) — 「직접 입력」과 「저장한 사람」이 화면 둘이
     아니라 **한 칸의 토글**이 됐다. 섞인 조합(내 사주 × 직접 적은 사람)이 이제 갈 곳이 있다.
     사이를 묻는 자리도 하나로 모았다

  **화면이 정답이고 문서가 낡았다는 전제로 고쳤다** — 이 판은 실제로 눌러 보고 적었다.

- **2026-09-10** — 시간 기준의 기본값이 **진태양시**로 옮겨 갔고(ADR 0057), 궁합 결과와
  한 사람 화면의 주소가 갈렸다(ADR 0055). 회원 메뉴의 `사주 보기`와 `궁합`이 `사주·궁합`
  한 줄로 합쳐졌다(§7.4.4)

- **2026-09-11** — **점수가 제품이 됐다.** 후보 카드가 「예측 궁합 점수」를 이름으로 들고
  (ADR 0059), 궁합풀이의 점수가 그 수를 기준점으로 삼아 항목별로 움직인다(ADR 0060).
  같은 날 공유 궁합이 궁합풀이와 같은 처리 방식으로 옮겨 오고(ADR 0058), 소식 화면의
  정보 구조와 자기 명식의 이름 규칙이 함께 바뀌었다. **이 셋에는 ADR 을 더 늘리지 않았다** —
  판단이라기보다 화면 편집이라, §3·§6 이 지금 모양을 든다

- **2026-09-12** — **이 개정.** 네 층을 다 돌려 값을 다시 재고(§0.3) 문서를 코드에 맞췄다
  (§0.7). 그 과정에서 **검사 여섯 자리가 낡아 있던 것**을 찾아 지금 화면으로 옮겼다 —
  흐름 검사가 첫 스크립트에서 멈춰 나머지 다섯이 한 번도 안 돌고 있었다. `README` 의 시험
  수와 화면 설명, `runbook` 의 **Supabase 프로젝트 ref**(서울 이전 뒤 옛것을 가리키고
  있었다)와 접속값 여섯도 이날 고쳤다

- **2026-09-22** — 간극 대장의 줄 셋이 움직였다(ADR 0090). **G-40 닫힘** — 위임 규약이
  `docs/agents/delegation.md` 한 장으로 섰고(맡길 이슈의 칸 · 권한 등급 다섯 · PR 이 드는 칸 · 일하는
  법 · 로컬 환경의 함정), 이슈 틀·PR 틀·`.claude/settings.json` 이 그 표와 같은 목록을 든다.
  세션 메모 63벌은 규약·절차·결정으로 흡수되거나 `docs/notes/` 열셋으로 옮겨졌다. **G-41 닫힘** —
  배포 규약 넷(앱 배포 ≠ DB 마이그레이션 · 마이그레이션이 먼저 · 넓히고 재고 좁힌다 · 올린 뒤 볼 것)이
  `docs/ops/runbook.md` 「배포」에 섰다. **G-02** 는 메모에만 있던 사용자 결정(운영에서 걷는다)을
  옮겨 적어 `결정 대기` 에서 `정했다` 로. PRD 본체는 안 바뀌었다

- **2026-09-22** — **G-02 닫힘.** 운영 DB 에만 있던 `ensure_rls` 이벤트 트리거와 `rls_auto_enable()` 을
  사용자가 대시보드 SQL Editor 에서 걷었다(트리거 먼저, 함수 나중). 걷은 뒤 트리거 0 · 함수 0 ·
  `public` 에 RLS 꺼진 표 0 을 확인했다 — 표 27개의 RLS 는 그물 없이도 그대로다. 이제 운영과
  로컬·CI 의 `public` 객체가 같고, pgTAP 33 의 RLS 잠금이 두 환경에서 같은 뜻이다(ADR 0084 정정)

- **2026-09-23** — **G-01 닫힘**(#113). §5.3 「아무것도 안 보인다」와 용어집 「자기 것은 읽는다」가
  어긋나 있던 것을 코드에 대고 쟀다 — 어긋난 쪽은 용어집이었다. `supabase/tests/08_suspended.test.sql`
  이 중지된 계정의 Person · 목록 · 프로필을 0행으로, 자기 `app_user.status` 만 읽힘으로 잠그고 있고,
  마이그레이션 `20260825080000` 이 「`app_user` 는 그대로 둔다 — 화면이 중지됐다고 말할 근거」라고
  적었다(ADR 0006 · 0014). 용어집 「중지」를 「상태만 읽고 자기 자료는 못 읽는다」로 고치고 §5.3 은 그
  한 줄만 남겼다 — 채팅 열람 규칙(§7.1)이 §5.3 을 따른다는 관계는 본문에 그대로다. 코드는 안 바뀌었다

- **2026-09-23** — **G-11 · G-12 닫힘.** 채팅 안전 베타 앞에서 사람이 답할 것 둘을 사용자가 정했고
  §7.1 에 적었다. **보존·삭제** — 열린 방은 기한 없이, 닫힌 방은 닫힌 날부터 90일 뒤 지운다(지우는 손은
  runbook SQL). 개별 메시지의 삭제·수정은 없다. 계정 삭제의 실제 처분 때 남는 쪽은 계속 보고 떠난 쪽의
  이름만 표시로 바뀐다. 신고 스냅샷은 메시지와 수명이 다르다. **전송 한도** — 계정당 1분에 30건, 한 건
  1,000자, 하루 총량은 없다. 첫 메시지를 저장하기 전에 정했으므로 소급이 없다. §7.0 접점 표와 G-10 ·
  G-25 의 메모가 그 자리를 가리킨다. 코드는 안 바뀌었다 — 채팅은 G-10(#115)이 든다

- **2026-09-23** — **계정 상태의 이름 넷을 정했다** — 사용자가 정했고 앱 전체의 기준이다. **정상** ·
  **탈퇴 대기**(`deletion_requested`, `탈퇴를 신청한 계정입니다.`) · **탈퇴**(행이 없다, `탈퇴한 사용자`) ·
  **이용 정지**(`suspended`, `이용이 정지된 계정입니다.`). 사람 사이의 상태는 **연결됨** · **차단됨**
  (`차단되어 대화할 수 없습니다.`)이고 계정 상태가 아니다. 옛 이름은 「중지」 · 「계정 삭제 요청」 ·
  「떠난 사용자」였다 — 「삭제」는 계정 상태에서 걷고 국내 서비스에서 익숙한 말로 간다. 용어집이
  「정지」를 피하던 까닭(되돌릴 수 있는지 말하지 않는다)은 낱말이 아니라 문장이 답할 일이라 접었다.
  §5.3 이 표를 들고, §7.1 닫힌 방의 표가 안내 문구 넷을 얻었다(탈퇴한 상대의 줄은 지금 처분이 방을
  함께 지워 설 자리가 아직 없다 — G-27 메모). 화면 문구도 같은 커밋에서 옮겼다 — `src/lib/account` 의 안내 둘(「이용이 정지된 계정입니다」 ·
  「탈퇴를 신청한 계정입니다」와 「탈퇴 신청이 접수되었습니다.」), `/me/settings` 카드 「탈퇴」와 버튼
  「탈퇴를 신청합니다」, 종료 화면의 「탈퇴 신청」, runbook 절 이름 둘. DB 값 이름(`suspended` ·
  `deletion_requested`)은 그대로이고, DB 함수가 던지는 「중지된 계정입니다.」만 **G-49**(어긋남)로
  남는다 — 마이그레이션이라 다음 마이그레이션 PR 에 얹는다. 채팅 안내 넷은 G-10(#115)

- **2026-09-23** — **G-10 닫힘 — 앱 내 채팅이 선다**(#115, PR B). DB 층(#117, ADR 0091) 위에 `src/lib/chat`
  (정책의 수 사본 · 닫힌 방의 문구 · 거절의 종류), `app/me/chat` 의 문 둘 · 액션 셋 · 화면 둘, 탭 여섯째
  「채팅」, 함께 보는 궁합에서 방으로 가는 길이 섰다. 화면 문구는 사용자가 정한 것 그대로 — 탭 · 목록 제목
  「채팅」, 빈 목록 「아직 채팅방이 없습니다」와 그 설명, 방 제목 「{닉네임} 님」, 입력 자리 · 「보내기」, 닫힌
  방의 안내 넷(§7.1). §7.1 「없다」→「선다」, §7.4.1 탭 다섯→여섯. `e2e/chat.spec.ts` 가 완료 조건 여섯을
  두 계정으로 밟고 `scripts/check-chat.mjs` 가 `chat_policy()` 와 lib 의 수를 견준다. **끝났다고 말하는
  조건은 그대로다** — 프로덕션에서 지정 테스트 계정으로 여섯을 밟는 것은 사람의 걸음(runbook 「채팅」).
  한도 거절 문장은 같은 날 승인됐다(#119 리뷰). 접속 상태(G-13)는 다음 이슈

- **2026-09-23** — **G-13 닫힘 — 접속 상태가 선다**(#121, PR #122 DB · PR B 앱, ADR 0092). 사람이 답한 넷 —
  「지금 활동 중」 창 **5분** · 쓰기 억제 창 **1분** · 화면 문구 「지금 활동 중 · 최근 24시간 내 활동 · 24시간
  이전 활동」 · 처리방침에 한 줄. 활동은 `user_activity` 표 하나에 `proxy.ts` 가 1분에 한 번 적고, 상대에게는
  구간 하나만 나간다(`my_chat_rooms.partner_activity` · `my_discovery_board.activity`) — 시각은 어느 반환
  칸에도 없다. 열린 방의 제목 아래와 후보 카드의 이름 아래에 선다. 옵트아웃·상호 조건은 범위 밖으로 적었다.
  §7.2 「없다」→「선다」와 경계 표, §7.1 의 한 줄. `e2e/chat.spec.ts` 가 방과 카드에서 구간을 밟고
  `scripts/check-chat.mjs` 가 `presence_policy()` 와 lib 의 수를 견준다. 프로덕션 확인 셋은 사람의 걸음
  (runbook 「채팅」 「접속 상태」)

- **2026-09-23** — **G-49 닫힘 — DB 도 「이용이 정지된 계정입니다.」로 거절한다.** 로컬과 운영의 살아 있는
  함수 정의(`pg_proc.prosrc`)를 재니 옛 문장을 던지는 함수가 **21개**(던지는 자리 24)였고 두 곳의 본문이
  같았다. 마이그레이션 `20260929090000_the_suspended_account_is_told_in_one_sentence.sql` 이 각 함수를 살아
  있는 정의에서 떠 문장만 갈았다 — 서명 · 보안 · errcode · 나머지 본문은 그대로다(적용 전후 본문 md5 를
  견줬다). `share_my_reading` 은 errcode 없이(`P0001`) 던지던 그대로 둔다. `supabase/tests/36_suspended_sentence`
  가 옛 문장이 남은 함수 0 과 대표 함수 아홉의 새 문장을 잰다. `app/db-error.ts` 는 문장을 그대로 내보내므로
  코드는 안 바뀌고 주석과 시험의 예문만 옮겼다
- **2026-09-23** — **G-45 닫힘 — 공유 링크의 열 수 없는 화면이 하나가 됐다.** 잰 값: `app/share/{readings,people,compat}/[token]/not-found.tsx`
  셋이 md5 까지 같았고(`e5b80ba4…`), 세 주소 다 같은 `SharedReadingView` 가 같은 조건(`sharedReadingOf` 가 `null`)에서
  `notFound()` 를 던졌다. 대장의 가설 「사람 공유 링크가 풀이라고 말한다」는 어긋남이 아니었다 — 사람 공유도 §4.8 의
  「저장한 사람의 풀이」이고 궁합도 궁합풀이라 「남아 있지 않은 풀이」는 셋 다에 맞는 말이다. §4.8 「이유를 안 가른다 ·
  같은 화면이다」대로 `app/share/not-found.tsx` 한 파일로 모았고 문구는 그대로다. `scripts/check-share.mjs` 는
  `readings` 하나만 재던 것을 세 주소 다 잰다

- **2026-09-23** — **G-48 닫힘 — 고칠 것이 아니라 기록할 것이었다.** 개인 풀이·궁합 프롬프트의 지시 세대
  셋(ADR 0075) 옆에 선 자리별 예외 둘을 코드에 대고 다시 쟀다. 세대에서 정말로 벗어나는 것은 「성격을
  읽는 순서」(`personality`) 하나다 — 세대로 접으면 옛 절판 두 조합이 빨개지고 **운영 네 판은 하나도
  안 빨개진다**(운영만 보는 표는 옛 판을 못 지킨다는 말이 지금도 참이다). 「얼마나 세게 말할까」
  (`claimStrength`)는 「세대 + 인연 궁합 빼기」와 논리로 같아 접어도 시험 346개가 전부 초록이었다.
  그대로 두고 ADR 0075 에 날짜와 함께 덧붙였고, 반쯤 틀렸던 `planOf` 주석 한 곳을 고쳤다. 프롬프트
  본문은 한 글자도 안 바뀌었다. PRD 본체는 안 바뀌었다

- **2026-09-23** — **G-47 닫힘 — vitest 가 안 닿던 셋을 다시 쟀다.** 셋 다 여전히 0% 였다. `src/lib/survey` 는
  「표라 시험이 없다」가 아니었다 — 폼과 운영 화면이 부르는 순수 판단 넷(`afterPicking` · `isAnswered` ·
  `withoutHidden` · `choiceLabel`)이 있어 `src/lib/survey/index.test.ts` 25건을 세웠고 100% 가 됐다(구현은
  그대로, 판단마다 한 줄을 일부러 틀리게 해 빨개지는 것을 봤다). `app/hash-query.ts` 는 브라우저 구독 훅이고
  `app/me/reading/preview.ts` 는 DB 를 읽는 문이라 단위로 안 잰다 — 앞의 것은 e2e `saju.spec.ts` ·
  `reading-entry.spec.ts` 가, 뒤의 것은 흐름 `check-reading.mjs` · e2e `signed-in.spec.ts` 가 이미 든다.
  「흐름도 안 두드린다」던 옛 문장은 e2e 를 빠뜨린 것이었다. `docs/agents/test-map.md` 「커버리지」 · 「재지 않는 것」

- **2026-09-23** — **G-44 닫힘 — 프롬프트의 자리 이름과 화면이 되읽는 자리 이름이 한 값이고, 시험이 둘을
  함께 잰다.** 재니 대장 문장은 반만 맞았다 — 글자가 두 벌이었다(`prompt.ts` 의 `FALLBACK_NAMES` 값과
  `display.ts` 정규식 안의 `'첫 번째'`). 그리고 이름을 모르는 공유 궁합 프롬프트에 서는 자리 이름 일곱 중
  그 값에서 오는 것은 한 쌍뿐이고 다섯은 지시 본문의 글자였다(`match-reading-guide.ts` 셋 · 자료 설명 둘),
  이름을 아는 프롬프트의 「자리 이름으로 부르지 마라」 한 줄도 글자다. 「새 풀이에는 자리 이름이 없다」도
  반만 맞다 — `reading_about` 은 한쪽 별명이 비면 `names` 를 `null` 로 두어 지금도 자리 이름으로 쓰게 한다.
  말은 `display.ts` 의 `SEAT_NAMES` 하나로 두고 `FALLBACK_NAMES` 와 정규식이 거기서 읽는다(화면이 부르는
  파일이 프롬프트 몸통을 끌고 가지 않는 방향). `display.test.ts` 가 보내는 프롬프트(공유 · 비공개 궁합,
  이름 모름 · 앎)를 통째로 화면 함수에 돌려 남는 자리 이름이 없는지 잰다 — 값 · 지시문 글자 · 정규식 ·
  금지 문장 중 한쪽만 고치면 빨개지는 것을 다섯 갈래로 봤다. **프롬프트 본문은 한 글자도 안 바뀌었다**
  (기준 넷 · 두 사람 궁합 이름 모름/앎/옛 조립 여섯, 열 벌의 sha256 이 전후 같다) — 실호출은 안 한다. 화면 문구도 그대로다

- **2026-09-23** — **G-27 · G-04 닫힘 — 탈퇴의 처분이 정해졌다**(ADR 0094). 떠난 사람 혼자의 것은 지우고,
  **대화방과 메시지는 남는 쪽에 남기며**(떠난 쪽의 자리만 빈다), **함께 보던 궁합은 남기지 않는다** —
  동의 당시 그 사람의 여덟 글자와 그 Match 의 궁합풀이가 지워지고 Match 행은 방의 닻으로만 남는다.
  처음 권고는 궁합도 남기고 익명화하는 것이었는데, 재어 보니 그 결과는 신청 때부터 상대 화면에서
  내려가 있었고(`visible_matches()`), 모두가 확인한 처리방침이 「상대 화면에서도 함께 사라집니다」라고
  약속했으며, 떠난 쪽의 여덟 글자가 들어 있어 익명화가 되지 않았다. 마이그레이션
  `20260930090000_the_leaver_is_gone_and_the_room_stays.sql` — FK 여섯이 `set null`, 트리거 셋(여덟 글자 ·
  궁합풀이와 빈 Match · 열린 방 닫기), 열람 술어의 한 절, `my_chat_rooms` 의 `partner_left`, 떠난 사람의
  메시지는 신고 불가. `forget_user` 는 안 고쳤다. 화면은 승인된 두 문장(「탈퇴한 사용자」 · 넷째 줄)만
  쓴다. §5.3 에 처분의 표, §7.1 의 두 줄, §8.1 에 답, PRD 0.2 의 15번 줄. `supabase/tests/37_withdrawal`
  이 43건을 잰다. 새 틈 셋 — 처리방침이 대화가 남는다는 것을 말하지 않는다(G-51), 신고 기록의 보존(G-52),
  자동 처분(G-53)

- **2026-09-23** — **G-43 일부 — 어긋난 이름 여섯 중 둘을 고치고 하나는 그대로 둔다.** 화면의 `isSelf` →
  `isSelfPerson`(여섯 파일), 엔진 `saeun` · `wolun` 입력의 `birthDate` → `solarBirthDate`(보정된 출생 시각의
  양력 날짜). `metaphor` 는 §10 에서 「그대로 둔다」로 옮겼다 — 구조화 출력 키는 프롬프트의 일부이고 칸 이름은
  RPC 의 반환 열 · 인자다. 남은 셋(`revision` 계열 · `DiscoveryProfile` · `discovery_snapshot`)은 정한 이름과 함께
  G-43 줄에 남았다 — 마이그레이션이 들어 G-27 뒤에 간다

- **2026-09-23** — **G-03 · G-05 를 다시 적었다.** G-03 은 「한 주치를 기다린다」가 아니었다 — 집계가 이미 있고
  (2026-09-11~19, 34회 · 하루 최대 13회), 이제 실제 사용자와 검증 호출을 갈라 비용과 함께 보는 차례다. G-05 는
  낱말 고르기가 아니라 판본 정책이고, 그것을 정했다(`결정 대기` → `정했다`) — 문서 판본과 재확인 기준을 따로
  두고, 표현만 고친 개정은 재확인을 부르지 않는다. 기존 확인 기록은 고치지 않는다

- **2026-09-23** — **G-50 닫힘 — 등급 3 의 잠금을 다시 켰다.** 사람이 공개 출시를 기다리지 않고 앞당겼다.
  `.claude/settings.json` 의 `ask` 에 스물하나(`db push` · `db query --linked` · 실호출 환경변수 · Vercel 변수와
  배포 · `gh pr merge` · 원격 가지 삭제)가 돌아갔고, `docs/agents/delegation.md` 의 등급 3 잠금 칸이 같은
  목록을 든다. 운영 베타의 단계 이름(§7.0)은 그대로다 — 잠금만 앞당겼다. ADR 0093 에 「되돌렸다」 절

- **2026-09-23** — **G-05 닫힘 — 판본은 보인 것을 적고, 다시 보여 줄지는 기준이 정한다**(ADR 0095). 재확인 판단은
  앱에만 있었고 세 곳(관문 · 가입 화면 · 계정 관리)이 `NOTICE_VERSION` 을 각자 견줬다 — DB 는 판본을 적기만 한다.
  `NOTICE_ACK_FLOOR`(이 판본 이상을 확인했으면 유효하다)를 두고 판단을 `noticeAckHolds` 하나로 모았다. 차례는
  `notice-v<수>` 의 수로 센다. 처리방침의 「사주풀이 만들기」(한 번)를 「사주풀이 받기」로 맞춘 것이 첫 시연이다 —
  판본만 `notice-v5` 로 오르고 기준은 `notice-v4` 라, 운영의 확인자 열넷은 다시 안 보고 기록은 `notice-v4` 로
  남는다. 재다가 찾은 OpenAI 줄의 좁은 사실은 G-54 로 남겼다(내용 개정이라 기준이 오른다). 마이그레이션 없음

- **2026-09-23** — **G-46 을 다시 재고 여섯 중 넷을 닫았다.** #88 의 문장은 이번에도 반쯤 틀렸다.
  **죽은 export** — TypeScript 검사기로 모든 export 의 원본 심볼을 import 가 가리키는지 셌다(되내보내기는
  쓰임으로 안 친다). `app/` · `src/` · `scripts/` · 루트에서 Next 가 이름을 정한 것을 빼고 셌고, 「75/543」과는
  세는 법이 달라 수가 안 맞는다. 셋으로 갈라 **시험에서만 쓰는 것은 그대로 두고**, `export` 만 떼면 되는 것은
  떼고(타입이 대부분), 아무 데서도 안 쓰는 것은 하나씩 봤다 — 2026-09-11 에 화면에서 내린 공유 풀이 문장 둘
  (`READING_PINNED_NOTE` · `readingOrderNote`), 머리말만 남은 `EVIDENCE_SECTION`, 「여섯 자리가 쓴다」던
  `TableRow`(0 자리), DB 가 스스로 적는 `SURVEY_VERSION` 등을 지웠다. 문장을 한 번도 안 읽힌 채 이름만 내주던
  `READING_FAILURES` 는 이름의 유니언이 됐다. `READING_REDACTION_NOTE` 는 머리말이 남겨 둔다고 적은 대로 둔다.
  **골든 인용** — 옛 수(30벌 · 31건 · 서른)를 든 자리는 셋이 아니라 **일곱**(README · ADR 0002 · ADR 0071 ·
  `test-map.md` · `version.ts` · `pillars/index.ts` · `evidence/index.ts`)이었다. 수를 다시 적지 않고 지웠다 —
  건수는 `golden.snapshot.txt` 머리의 「케이스 N건」을 시험이 찍는다. 그날 잰 비교 둘은 날짜를 달았다.
  **후보 카드 문구 조립** — 날값에서 카드 말 한 벌을 짓는 걸음이 `app/me/candidates.ts` 와 미리보기 예시에
  두 벌, 요청함·인연 결과의 `suppliedText(knownElementsOf(…))` 가 다섯 자리였다. 앞의 것은
  `candidateCardText` 하나로, 뒤의 것은 `suppliedText` 가 날값을 받게 해 모았다 — 옛 조립과 새 조립이 입력
  215,605 가지에서 같은 글자를 내는 것을 견줬고 e2e 가 덱·보관함·요청함·결과를 밟았다. **`src/lib/matching`
  삭제 시험** — 틀렸다. 부르는 쪽은 하나(`app/compat-match.tsx`)지만 지우면 판단과 문장이 vitest 가 못 닿는
  `.tsx` 로 옮겨 가고 그것을 재는 시험 셋이 갈 곳을 잃는다(ADR 0080) — 복잡함이 사라지지 않고 옮겨 간다.
  그대로 둔다. **린트 경고 둘** — `emptiness.ts` 의 안 쓰는 import 를 지웠다. 남은 하나는 `src/lib/account`
  라 G-27 뒤로 미뤘고, 풀이권 칩 위치는 사람이 답할 화면 결정이라 G-46 에 남겼다

- **2026-09-23** — **G-43 일부 — 후보 목록의 표가 「스냅샷」을 내려놓는다.** `discovery_snapshot` ·
  `discovery_snapshot_slot` → `discovery_candidate` · `discovery_candidate_slot`(제약 · 인덱스 · 시퀀스 이름도
  따라 옮겼다). 앱은 표를 직접 안 읽고 정책도 없어 한 마이그레이션으로 된다 — 표를 부르는 함수 여섯을 살아
  있는 정의에서 떠 표 이름만 갈았다. 앱이 부르는 RPC 셋과 `snapshot_id` 칸은 §10 에 「그대로 둔다」

- **2026-09-23** — **G-43 일부 — DiscoveryProfile 이 타입과 읽는 문 하나를 얻었다.** `src/lib/discovery` 의
  `DiscoveryProfile`(`preferGender` · `optedOut`)과 `app/me/discovery/discovery-profile.ts` 의 `myDiscoveryProfile`.
  못 읽으면 값으로 말한다(`SkippableRead`) — 홈과 매칭은 미리 거르지 않고 RPC 에 맡기고(전과 같다), 설정은 두 칸을
  비운다(전에는 기본값으로 메워 끈 사람에게 「켜져 있다」고 말할 수 있었다). 화면 안의 옛 자리 둘이 빠져 열셋이
  열하나가 됐다. 값의 목록 `PREFER_GENDERS` 는 lib 로 내려갔다. 저장 액션의 「처음인가」 확인은 쓰기의 앞자리라 그대로 둔다

- **2026-09-23** — **G-43 닫힘 — 코드가 용어집과 다른 이름 여섯을 고치거나 까닭과 함께 남겼다.** 좁히기
  `20261002090000_the_old_edit_name_is_gone.sql` 이 옛 겉 `add_person_revision` 을 지웠다 — 앱(#137)과 스크립트 · pgTAP
  에서 부르는 자리 0, DB 안에서 부르는 함수 0 을 재고 썼다(운영 호출 수는 `track_functions = none` 이라 못 쟀다).
  여섯의 끝: `isSelf` → `isSelfPerson` · `birthDate` → `solarBirthDate`(#130), revision 계열 → `edit_person_input` ·
  `may_edit_person_input` · `input/edit.ts` · `edit-input.tsx`(#135 넓히기 · #137 앱 · 이 좁히기), 후보 목록 표 →
  `discovery_candidate`(#136), `DiscoveryProfile` 타입과 읽는 문(#139). **그대로 둔 것**은 §10 이 까닭과 든다 —
  `metaphor`, 후보 목록의 RPC 셋 · `snapshot_id`, 사유값 `unreadable-revision`

- **2026-09-23** — **G-50 을 다시 열었다 — 등급 3 의 잠금은 공개 출시 전까지 꺼 둔다.** 같은 날 #134 가 잠금을 켰는데,
  「G-50 진행」을 「지금 켜라」로 잘못 읽은 것이었다. 사람의 뜻은 ADR 0093 그대로였다. `ask` 를 비우고 delegation.md 의
  「공식 운영에 들어가면 켜는 잠금」 절과 시험을 되살렸다. 켜 있던 동안 잰 것 둘 — 사용자 `allow` 가 `ask` 를 이긴다,
  `deny` 가 앞에 `cd` 가 붙은 force push 를 못 막는다 — 은 delegation.md 「잠그지 않은 것」과 ADR 0093 에 남겼고,
  G-50 의 조건에 넓은 `allow` 걷기와 창이 뜨는지 재기를 더했다

- **2026-09-23** — **G-46 닫힘 — 남은 둘.** ① G-27(#129)이 든 뒤 계정·채팅 쪽을 같은 잣대로 다시 쟀다.
  `export` 만 떼면 되는 것 여섯(`AccountStatus` · `ScreenAccount` · `AccountRead` · `SEND_OUTCOMES` ·
  `BodyCheck` · `SendResult`)은 뗐고, 아무 데서도 안 부르던 서버 쪽 `unreadChatCount` 는 지웠다 — 헤더는
  같은 문(`app/me/chat/unread.ts`)을 브라우저에서 부른다. `LEFT_USER_LABEL` 은 그사이 시험과 e2e 가 쓰게 돼
  그대로 둔다. 린트 경고 `ACCOUNT_STATUSES` 는 배열이 타입을 내주는 데만 쓰여 유니언으로 적었다 — 린트 경고 0.
  남은 것은 생성 파일(`database.generated.ts`)과 머리말이 일부러 남긴 `READING_REDACTION_NOTE` 뿐이다.
  ② 풀이권 칩은 **헤더(`app/site-header.tsx`)에 그대로 둔다** — 2026-09-23 사용자 답. `/` 를 정적으로 두려고
  브라우저에서 읽는다(ADR 0078)
- **2026-09-23** — **G-50 을 대장에서 지웠다 — 등급 3 의 잠금은 할 일이 아니라 단계에 매인 불변조건이다**(#142, ADR 0093
  추기). 「다시 켠다」라는 줄이 `정했다` 로 서 있어 #134 가 지금 할 일로 읽었다. 이제 단계는 §7.0 표의 「(지금)」
  한 곳이고, `scripts/code-rules.test.ts` 가 공개 출시 전에는 `ask` 가 비어 있는지 직접 잰다. 그날의 걸음 셋은
  `docs/agents/delegation.md` 「공식 운영에 들어가면 켜는 잠금」이 든다
- **2026-09-23** — **§6.2 의 「채팅방만 아직 구현하지 않았다」 · 「상대와 연락하는 기능은 아직 없으므로」를 걷었다**(#147).
  채팅은 #119 에 섰는데 PRD 와 매칭 요청 확인창이 옛 제한을 말하고 있었다. 확인창의 그 문단은 대체 없이 지웠다 —
  풀이권 임시 차감과 공개 범위 두 문장이 남는다. 대화방은 수락하는 순간 열린다(§7.1)

- **2026-09-23** — **보류 띠와 미정 여섯에 사람이 답했다.** 닫은 것 — **G-14**(실시간 접속 표시는 안 한다 — 구간 셋을
  화면을 다시 읽을 때 갱신하는 지금 방식, §7.2 · §8.8) · **G-30**(사주 보기와 궁합 보기를 한 토글로 합치지 않는다 — 폐기,
  §7.4.2) · **G-31**(궁합을 볼 때마다 관계를 보여 주고 저장한 관계를 미리 고르며 바꾸면 새 값을 남기는 지금 동작이 결정,
  §8.2) · **G-34**(인연 궁합에 개인 판정을 넣지 않는다, §8.6) · **G-35**(개인 풀이 말투 실험 폐기) · **G-36**(긴 각주 넷의
  접이칸 폐기). 정했다로 옮긴 것 — **G-03**(하루 상한 500 · 알림 400 · 운영 검증 계정 따로 세기, §8.3) · **G-32**(판을 고르는
  기능은 없다, 검산 화면 정리, §8.4) · **G-33**(자리 색인을 더한다 — 9/1 실험은 실익을 못 보였고 이번은 사람의 새 결정,
  §8.5) · **G-37**(로그인 사용자의 생성만 세는 최소 재사용 측정) · **G-42**(크론 실패 알림부터) · **G-52**(떠난 뒤에도 신고
  자료를 6개월 따로 둔다 — 법령 근거를 ADR 로 먼저) · **G-53**(신청 뒤 3일 안에 크론이 처분한다 — 영업일 · 공휴일 계산 폐기).
  G-52 · G-53 은 공개 출시 띠에서 운영 베타 띠로, G-32 · 33 · 37 은 보류 띠에서 운영 베타 띠로 옮겼다. G-51 문구는
  승인됐고 G-54 · G-53 · G-52 의 문구와 한 판본(`notice-v6`)으로 오른다(#165). 대장 줄이 16으로 줄어
  `code-rules.test.ts` 의 파싱 문턱(줄 20 · § 15)을 10으로 내렸다 — 0 으로 떨어지는 것을 막는 문턱이다
- **2026-09-23** — **G-03 닫힘 — 하루 전체 상한이 500, 알림이 400 이고, 운영 검증 계정의 시도를 따로 낸다**(#167, ADR 0039 덧).
  세는 법은 안 고쳤다 — 시도 행을 여는 문이 `start_reading_run_for` 하나이고 다섯 경로가 다 지난다(로컬에서 잼).
  새 표 `verification_account` 와 지출 표의 `verification_*` 칸 넷. 운영의 34회 중 5회가 검증 계정의 것이었다.
  바깥 벽: 분당 요청 500 · 토큰 500,000(응답 머리로 잼), 외부 월 예산은 확인하지 못했고(usage API 403) 예산 소진 때 provider 가 먼저 거절할 위험은 사람이 수용했다
- **2026-09-23** — **G-32 닫힘 — 검산 화면에 실험판이 서지 않는다.** `/me/reading/inspect` 의 「실험용 변형」 카드와
  미리보기의 `variants` 칸을 걷었다 — 판을 고르는 자리는 코드 어디에도 없었고(생성 경로는 `CONTROL` 하나), 운영판
  곁에 나란히 서 있어 살아 있는 것처럼 읽혔다. `PROMPT_VARIANTS` · `PAIR_VARIANTS` · `MATCH_INPUT_VARIANTS` 는
  단위 시험 다섯 파일과 `call.live.test.ts` 가 도구로 쓰므로 남긴다. e2e 는 변형 id 가 화면에 없는지를 판의 id 로 잰다(§8.4)
- **2026-09-23** — **G-52 의 법령 근거를 ADR 0098 에 적었다**(#168). 6개월은 다른 법령이 시킨 보존이 아니라 목적 보유다
  (개인정보 보호법 제21조① 본문 · 제15조①6 · 제30조). 전기통신사업법 제83조③ 은 가진 것을 줄 수 있는 절차이지 보존
  의무가 아니고, 이 서비스는 소규모 부가통신사업자로 신고 간주될 가능성이 높다(시행령 제30조). 통신비밀보호법 시행령
  제41조의 접속 기록 보관이 걸리는지는 자문에 남겼다 — 지금 IP 는 남기지 않는다. 처리방침 문구는 초안으로 승인 대기
- **2026-09-23** — **G-53 닫힘 — 탈퇴의 처분을 크론이 신청 뒤 3일 안에 한다**(#170, ADR 0094 덧). 달력의 3일이고
  영업일 · 공휴일 계산은 폐기했다. 앱에서 신청한 경우 하나만 대상이고 연락처 요청 경로는 두지 않는다. 신청 뒤 하루가
  지나면 매시 집고, 처분과 흔적 검사가 한 덩어리라 흔적이 남으면 되감긴다. 실패 · 기한 초과만 운영자에게 알린다.
  §5.3 · CONTEXT 「탈퇴 대기」 · 「탈퇴」 · runbook 「탈퇴 신청의 처리」를 고쳤다. 화면과 처리방침의 「영업일」 문장은
  #165(`notice-v6`)가 고친다
- **2026-09-23** — **G-52 의 물음 둘에 사람이 답했다**(ADR 0098 추기). 보존 항목에 가입일 · 탈퇴일을 넣는다 — 처리방침
  초안 3 에도 붙었다(여전히 초안). IP 는 새로 수집하지 않고, 통신비밀보호법상 접속 기록 보존 의무와 전기통신사업자 해당
  여부는 G-25 로 넘겼다
- **2026-09-23** — **G-42 닫힘 — 크론이 실패하거나 꺼지거나 밖으로 부른 요청이 실패할 때만 운영 알림이 간다**(#173, ADR 0039 덧).
  `cron-watch`(10분)가 등록된 잡 전부의 실패(`cron-failed:<잡>`), `pg_net` 의 2xx 아닌 응답(`net-request-failed` —
  복구기는 잡이 초록이어도 주소가 실패할 수 있다), 꺼진 잡(`cron-inactive:<잡>`)을 `notify_ops` 로 알린다. 정상 실행은
  안 알린다. 재시도 소진은 그 자리(처분 기한 초과 · 상한 도달)가 이미 알린다. 분석 도구 · Sentry 는 범위 밖
- **2026-09-23** — **G-33 닫힘 — 모든 풀이 프롬프트가 사실에 번호를 달고 자리 색인을 싣는다**(ADR 0099, `reading-prompt-v13`).
  개인 풀이(self · person)에도 「자리가 붙은 사실」이 처음 선다. 줄마다 `[R1]` · `[S1]` 번호, 맨 아래 자리 색인은
  번호만 들어 한 사실이 두 번 세어지지 않는다. 판본 두 칸이 다 v13 으로 올랐다. 새 한글 셋은 사람이 승인했다.
  실호출 두 차례 열 편(네 kind + 시간 미상 개인 풀이) — 저장 검사 10/10, 자리 검사 넷(틀린 자리 · 번호 누설 ·
  천간 신살과 지지 관계 묶기 · 시간 미상 합 단정) 0. 인연 궁합 한 편이 검토용 근거 절에 번호를 적었다 — 화면에는
  안 나간다(§8.5)
- **2026-09-23** — **G-37 닫힘 — 최소 재사용 측정은 runbook 의 질의 하나다**(#171). 「제품 지표」의 「풀이 재사용」이
  요청 · 성공 · 실패 · 성공률과 첫 성공 뒤 30일 안에 다시 성공한 사용자 비율을 `reading_run` 에서 센다 — 사용자 ID ·
  상태 · 시각만 읽고 운영 검증 계정은 뺀다. 마이그레이션 없음. 운영 첫 값: 요청 29 · 성공률 96.6% · 창이 닫힌 사람 0
- **2026-09-23** — **G-55 를 열었다 — 개인 풀이 본문에 「명식」 · 「원국」이 남는다**(ADR 0099 · #178 의 실호출에서 잼, v13 회귀 아님)
- **2026-09-23** — **근거 칸의 사실 번호를 기록하고, 인연 궁합 입력 평가가 번호 붙은 관계 인용(`relations R4 자축합토`)을 같은 인용으로 센다**(ADR 0099 추기). 프롬프트 본문 · 판본은 그대로
- **2026-09-23** — **G-51 · G-54 닫힘 — 처리방침의 내용 개정 넷이 한 판본(`notice-v6`)으로 올랐다**(#165, ADR 0095).
  ① 보낸 메시지가 상대의 대화방에 닫힌 날부터 90일 남는다(「파기 절차와 방법」 · `DELETION_NOTE`, G-51) ② OpenAI 로
  나가는 때가 셋이다 — 사주풀이 받기 · 궁합풀이 받기 · 수락 뒤 자동 생성(G-54), 「밖으로 나가는 것」의 끝 줄도 그만큼
  ③ 탈퇴는 앱의 신청 뒤 3일 안에 자동으로 지운다 — 「운영자가 확인한 뒤」 · 영업일 문구가 코드에서 0(G-53 의 크론은
  #170) ④ 새 절 「신고 기록은 따로 둡니다」 — 떠난 사람의 신고 기록을 처분일부터 6개월 분리 보관(G-52, ADR 0098 —
  보관을 켜는 것은 #174). 기준이 `notice-v4` → `notice-v6` 으로 올라 확인한 사람은 **한 번** 다시 읽는다. 연락처로 보내는
  삭제 요청의 자동 처리 경로는 두지 않는다(사람의 결정) — 일반 문의 연락처는 권리 행사 절에만 남는다
- **2026-09-23** — **개인 풀이 지시문이 「명식」 · 「원국」 · 「충」 대신 사용자에게 쓸 말로 가르치고 판본이 `reading-prompt-v14` 로 오른다**
  (G-55, ADR 0099 추기). 공통 규칙이 「타고난 구조」 · 「사주」 · 「맞부딪힘」으로 적히고 쉬운 말 규칙에 본보기 한 쌍이 선다.
  시간 미상 규칙이 궁합 읽는 법에도 실려 두 궁합도 v14 다. `plain-terms-exposed` 는 목표 그대로. G-55 의 기준값 단위를
  바로잡았다(v13 은 낱말 가짓수, 옛 아홉 편은 출현 횟수). 실호출과 G-55 의 닫힘은 아래 줄
- **2026-09-23** — **G-52 닫힘 — 떠난 사람의 신고 기록이 처분일부터 6개월 따로 남고 운영자만 읽으며 지나면 지워진다**(#174, ADR 0098 추기).
  `auth.users` 의 `before delete` 트리거가 그 사람이 든 신고와 스냅샷을 `retention.report` 로 옮긴 뒤 일반 표에서 cascade 로
  지운다 — 사유와 상세 · 신고 시각과 검토 상태 · 불변 스냅샷 · 두 계정의 UUID · 당시 로그인 이메일 · 가입일 · 탈퇴일. `retention`
  스키마는 `anon` · `authenticated` · `service_role` 셋 다 못 쓴다. 처분일(먼저 떠난 쪽의)부터 6개월 뒤 크론
  `report-retention-purge`(매시 47분)가 지우고, 보류(`hold_reason`)가 걸린 줄만 남긴다. IP · 실명 · 전화번호는 받지 않는다.
  §5.3 · CONTEXT 「탈퇴」 · runbook 「떠난 사람의 신고 기록」(수사기관 요청 · 보류 걸고 풀기)을 고쳤다. 처리방침 문구는 그대로다(#165)
- **2026-09-23** — **G-55 닫힘 — 지시문이 가르치던 「명식」 · 「원국」 · 관계 「충」을 걷었다**(#186, ADR 0099 추기, `reading-prompt-v14`).
  종료 조건을 좁혔다: `plain-terms-exposed` 전체 0 이 아니라 지시문이 가르치던 세 말을 걷는 것이다 — 검사는 목표(`target`)
  그대로이고 **전체 분류명이 0 이 된 것은 아니다.** v14 실호출 개인 풀이 9편(self · person · 시간 미상 self 각 3)에서
  「명식」 0 · 「원국」 1(모델이 스스로 쓴 「자료 밖」 단서 문장) · 관계 「충」 0(한 편은 원문 유실로 미계수). v13 은 개인 풀이
  6편 중 3편이 넘었다. 남은 것은 후속 이슈 셋이 든다 — 개인 풀이의 `일간` · `대운`(G-56, #191), `plainTermsIn` 의 낱말 조각
  오탐(#189), 동시 실호출의 원문 덮어쓰기(#190)
- **2026-09-23 · G-56 · #191** — 개인 풀이 판본 `reading-prompt-v15`. 규칙이 「일간」·「대운」을 스스로 부르지 않고
  (단정의 예 「金이 셋이다」, 운 칸 「십 년 단위의 큰 흐름」·「그해의 흐름」·「그달의 흐름」) 쉬운 말 §5 에 일간 · 대운 본보기
  한 쌍이 더 선다. 궁합은 v14 그대로. 운영자 판단으로 실호출 없이 G-56 을 닫는다(ADR 0099 추기)
- **2026-09-23** — **G-22 닫힘 — 파는 것은 풀이권 묶음이다**(ADR 0100). 1회 · 3회 · 5회 · 20회 넷, 산 풀이권에 유효기간
  없음, 무료 몫은 공개 출시부터 1개(지금 5), 인연 요청은 지금처럼 같은 풀이권을 예약한다. 산 몫은 한도 위에 얹힐 뿐이라
  ADR 0021 · 0038 은 그대로 선다. 가격 · 베타 테스터의 몫 · 일부 쓴 묶음의 철회는 G-21 줄로 옮겼다. 코드는 안 바뀌었다
- **2026-09-23** — **G-21 에 적었다 — 무료 몫과 예외 몫은 산 것이 아니라 어떤 경우에도 환불되지 않는다**(ADR 0100 덧)
- **2026-09-23** — **G-20 을 고쳐 정했다 — 본인인증이 가입의 문이다**(ADR 0101). 가입 폼이 PASS 인증 → 닉네임 → 안내 확인
  순이고, 만 19세 이상, CI 로 한 사람 한 계정. 인증 생년월일은 나이 확인에만 쓰고 **§7.3 의 성별 · 년 잠금을 걷었다.**
  무료 풀이권은 한 사람에게 평생 한 번 — 떠난 뒤에도 HMAC 식별값과 받은 사실 · 시각만 따로 남고, 그 보관의 적정성은
  G-25 가 든다. 베타 계정은 공개 출시 때 인증만 받는다. §7.0 의 채팅 접점 줄이 닫혔다. 코드는 안 바뀌었다
- **2026-09-23** — **G-21 가격을 정했다** — 1회 4,900 · 3회 12,900 · 5회 19,900(추천) · 20회 59,000원(최대 할인), 부가세 포함,
  할인율은 「1회권 대비」로 적는다(§7.0). 판매는 G-25 의 철회 기준 뒤에 연다. 결제수단은 국내 카드 먼저. PG 는 PortOne V2 +
  NHN KCP 가 후보이고 업종 입점 · CI 제공을 확인한 뒤 확정한다. **저장 자리 10명을 공개 출시에서 걷는다**(ADR 0102) — 20회권이
  ADR 0032 의 「자리 > 풀이권」을 깨므로. 걷기 전에 궁합의 고르는 칸이 바뀐다. 남용 빗장은 G-23. 코드는 안 바뀌었다
- **2026-09-23** — **G-25 에 적었다 — 결혼중개업법은 지금 비해당으로 판단하고 공개 출시 전 법률 검토에서 적용 여부만 확인한다.**
  요청 · 연결을 무료로 가르는 안은 걷었다 — 인연 요청의 풀이권 예약과 수락 때 자동 생성은 그대로이고(ADR 0038 · 0100),
  하루 요청 상한도 새로 두지 않는다. 광고 · 수집 · 보장 · 약관 정의의 경계 넷을 G-25 줄에 적었다
- **2026-09-23** — **G-25 를 다시 적었다 — 정한 것 다섯과 변호사 검토로 정할 것 여섯.** 정했다: 결제 · 철회 기록 5년과 분쟁 처리
  기록 3년을 탈퇴 처분의 예외로 따로 둔다 · 운영자 접속기록 1년 이상 · 문의는 이메일과 앱 안, 3영업일 첫 답(1372 안내는 문의 절차에
  두지 않는다). 검토로 넘겼다: 환불 산식 · 탈퇴 때 남은 유료 몫 · HMAC 기한 · 이용자 접속 기록 3개월 · 약관 전문(1372 를 분쟁
  조항에 넣을지 포함) · 결혼중개업법. 끝 조건에 변호사 검토 1회를 넣었다
- **2026-09-23** — **Next 16.3.1 → 16.3.6**(`1e1f6c8`) — critical 셋(이미지 최적화 AVIF RCE · Windows 호스트 RCE · `next/og` RCE)과
  sharp high 를 닫았다. 운영 의존성 감사 0
- **2026-09-23** — **G-23 을 자체 점검표 열둘로 정했다.** 끝 조건은 줄마다 증거를 남기는 것이고 외부 모의해킹은 결제 운영 뒤 따로다.
  사람 저장 빗장 시간 20명 · 하루 100명(총량 없음, ADR 0102 덧), 신고 빗장 하루 20건과 같은 대상 · 같은 사유 중복 차단 —
  둘 다 DB 에서 원자적으로 센다. IP 보조 제한은 두지 않는다(CI 한 사람 한 계정이 막고, IP 수집은 G-25 가 안 정했다)
- **2026-09-23** — **G-23 의 ② ③ ④ ⑤ 를 만들었다**(`20261007090000`). 사람 저장은 사용자당 시간 20명 · 하루 100명, 신고는 하루
  20건이고 검토 전의 같은 대상 · 같은 사유는 또 못 낸다 — 둘 다 DB 가 그 사람을 잠근 뒤 센다(§2). 두 신고 문이 가입 완료를
  묻는다. `security definer` 129개가 이미 `search_path` 를 고정했음을 pgTAP 이 지킨다. 보안 헤더 넷을 강제하고 CSP 는
  `Report-Only` 로 세웠다 — e2e 가 모든 화면에서 어긴 자리 0 을 잰다. CSP 강제는 G-23 ② 에 남았다 운영 반영(2026-09-23): `migration list` remote 칸에 `20261007090000` · 트리거 `person_save_rate` 있음 · 고정 안 된 definer 0 ·
  발행 키로 `report_user` 를 부르면 `42501`(PostgREST 가 함수를 찾았다). 옮기기 전 운영의 두 신고 함수 정의가 로컬 덤프와 같음을 대조했다
- **2026-09-23** — **G-23 ⑤ 를 검토대로 고쳤다**(`20261008090000`). 중복 신고를 하루 수보다 먼저 판정하고(한도에 닿은 사람도 같은
  신고에는 「이미 접수」를 읽는다), 중복 문구를 「같은 사유의 신고가 이미 접수되어 검토 중입니다.」로 바꾸고(사람이 승인), 신고한
  사람 · 미검토 쌍 · 스냅샷 메시지 인덱스 셋을 더했다. 익명 e2e 도 CSP 자동 손잡이(`e2e/anon.ts`)를 지나 증거가 「시험 전부」가
  됐다. 사람 저장 문구 둘과 신고 하루 한도 문구 · 「가입을 먼저 끝내 주세요.」는 승인됐다. 운영 반영: remote 칸 `20261008090000` ·
  인덱스 셋 있음 · `report_user` 캐시 `42501`
- **2026-09-23** — **G-23 ① ⑫ 를 닫았다.** 운영 의존성 감사가 CI 차선 `audit`(`npm audit --omit=dev --audit-level=high`)이 됐다 —
  의존성 목록을 바꾼 PR 만 막고, 새 advisory 는 main 푸시 · 하루 한 번의 일정이 `ci-main-red` 로 알린다(ADR 0104). 2026-09-23
  값은 운영 0. 개발 의존성은 vitest 4.1.11 · js-yaml 4.3.2 로 올려 `npm audit` 전체 3 → 0
- **2026-09-23** — **G-23 ② CSP 를 강제로 올렸다**(`next.config.ts`). 보고만 하던 정책과 `frame-ancestors` 한 줄을 강제
  정책 하나로 합치고 `Report-Only` 를 걷었다. `script-src` · `style-src` 의 `'unsafe-inline'` 은 남겼다 — nonce 는
  돌지만 정적 넷이 동적이 되고, SRI 는 인라인 조각에 막힌다(값은 `docs/notes/csp-nonce-2026-09-23.md`). 되돌리는 법은
  runbook 「CSP 가 화면을 막을 때」
- **2026-09-24** — **G-24 관리자 화면 1차판 「신고 증거 열람대」가 섰다**(ADR 0103, `20261009090000`). `/ops/reports` 는 현재 계정에
  연결된 신고를 최신부터 30건씩 내고 검토 상태 · 사유 · 대화 근거로 거른다. `/ops/reports/[reportId]` 는 신고 내용 · 두 계정의
  닉네임 · UUID · 지금 상태와 신고 당시의 불변 스냅샷을 시간순으로 세우고 고른 메시지 하나를 「신고한 메시지」로 강조한다.
  읽기 전용이고 경계는 `/ops/survey` 와 같다 — 운영자 전용 문 셋이 `is_operator()` 를 묻고 `service_role` 에는 안 열린다. 이메일 ·
  출생정보 · 풀이 · 방으로 가는 실마리는 문이 안 내준다. 떠난 사람의 신고는 화면에 없다(ADR 0098). G-24 의 끝 조건을 이 열람대로
  좁혔고, 운영자 계정으로 프로덕션에서 한 번 열면 닫는다. `/ops/**` 의 운영자 전용 설명 · 제목 · 빈 상태 문구는 표 승인 없이 쓰고
  PR 에 모아 보고한다(개인정보 · 권한 · 제재 · 사용자에게 가는 문구는 예외 밖)
- **2026-09-24** — **G-23 ⑧ 을 닫았다 — 비밀은 서버에만 있고, 새면 교체가 먼저다.** 앱이 읽는 환경변수 열둘과 SDK 가 읽는 둘을
  비밀 · 공개 · 설정으로 갈라 `scripts/secret-env.mjs` 에 두고, 비밀을 읽는 모듈 셋(`keyed-client` · `model` · 복구기 라우트)을
  `import 'server-only'` 로 잠갔다. `npm run build` 가 끝에 브라우저로 가는 파일에서 비밀 이름과 그 빌드의 비밀 값을 찾는다 — Vercel 의
  배포 빌드도 진짜 값으로 지난다. webhook 의 401 이 SDK 오류 문장(「서명 비밀이 비었다」)을 답에 싣던 것을 걷었다. runbook 에
  비밀마다 새 값의 자리 · 넣는 곳 · 재배포 · 끊는 동안 멈추는 것을 적었다. git 역사에 샌 흔적은 없었다. 운영의 Vercel 변수는 일곱 줄로
  코드와 맞고, 서명 비밀은 Production 에만 있다(runbook 이 「둘 다」라 적고 있었다)
- **2026-09-24** — **G-23 ⑪ 을 닫고 ⑩ 의 표를 세웠다**(`20261009120000`). 운영 security advisor WARN 87 → 71: 상수 함수 열다섯의
  `search_path`(0011 15 → 0)와 밖에서 아무도 안 부르는 definer 셋(`claimed_by` · `may_edit_person_input` · `beta_is_over`)의 문을
  닫았다. 같은 날 G-24 의 운영자 문 셋이 0029 에 더해졌다. 남은 70 은 앱이 부르라고 연 RPC 문, 1 은 Pro 플랜부터인 유출 비밀번호
  검사다(runbook 「보안 advisor」). 운영자 접속기록은 자리마다 보존 일수를 쟀다 — Supabase SQL · 표 편집기와 `/ops` 는 읽기 기록이
  0일이라 ⑩ 은 열려 있고, DB 안 운영자 접속기록 표를 권했다(runbook 「운영자 접속기록」). 운영 반영: remote 칸 `20261009120000` ·
  발행 키로 `rpc/beta_is_over` 는 `42501`
- **2026-09-24** — **궁합의 두 사람 고르는 칸이 찾아 고르는 모양이 됐다**(ADR 0102, G-21). `select` 둘이던 「저장한 사람」
  칸이 이름을 입력하거나 목록에서 고르는 칸이다 — 이름 · 초성을 쳐서 좁히고, 누르면 전체 목록(다섯 줄 반 높이)이 서서 스크롤이나 키보드(↑ ↓ · Enter · Esc)로 고른다 — 저장 한도 10 을 걷는 날 스물 · 백이 들어와도
  읽히라는 것이다. 다른 칸에서 고른 사람이 안 서는 규칙 · 첫 칸의 내 사주 · 주소로 미리 골라져 들어오는 길은 그대로다(§4.5).
  한도 10 은 그대로이고 `/me/people` 목록은 같은 물음을 받아 G-21 에 남았다. 문구 「이름으로 찾기」 · 「찾는 사람이 없습니다」는 운영자가 승인했다
- **2026-09-24** — **운영자가 개인정보를 읽는 순간부터 접속기록이 남는다**(ADR 0105, G-23 ⑩ · G-24 · G-25). 개인정보 열람은
  `/ops/**` 화면으로만, 개인정보 직접 SQL 은 사람만 break-glass 로(에이전트는 예외 없이 금지). 운영자 문 셋이 읽을 때마다
  `audit.operator_access` 에 한 줄(추가만 된다), 거절은 앱의 문이 따로 적고, `npm run db:remote` 는 목적 없이 안 돌며 목적 · SQL
  해시만 남긴다. 매일 S3(Object Lock) 반출 크론이 섰지만 AWS 계정이 없어 「설정 안 됨」으로 꺼져 있다 — ⑩ 은 켜지는 날 닫힌다.
  신고에 검토 기록(검토한 사람 · 결과 넷 · 근거 · 제재를 받은 쪽 · 실행한 사람)이 붙고 runbook 의 검토 SQL 이 채우며 상세가 읽는다 —
  신고한 사람에게는 안 열린다. 운영 주기(신고 영업일마다 · 3영업일 안 1차 판단 · 접속기록 월 점검)는 내부 목표이고 화면 문구는
  안 바꿨다. 운영자가 3영업일 넘게 비우면 `public.signup_pause` 한 줄로 새 가입을 닫는다(기존 문장 「지금 쓸 수 있는 코드가
  아닙니다.」). ADR 0103 의 「쓰는 누름이 드는 날 접속기록」을 「열람이 시작되는 날부터」로 고쳤다. Vercel Preview 는 문서 ·
  마이그레이션 · pgTAP · 단위 시험만 바뀐 커밋을 건너뛴다(#199)
- **2026-09-24** — **G-25 의 초안 셋이 섰다 — 화면은 그대로다.** 이용약관 초안(`docs/legal/terms-draft.md`, 31조 · 과기정통부 고시
  제2022-66호의 조 구성), 처리방침 v7 후보(`docs/legal/privacy-v7-draft.md` — 본인확인 · 결제 · 무료 지급 기록 · 운영자 접속기록을 더하고
  보유기간을 베타 종료일에서 탈퇴 기준으로 옮김, v6 대조표), 변호사 검토 요청서(`docs/legal/counsel-request.md`). 사람만 채울 값은
  `［사업자등록 후 기입］`, 법률 판단은 `［변호사 검토 필요］` 로 두고 환불 산식 · 탈퇴 때 남은 유료 풀이권 · 분쟁 조항 · 무료 지급 기록의
  보관 기간 · 이용자 IP 는 선택지를 나란히 적었다. 법령은 국가법령정보센터 공개 API 로 원문을 읽은 것만 인용했다. `notice-v6` 과
  가입 폼은 안 건드렸다 — v7 반영은 검토와 문구 승인 뒤(G-25)
- **2026-09-24** — **산 풀이권의 장부가 섰다 — 판매는 닫힌 채**(G-21 ⑤ ④, ADR 0106). 결제 주문 · 산 묶음 · 환불 · 제공자 알림 · 사용 이력 다섯 표(`20261011090000`). 한도가 무료 + 예외 + 산 묶음(걷은 것 뺀)이 되고 잔액은 여전히 센다 — 산 몫이 0 인 지금 모든 계정의 한도 · 잔액은 그대로다. 사용 이력은 무료 → 예외 → 오래된 묶음 차례로 자리를 잡는 순간 적히고 환불 셈에만 든다. 승인 · 취소 · 환불은 서버 문, 판매 스위치는 꺼짐. 운영자 문 `operator_reading_refund_basis` 가 환불 셈의 입력을 묶음별로 내고 접속기록에 남긴다. 떠난 사람의 결제 기록은 `retention.reading_payment` 에 5년, 로그인 이메일은 안 옮긴다. `request_match` 가 셈 전에 풀이 시작과 같은 사람 자물쇠를 잡는다 — 둘이 마지막 한 번을 나란히 읽으면 둘 다 지나가던 틈을 닫았다. 가격 표의 할인율 19 · 40% 는 반올림(실제 18.8 · 39.8%)이라 G-21 에 사람이 정할 것으로 남겼다
