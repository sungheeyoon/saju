# 엔진을 지은 차례

> **세션 기록이다.** 세션 메모 「엔진이 어떻게 지어졌나」(2026-08-13~17)를 2026-09-22 에 저장소로 옮겼다(ADR 0090). 요구사항도 규칙도
> 아니다 — 무엇을 만드는가는 `docs/prd.md`, 결정은 `docs/adr/`, 낱말은 `CONTEXT.md` 가 답하고,
> **코드와 어긋나면 코드가 맞다.** 날짜가 붙은 문단은 그날의 사정이고 커밋 해시는 그날의 자리다.
> 여기 적힌 경로가 실재하는지는 시험이 재지 않는다.
>
> **지금은:** v1(L1·L2·L3 + 1:1 궁합)은 2026-08-17 에 끝났다. 코드가 왜 이 모양인지 되짚을 때만 연다 — 로드맵으로 읽지 않는다.

엔진이 **어떻게 지어졌는가**의 기록이다. 무엇을 만들지의 목록이 아니다 —
지금 기준은 `docs/prd.md` 와 GitHub 이슈다((옛 메모 saju-prd-axis — 저장소 문서로 흡수됨)).

코드가 왜 이 모양인지 되짚을 때 쓴다. 판단의 이유는 `engine-decisions.md` 와
`text-layer-decisions.md` 가 든다.

`~/Documents/projects/saju` (github.com/sungheeyoon/saju) 는 3층 설계다.
**L1·L2·L3 모두 구현됐고 두 화면에 다 붙었다(2026-08-17).** v1 목표(L2 + 1:1 궁합 +
링크 공유)는 2026-08-16 에 달성했다.
현재: 커밋 `5a548a1`, 테스트 **1047건**, 말뭉치 **90/90**, `npm run verify` 통과.
e2e 는 **31건**(데스크톱·모바일) — `npm run verify` 가 e2e 를 **안 돌린다**.

- **L1 만세력 엔진** — 순수 함수, AI 0%. 절기 → 시간 보정 → 4주 → 오행/십성.
  2026-08-15 완료(v0). `src/lib/saju/` 참조.
  이후 같은 날 추가: 입력 검증(`input.ts`), 시간 미상(`hour: null`), 성별(필수),
  **대운**(`daeun/`). 테스트 245건.
- **L2 관계 연산** — 순수 함수, AI 0%.
  - **1단계 원국 형충회합: 2026-08-15 완료.** `relations/` 의 `findRelations(pillars)`
    가 `constants/relations.ts` 표를 소비한다. `Saju.relations` 로 나오고 화면에도
    표로 붙었다. 정책은 `engine-decisions.md` 참조.
  - **만세력 본체 보강: 2026-08-15 완료.** 공망(`sinsal/emptiness`), 12운성(`stages/`),
    12신살(`sinsal/twelveSpirits`), 핵심 신살 여덟(`sinsal/stars`). 화면에도 붙었다.
    지장간은 그 전부터 사주팔자 표에 있었다. 테스트 478건.
  - **세운 + chartId: 2026-08-15 완료.** `saeun/` · `Participant.chartId` ·
    `Relation.scope`(withinChart/betweenCharts/combinedFormation) ·
    distance·adjacent nullable · direction 을 participants 인덱스로.
    `findRelationsAmong(charts)` 가 입구이고 `findRelations` 는 한 판짜리 껍데기.
  - **월운: 2026-08-15 완료.** `wolun/`. 경계는 절입, 월간은 오호둔.
    관계는 원국+세운을 함께 놓고 보되 그 달이 낀 것만 남긴다.
  - **신살 3종·궁성·오행 %: 2026-08-15 완료.** 학당귀인(장생지)·고신살·과숙살은
    유도돼서 넣었고, 관귀학관·현침·천문·태극귀인은 산출법이 갈려 뺐다
    (`SINSAL_POLICY.omitted`). 궁성은 화면에만(계산 아님). 오행 개수 옆에
    겉글자 % 를 적어 외부 만세력과 바로 대조된다.
  - **조후용신: 2026-08-15 완료.** `analysis/johu.ts` 에 궁통보감 120칸(일간 10 ×
    월지 12) 전부. 칸마다 `stems` 우선순위와 원문 조건 요약 `note`, 출처와 전사
    오류 수정 기록. 조건을 자동 판정하지 않으므로 `status: 'reference'` 다.
  - **역마·도화·화개 재수록 + 신살 기둥별 표: 2026-08-16 완료.** 신살 목록에도
    적되 값은 12신살에서 옮겨 담기만 한다(`travelPeachCanopy:
    'restated-from-twelve-spirits'`). `Star.id` 추가(년지/일지 기준이 각각 한 항목).
  - **종격 문서화: 2026-08-16 완료**(커밋 30bc3ce). `docs/analysis/following-patterns.md`
    가 진종·가종 조건과 도입 문턱을 적는다. 그 위에 판정을 얹은 것은 아래 항목이다.
  - **종격 판정·조후 상하반월·외부 데이터셋: 2026-08-16 완료**(커밋 e36b90d~b3c7218).
    종격은 사실 층 + experimental v1 판정(축을 두 번 갈아엎었다 — `engine-decisions.md`),
    조후는 상·하반월만 자동 판정(중기 = 절기 +15° 라 천문으로 정해짐),
    억부·종격 외부 대조 데이터셋 둘.
  - **가벼운 것들도 완료**: 결과 링크 복사 버튼, 충·해·파·원진·귀문 표의 독립 기대값
    (상수를 순회하는 테스트는 표가 통째로 바뀌어도 통과한다).
  - **외부 자료 계통별 확장: 2026-08-16 완료**(커밋 da3d506). 억부 23건(계통 셋),
    종격 35건(계통 둘). 자세한 것은 `engine-decisions.md`.
  - **~~남은 것 ①: 합충으로 인한 뿌리 변화~~ — 2026-08-21 완료**(커밋 cdebe77~e7ee846).
    셋 다 했고 예상과 달리 **합화가 제일 쉬웠다**(조건이 고전에 또렷하다).
    - `transformation.ts` 합화를 化·조건부·合而不化 셋으로. **일간이 낀 합은 무게를
      안 옮긴다** — 그것은 화격이고 아직 채택 안 했다.
    - `bureau.ts` 삼합·방합·반합·**공협**을 무게 이동으로. 공협은 관계 표가 일부러
      안 내는 것이라 관계가 아니라 세력으로만 낸다.
    - `effectiveElements.ts` 바탕 분포를 지우지 않고 나란히 낸다. 옮긴 몫 0 = 원복.
    - `rootQuality.ts` 뿌리의 질(자리·역할·글자·왕지/생지/고지·충·국). `rootedness.ts`
      의 「질은 매기지 않는다」 약속을 깨지 않으려고 파일을 갈랐다.
  - **남은 것 ②**: 궁합 목적별 가중치(`purposeScores`) — 가중치를 화면에 전부
    드러낼 수 있을 때만. 보류 조건이 아직 안 풀렸으니 지금 손댈 것이 아니다.
  - **2단계 궁합: 2026-08-16 완료(엔진+화면).** `compat/` 의
    `analyzeCompatibility(a, b)` — 사이 관계(`scope !== 'withinChart'`) ·
    `combinedFormations` · 오행 보완 · 십성 양방향 · 억부 부합(딱지째 물려받음) ·
    시간 미상 경고. **점수는 안 낸다**(`COMPAT_POLICY.scoring: 'not-scored'`).
    화면은 별도 경로 `/compat`, 주소는 `a.`·`b.` 접두사 두 벌.
    일지↔일지는 엔진이 아니라 화면이 거른다(궁성과 같은 취급).
    **남은 것**: 목적별 가중치(`purposeScores`)는 가중치를 화면에 전부 드러낼 수
    있을 때만. 아래 옛 계획은 참고용이다.
  - (옛 계획) `analyze(A: Saju, B: Saju): Compatibility`
    같은 표를 두 사주에 걸쳐 돌리는 것이라 1단계의 `matchGroups`·쌍 열거를
    두 원국 합집합으로 확장하면 된다. 여기에 오행 보완·십성 관계·용신 부합을 더한다.
  출력에 `purposeScores { growth, stability, counsel }` 를 둬서 "성장하고 싶다 /
  안정형이 필요하다" 같은 목적별로 가중치를 다르게 건다.
- **판정 정확도 — 2026-08-21 완료**(커밋 cdebe77 · e26a2af · 3f6f505 · e7ee846).
  네 덩어리를 한 번에 했고 **전부 숫자를 남겼다**. 자세한 판단은 `engine-decisions.md`.
  1. **종격 v2** — 문턱은 그대로(자당 ≤30%·≥70%) 두고 **세는 법**을 고쳤다.
     재현율 14/30 → **17/30**, 오검출 1/4 유지, 같은 3000건 발화율 10.9% → **10.6%**.
     발화율을 함께 재지 않으면 재현율이 오른 이유를 구별할 수 없다.
     **게이트는 여전히 닫혀 있고 막는 것이 재현율에서 발화율·오검출로 바뀌었다.**
  2. **강약 v2** — 득세를 실효 분포에서 잰다. 억부도 같은 분포를 보게 기본값을 바꿨다
     (호출부가 잊지 않아야 맞는 기본값은 틀린 기본값이다). 억부 오행 11/20 → 10/20,
     잃은 한 칸은 출처가 「己見亥子丑」이라 적은 명조라 **세력 읽기는 오히려 같아졌다**.
     **재어 보고 안 켠 것 둘**: 투출 가산(외부 20건이 안 갈림, 모집단 1.9% 뒤집힘),
     득령·득지를 통근의 질로(외부 한 칸 좋아지고 모집단 17.6% 뒤집힘).
  3. **격국·오신·조후 후보의 실재·암합** — `structure.ts` · `favorability.ts` ·
     `johu.ts` · `hiddenRelations.ts`. 넷 다 억부를 안 뒤집는다.
     격국은 외부 대조 **0건**이라는 것을 정책에 값으로 든다.
  4. **억부 데이터셋 20 → 34** — 《적천수천미》 衰旺편 열넷. **강약 대조가 12 → 26 이
     됐고 성적 유지(25/26).** 그 장의 용신은 억부가 아니라 극왕극쇠 논리라
     `yongsinDoctrine` 필드로 나눠 센다.
  **다음 후보**: 새 판정들을 화면에 붙이기(격국·오신·암합·국·합화가 아직 엔진에만
  있다 — 고지가 좁아지는 것이 채워졌다는 증거라는 잣대로 보면 아직 안 채워졌다),
  격국 외부 대조 데이터셋, 생성기.

- **고지·시주 민감도·격국 — 2026-08-21 완료**(커밋 d958a29 · 503a016 · bf5d75f).
  「새 판정을 화면에 붙이기」를 시작하려다 **계약이 엔진을 안 따라오고 있던 것**을
  셋 연달아 찾았다. 판단은 `engine-decisions.md` 참조.
  1. `UNCOVERED_FACTS` 가 산문이라 안 늘고 있었다. `ClaimPath` 로 바꾸고 자물쇠 둘.
     고지 **6줄 → 13줄**(늘어난 것이 맞는 상태다).
  2. `HOUR_SENSITIVE_PATHS` 에 다섯이 빠져 있었다. **재서** 넣었다 — 뿌리질 64.6%,
     국 39.7%, 실효분포 31.4%, 격국 성패 26.5%, 오신 25.4%, 격국 종류 8.7%.
     **암합은 0%** 라 안 넣었다(짧아질 뿐 → 인식 규칙 셋째 줄).
     `analysis/hourSensitivity.test.ts` 가 잠근다.
  3. **격국을 화면에 세우자 엔진 버그가 나왔다.** `as StructureKind` 캐스트가
     `比肩格` 을 흘렸고 골든에 `격국 undefined` 가 찍힌 채 커밋돼 있었다.
     투출한 비겁을 격으로 잡던 것을 고치고 캐스트를 표로 바꿨다. 변종 넷
     (`self-revealed-only` 는 3.8% 라 갈랐다 — 안 갈랐으면 문장이 거짓이었다).
  **모집단을 `src/lib/saju/population.ts` 한 군데로 모았다** — 주석의 「3000건의
  몇 %」들이 서로 다른 표본이었을 수 있다.
  **다음**: 고지에 남은 것 — 암합·종격 후보. 그 밖에 stages·sinsal·tenGodCounts·
  대운 표 전체는 「표가 들 것인가 문장이 들 것인가」를 먼저 정할 일이다.

- **오신을 화면에 — 2026-08-22 완료**(커밋 3e75c54). 말뭉치 59 → **61칸**.
  `favorability.seating` 한 주제 × 한 변종 × 두 벌.
  1. **세우려니 계약이 막았다.** `FORBIDDEN_CLAIMS.unfavorable-element` 가
     '기신'·'희신'을 낱말째 막고 있었고, 그 `source` 는 **이미 없어진 정책 값**
     (`YONGSIN_POLICY.unfavorable = not-judged`)을 가리키고 있었다. 막아야 하는
     것은 **동일시**라 `ForbiddenClaim.onlyBefore: ['자리']` 로 바로 뒤 한 낱말만
     연다 — `yongsin-fixed` 가 처음부터 이 모양이었다('용신'이 아니라 '용신은').
     오신 이름을 `MYEONGRI_LEXICON` 에 **안 넣는다**: 넣으면 근거가 되고
     `insideGroundedTerm`(오미합화용 통로)이 금지를 통째로 푼다.
  2. **한신은 나머지가 아니었다.** `explain.neutral` 이 「어느 쪽에도 직접 걸리지
     않는 나머지」라고 적혀 있었는데 다섯 칸 전수로 **남는 하나는 언제나
     `GENERATES[용신]`** — 용신이 설기되는 자리다. 계산은 그대로(고전의 정의가
     「나머지」), 설명만 고쳤다. `detail` 은 아무도 안 읽어서 아무도 못 봤다.
  3. **변종 하나 — 재어 보고 안 켰다.** 자리마다 원국에 몇 자인지로 가르려다
     쟀다(3000건: 기신 24.7% · 희신 23.5% · 용신 21.6% 가 0자). 안 켰다 —
     「한 자도 없다」는 **없다는 주장**이라 배정 문장과 방향이 다르고, 세어진 수는
     행의 몫이다. 고지가 그 공백을 든다.
  `FAVORABILITY_POLICY` 를 계산 골든에 처음 찍게 했다(금지의 근거인데 어느 골든도
  안 찍고 있었다).

- **국(局)을 화면에 — 2026-08-22 완료**(커밋 4ebb421). 말뭉치 61 → **69칸**.
  주제 둘: `bureau.standing`(변종 둘) · `elements.heaviest`(변종 둘).
  1. **화면이 스스로 어긋나 있었다.** 오행 분포 표는 `analysis.elements`(글자
     그대로)를 보이는데 강약·억부·격국·종격은 전부 `effectiveElements` 에서 세력을
     잰다. 3000건에서 **무게가 움직인 명식 78.3%, 그중 최강이 갈린 것 11.2%**
     (전체의 8.7%). 표는 그대로 뒀다(외부 만세력 대조용이라 캡션이 그렇게 적음).
     대신 **두 번 셌다는 것을 문장이 말한다.** 화면을 가리키지 않는다.
  2. **변종은 관계 표에 실렸는가.** 측정이 축을 갈라 줬다 — 공협 406건 전부 관계
     목록에 없고 나머지 2570건 전부 있다. 공협만 「관계 표에는 싣지 않고 세력으로만
     셉니다」를 스스로 든다. 두 모듈이 서로를 모르므로 **모집단 시험이 그 약속을
     지킨다.**
  3. **몫을 정한 조건(등급·받침·왕지의 충)은 안 말한다** — 조합 여섯이라 접으면
     격국 성패와 같은 반올림. 결과인 `{pull}` 만 든다. 받침을 안 가른 것은 **엔진이
     월령과 투간을 같게 세기** 때문이다(갈릴 근거 없으면 안 가른다).
  4. **공협·반합·반방합 이름이 어느 그물에도 없었다** — 관계 목록에 없고
     `partialName` 이 그 자리에서 조합하니 `MYEONGRI_LEXICON` 에도 없었다.
     두 글자 형(申刑寅 → '신인형')에서 이미 겪은 자리인데 국은 빠져 있었다.
     `BUREAU_NAMES` 를 `bureau.ts` 에서 낸다(조합 규칙을 두 벌 안 적는다).
- **합화(化)를 화면에 — 2026-08-22 완료**(커밋 6e31c6f). 말뭉치 69 → **77칸**.
  `transformation.verdict` 변종 넷. 이로써 `analysis.bureaus`·`effectiveElements`
  고지는 **「몫과 등급을 정한 조건들」만** 남았다.
  1. **표가 부르는 이름과 판정이 거의 언제나 어긋난다.** 관계 표의 '을경합금'이
     「합해서 금이 됐다」로 읽히는 것이 '합화' 금지의 이유였는데, 관계 열거에는
     바로잡을 말이 없다(성립 여부를 판정 안 하기로 한 자리). 천간합 있는 명식
     45.7%, 합 1774건 중 **化는 1.9%(34건)**, 조건부 3.3%, 나머지는 묶이기만 한다.
  2. **금지를 우회하지 않고 `insideGroundedTerm` 을 쓴다.** '기신'과 달리 '합화'는
     **化한 명식에서만** 근거가 되므로 근거 대조가 실제로 일을 한다 — 새 규칙 없이
     `groundedTermsOf` 가 `TRANSFORMATION_VERDICT_KO` 를 담기만 한다. 그 한 줄을
     빼면 모집단 시험이 곧바로 깨지는 것을 확인했다. '합이불화'는 애초에 안 걸린다.
  3. **일간이 물리면 등급이 나도 무게를 안 옮긴다**(화격 판정 안 함). 판정만 보고
     변종을 고르면 옮기지도 않은 무게를 옮겼다고 적는다 — 합 1774건에 10건뿐이라
     무작위 400건이 못 밟고 지나갈 수 있어 **문장 골든에 명식 둘을 넣었다**
     (`transformation-true` 1999-10-23 · `transformation-day-master` 1990-03-09).
     골든에서 '합화'라는 낱말이 처음 서고, e2e 도 그 명식으로 한 번 본다.
  4. **化를 막은 이유(`blockers`)는 안 말한다** — 여섯 종류에 한 건당 평균 두셋
     (하나뿐인 것은 101/1679). 조건의 목록이라 접으면 반올림.

- **뿌리의 질을 화면에 — 2026-08-22 완료**(커밋 edb3c3f). 말뭉치 77 → **86칸**.
  주제 둘: `rootQuality.damaged`(presence) · `rootQuality.pulled`(absence),
  변종은 각각 충/국/둘다. 고지가 「일간 밖의 천간」만 남았다.
  1. **뿌리 표는 자리만 늘어놓는다**(「질은 매기지 않는다」가 정책이라 옳다).
     3000건: 멀쩡 27.9% · 국만 26.4% · 충만 22.2% · 충+국 10.2% · 무근 9.6% ·
     **뽑힘 3.8%**. 세 명 중 두 명꼴로 화면이 안 하던 말이다.
  2. **「根已拔」이 3.8%.** 판정은 값으로 있었는데(`effectivelyRootless`) 화면은
     「뿌리를 둡니다」만 말하고 있었다. 없다는 주장이라 시간 미상에서는 침묵
     (세 기둥 같은 표본에서 **8.7% 뒤집힘**) — 그래서 뽑힘 쪽만 조각이 한 벌.
  3. **문턱이 혼자 발화하는 일은 없다** — 깎이지 않고 뽑힌 명식 **0건**. 가장 얕은
     뿌리가 0.105 로 `EFFECTIVE_ROOT_FLOOR` 바로 위다. 변종이 원인으로 갈리는 것이
     이 사실에 기대고 있어 시험으로 잠갔다.
  4. **질의 수치는 안 든다** — 배수 다섯을 곱한 값이라 단위가 없다. 국의 `{pull}`
     («그 자리 무게의 몇 %»)과 정반대라 그 대비를 주석에 남겼다.
  5. **게이트를 세웠다가 떼어 냈다** — 무근을 막는 줄을 넣었는데 떼어 봐도 아무
     시험이 안 깨졌다(뿌리가 없으면 깎일 것도 없다). 지우고 결과만 시험이 잠근다.

- **격국 성패를 화면에 — 2026-08-22 완료**(커밋 3b6afc2). 말뭉치 86 → **90칸**.
  `structure.outcome` 변종 넷(formed/broken/mixed/none), `absence`, 한 벌씩.
  1. **「접으면 반올림」의 답은 안 말하는 것이 아니라 접지 않는 것**이었다 —
     조후가 절반을 못 고를 때 양쪽을 나란히 드는 것과 같다. 조건 이름을 슬롯으로
     그대로 든다. 3000건: 성격 39.9% · 미정(섞임) 22.5% · 파격 22.1% ·
     미정(둘 다 없음) 15.5% — **엔진의 `unresolved` 한 칸이 뜻이 정반대인 둘**이라
     갈랐다. 값을 새로 만들지 않고 **두 배열의 길이**를 읽는다(밖에서 보인다).
  2. **결론을 이름으로 안 부른다** — `STRUCTURE_OUTCOME_KO` 의 '성격'이 사주
     화면에서 사람의 성격으로 읽힌다. 골든(표)은 그대로, 산문만 조건까지.
  3. **세 번째 낡은 금지**(`structure-pattern`). 조건 이름 스물 중 둘이 금지어라
     3000건의 13% 에서 조각이 위반을 냈다. 합화와 같은 통로.
     **시험을 쓰다 막는 것이 금지 목록이 아님이 드러났다** — 근거로 들이밀면 어느
     이름이든 열리므로 실제 방벽은 `groundedScope: 'chart-produced-only'` 다.
  4. **그물에도 같은 통로가 필요했다** — `insideGroundedTerm` 이 여태 금지 목록에만
     걸려 있었고, '식상생재' 안의 '식상'이 잡히면서 드러났다. 어휘 그물에도 걸었다.
     `STRUCTURE_FACTOR_NAMES` 정적 목록도 냈다(양방향 시험이 유일한 자물쇠).

- **매칭 베타(`match-v0`) — 2026-08-21 완료**(커밋 0471ef2 · 8a3570c).
  `docs/product/matching-beta.md` 가 제품 가설과 계층을 적고, `src/lib/matching/` 이
  **엔진 밖 정책 계층**으로 지표를 낸다. 사주 엔진은 여전히 점수를 안 낸다.
  네 축(오행 보완 35% · 함께 놓은 균형 30% · 관계 신호 25% · 입력 완성도 10%),
  억부·종격·격국·조후는 `excluded` 에 이름을 남기고 뺐다 — 그것들이 바뀌어도
  match-v0 가 안 흔들려야 한다.
  `/compat?variant=A|B|C` 프로토타입 셋 중 **A(수치 대시보드)로 정했다**(사용자 결정).
  함께 정한 것: 기본 화면에 올린다 · **사실 아래에 놓는다**(e2e 로 못박음) ·
  관심 버튼은 아무것도 저장하지 않고 그렇다고 화면이 직접 말한다.
  **남은 것**: 유저 테스트로 「A를 가장 신뢰하는가」와 구매 의향을 확인하기,
  항목별 피드백 수집, `match-v1` A/B.

- **L3 해석 문장 — 미착수, 다음에 할 것.** 사전 생성 텍스트, **런타임 AI 호출 0회**.
  일간 10 × 상대 일간 10 = 100, 십성 10, 합충 20여 개로 조합이 유한하므로 빌드 타임에
  전부 생성해 JSON 으로 저장하고 런타임에는 조각을 조회·조립만 한다.
  생성 프롬프트 제약: "주어진 관계만 설명. 없는 합충 언급 금지."

  **첫 커밋은 생성기가 아니다**(2026-08-16 결론). 순서를 뒤집으면 되돌리기 어렵다.
  1. ~~**문장 계약**~~ — **2026-08-16 완료.** `src/lib/saju/text/policy.ts` ·
     `docs/text/claim-policy.md` · 테스트 25건(전체 752건). 생성기는 없다.
     자세한 판단은 `engine-decisions.md` 의 "문장 계약" 항목(`text-layer-decisions.md`).
  1-b. ~~**출처 의무 버그**~~ — **2026-08-16 완료**(커밋 bbaf256). 조각 스키마를
     설계하다 계약의 구멍을 먼저 발견해 떼어 고쳤다. `engine-decisions.md` 참조.
  2. ~~**조각 스키마**~~ — **2026-08-16 완료**(커밋 aa5ae8f). `text/fragment.ts` ·
     `docs/text/fragment-schema.md` · 테스트 21건(전체 777건). 씨앗 6개,
     지시서 41칸(`expectedFragmentKeys`). 정한 것:
     - **`paths`·`polarity` 는 조각이 아니라 주제(topic)가 적는다.** 조각이 직접
       적으면 `strength: 'fact'` 를 손으로 적는 것과 같은 구멍이 된다 —
       억부 문장이 `paths: ['pillars']` 라고 적으면 상한이 `fact` 가 된다.
     - **키는 `주제/변종@강도`.** 강도는 여전히 조각이 선언하는 속성이 아니라
       **표현 변형을 고르는 키**다. 시간 미상이면 강도가 한 칸 내려가는데 조각이
       하나뿐이면 가장 약한 강도에 맞춰 써야 해서 상한 체계가 바닥으로 무너진다.
     - **변종은 유한(빌드 타임 전수 생성), 슬롯은 데이터 주입.** 관계 이름
       `자축합토` 는 변종이 아니라 슬롯이다(`vocabulary: 'data-only'`).
     - **정적 검사 = 슬롯 비운 뼈대를 근거 없이 `checkSentence` 에 넣는 것.**
       뼈대에 명리 용어가 있으면 `ungrounded-term` 으로 걸린다. 규칙 두 번 안 쓴다.
     - **슬롯 뒤에 조사 금지**(`{element}를` 은 화·토·수에서만 맞다). 정적으로 잡는다.
     - **슬롯 값을 근거에 자동으로 넣지 않는다** — 넣으면 꽂은 값이 스스로를 근거로
       삼아 "없는 관계 검사"가 언제나 통과한다.
     - 조후 조각은 미뤘다(출처를 문장에 넣는 규칙이 하나 더 붙는다).
  3. ~~**없는 관계를 언급하면 실패하는 테스트**~~ — 2번에 함께 들어갔다.
     조각을 거쳐 돈다(`fragment.test.ts` 의 "없는 관계를 말하면 걸린다").
  4. ~~**조립기 + 문장 골든**~~ — **2026-08-16 완료**(커밋 7442e82).
     `text/assemble.ts` · `text/text.snapshot.txt` · `docs/text/assembly.md` ·
     테스트 24건(전체 801건). 정한 것:
     - **고르지 않는다**(`selection: 'all-facts-speak'`). 발화하지 않는 이유는 셋으로
       구분된다 — 사실이 없다 / 주제가 없다(`UNCOVERED_FACTS`) / 조각이 없다
       (`missingFragmentsOf`). 섞으면 "안 중요해서 뺐다"가 조용히 들어온다.
     - **`findUtterances` 는 말뭉치를 모른다** — 조각 0개여도 같은 목록. 그래야
       "말할 수 있는 것"과 "말할 조각이 있는 것"이 따로 세어진다.
     - **슬롯 표본(`samples`)은 fixture 이지 근거가 아니다** — `grounded` 로 흘리면
       "없는 관계 검사"가 무력해진다. 표본에 일부러 명리 용어를 넣어 잠갔다.
     - **문장 골든은 파일을 나눈다**(계산 골든과 변경 이유가 다르다). 시간 미상
       명식 필수 — 시주 뿌리가 사라져 무근이 되고 그 무근은 침묵하는 것이 보인다.
  5. ~~**말뭉치 1차 — 뿌리·강약·억부 14칸**~~ — **2026-08-16 완료**(커밋 c90d7ae).
     `text/corpus.ts` · `docs/text/corpus.md` · 테스트 813건. **20/41.**
     칸 수가 아니라 **사실·유도·후보·참고를 한 번씩 지나가는 가장 작은 묶음**이라
     이 셋을 골랐다 — `reference` 는 출처 의무를 강도에서 뗀 뒤 한 번도 문장이 되어
     본 적이 없던 칸이다. 정한 것은 `engine-decisions.md` 의 "말뭉치" 항목(`text-layer-decisions.md`).
  6. ~~**말뭉치 2차 — 관계 22칸**~~ — **2026-08-16 완료**(커밋 ed374c1). **41/41.**
     열한 종류에 문장은 여덟 벌이고 **해·파·원진·귀문이 하나를 나눠 쓴다** —
     엔진이 아는 것이 짝의 성립뿐이라 넷으로 가르면 없는 구별을 지어내는 것이다.
     앞 커밋의 규칙을 "갈릴 근거가 있을 때만 가른다"로 고쳤다(`engine-decisions.md`).
  7. ~~**주제 추가 1차 — 조후·종격**~~ — **2026-08-17 완료**(커밋 6c8dbf2, d160b29).
     50/50 이었다가 관계를 행으로 접으면서 **32/32** 가 됐다(아래 7-b).
     고른 순서는 "가장 작으면서 아직 한 번도 안 돈 규칙을 지나간다"였다.
     - **조후(3칸)** — `johu.table`. `ATTRIBUTION_PATHS` 의 유일한 원소를 읽는 주제가
       없어 출처 검사 분기가 프로덕션에서 죽어 있었다. 변종 셋은 원문이 그 칸을
       어떻게 말하는가로 갈린다(갈리지 않는 칸 / 상·하반월 / 절반 미판정).
       강도는 `reference` 한 벌 — 표를 여는 열쇠가 일간·월지뿐이라 시주가 안 바꾼다.
     - **종격(6칸)** — `following.verdict`. **강도가 나오는 축이 (주제, 시각)에서
       (주제, 변종, 시각)으로 넓어졌다** — `TopicSpec.silentVariants` + `speaks()`.
       `ceilingForFollowing` 은 지웠다(부르는 곳이 테스트뿐이었다). 판정 셋 × 방향
       둘인 이유와 `absence` 로 잠근 이유는 `engine-decisions.md` 의 "주제 추가"(`text-layer-decisions.md`).
     남은 주제: `analysis.elements` · `tenGodCounts` · `rootedness`(일간 밖) ·
     `stages` · `sinsal`. 전부 사실을 세기만 하는 상한 `fact` 라 새 규칙이 없다.
     대운·세운·월운은 아래 10번에서 **지금 도는 칸만** 발화하게 됐다.
  7-b. ~~**관계를 행으로 · 궁합 · 이름 입력 · 인식 규칙**~~ — **2026-08-17 완료**
     (커밋 888518d, 1064cf2, 847271e, 06d214f, e7f14d3, 9276246). **32/32**, 테스트 846건.
     사용자가 "충은 뭐고 합은 뭐 말을 바꿀 필요가 없다"고 지적해서 관계 22칸을 2칸으로
     접었고, 그 위에 궁합·이름 입력이 얹혔다. 이어서 "`fact → derived` 가 관계 자체가
     추론이라는 뜻으로 읽힌다"는 지적에서 **인식 규칙**(`INCOMPLETE_INPUT_RULE`)이
     나왔고, 그 규칙대로 관계 행을 `fact` 로 되돌리고 목록의 한계를
     `relation.coverage` 로 떼어 냈다. 판단은 전부 `engine-decisions.md` 의
     "사실은 행, 판정은 문장" · "불완전한 명식에서 어디까지 말할 수 있는가" ·
     "모르는 것을 아는 것처럼 만들지 않는다 — 폼에서도".
     - **L3 문장은 아직 화면에 안 붙어 있다.** 원국·궁합 둘 다 엔진과 골든에만 있다.
       화면에 붙이는 것은 별개 결정이라 일부러 맞춰 뒀다.
     - 궁합에서 아직 주제가 없는 사실은 `UNCOVERED_COMPAT_FACTS` 에 있다 —
       `elementSupport` · `tenGods`(양방향) · `eokbuMatch`. **셋 다 판정을 거친 것이라
       행이 아니라 문장이어야 하고**, 특히 십성은 "민수가 본 지영은 정관"처럼 누구
       눈으로 본 것인지가 문장에 들어가야 한다.
  7-c. ~~**궁합 십성 양방향**~~ — **2026-08-17 완료.** `tenGods.between` 주제,
     말뭉치 **37/37**, 테스트 852건. 로드맵이 "문장이어야 한다"고 적어 뒀는데
     **행이었다** — 십성은 두 일간을 표에서 읽은 값이라 `fact` 이고, 완충 표현이
     필요 없으면 산문일 이유가 없다(`ClaimForm`). 로드맵의 그 줄은 `ClaimForm` 이
     생기기 전에 적힌 것이라 갈라 볼 잣대가 없었다. 판단은 `engine-decisions.md`
     의 "누구 눈으로 본 것인가". 남은 궁합 사실 둘(`elementSupport`·`eokbuMatch`)은
     **여전히 문장이 맞다** — 둘 다 판정을 거친다.
  7-d. ~~**궁합 억부 부합**~~ — **2026-08-17 완료**(커밋 67053e1). `eokbuMatch.supplied`
     · `eokbuMatch.missing`, 말뭉치 **40/40**, 테스트 859건. **궁합의 첫 산문**이다 —
     그 전까지 궁합 발화 67건이 전부 `fact` 라 `hourKnown` 이 궁합에서 죽은 값이었다.
     방향이 갈려 주제가 둘이고(`presence`/`absence`), 궁합에서 침묵이 처음 보인다.
     새 규칙: **강등된 문장이 누구의 시주를 뺐는지 부른다**(`{who}`).
     남은 궁합 사실: `elementSupport` · `combinedFormations`.
  7-e. ~~**궁합 오행 보완**~~ — **2026-08-17 완료**(커밋 c1ed680). `elementSupport.absent`
     · `elementSupport.weakest`, 말뭉치 **44/44**, 테스트 864건. **행이다** — 타입이
     답을 줬다(`EokbuMatch` 에는 `status: 'experimental'` 이 있고 `ElementSupport` 에는
     없다). 새 규칙 둘: **낱말이 참인 자리에서만 선다**(argmin 이 0을 가리키면 "얇다"가
     거짓), **행도 한 칸 내려앉는다**(`rowStrength: 'not-pinned-to-fact'`).
     **궁합 사실은 이제 `combinedFormations` 만 남았고 그것은 행이 이미 발화한다.**
  9. ~~**L3 를 화면에 붙이기**~~ — **2026-08-17 완료**(커밋 c8b2788·bae53c6·8a79e3d).
     `app/utterances.tsx` 의 `UtteranceList` 가 강도를 옆 칸으로 세운다. 궁합은 손으로
     쓴 카드 셋을 **갈아 끼웠고**, 원국은 카드가 발화보다 자세해서 **요약으로 얹었다**.
     함께: 완전 삼형의 도는 순서(`Relation.cycle`). 판단은 `engine-decisions.md`.
     겹침도 정리했다(커밋 68588e7): 궁합 `주의` 카드는 **경고에 종류를 붙여**
     발화가 대신 말하는 둘만 뺀다 — 카드째 지우면 나중 경고가 조용히 사라진다.
  8. **생성기.** 런타임 줄의 다음이 아니라 **옆**이다 — 빌드 타임에 `corpus.ts` 를
     만들어 넣는 놈이라 `runtimeAi: 'none'` 이 유지되고, 나중에 LLM 으로 바꿔도 바뀌는
     것은 생산 전략뿐이다.
     **손으로 채우는 동안 규칙이 아홉 생겼다** — 강도별 표지 고정·시주 표시·공유 문장
     (41칸), 출처·판정하지 않은 조건(조후), 억부보다 뒤·판정 이름은 슬롯(종격),
     사실은 행(관계), 목록의 한계는 목록이 든다(`relation.coverage`).
     마지막 둘이 결정적인 증거다 — **관계 22칸을 손으로 다 쓰고 나서야 서술어가
     동어반복이라는 게 보였고, 행을 다 만들고 골든을 읽고 나서야 목록과 항목이 다르다는
     것이 보였다.** 먼저 자동화했다면 잘못된 모양이 22벌로 복제된 뒤에 알았을 것이다.
     남은 구멍: `checkSentence` 는 아직 `strength` 를 손으로 받는다(조각·조립 경로는
     `renderFragment` 가 계산해서 닫혀 있다).

  10. ~~**현재운 — 지금의 대운·세운·월운**~~ — **2026-08-17 완료**(커밋 bab4a14·a83e528·
     e7cd729·c851b80). 로드맵이 "가장 큰 설계"로 적어 둔 것인데 사용자가 **더 작게
     만들었다** — "몇 년 몇 월을 볼까"를 묻지 않으면 시계가 하나 더 생기는 게 아니라
     **시계 하나가 고정값으로 들어온다**. 주제 여섯, 말뭉치 51/51, 테스트 915건.
     - `now/` 의 `currentFortuneOf(saju, viewedAt)`. `Saju` 에 얹지 않고 따로 부른다
       (`analyzeCompatibility` 와 같은 자리). `Date.now()` 를 엔진 안에서 안 부른다.
     - 표는 그대로 두고 카드를 얹었다(사용자 결정). 표 = 둘러보기, 카드 = 지금.
     - 판단은 전부 `engine-decisions.md` 의 "세 번째 시계" 항목(`luck-and-third-clock.md`).
  10-b. ~~**대운 칸을 세운·월운 칸과 같은 모양으로**~~ — **2026-08-17 완료**(커밋 985a50a).
     `DaeunEntry` 가 십성·12운성·12신살·관계·`chartId` 를 든다. `DaeunInput` 이 명식
     전체를 받고(네 값이 다 `Pillars` 안에 있었다), 간지 순서는 `daeunPillarAt` 으로
     떼어냈다(甲子월이 오호둔에서 안 나와 테스트가 명식을 지어내야 했다).
     **화면이 '지금'을 한 번만 짚는다** — 세운·월운 표가 각자 절입 시각을 견주던 것을
     `CurrentFortune` 하나로 모았고, 대운 표에 '현재' 강조가 처음 생겼다.
     판단은 `engine-decisions.md` 의 "대운 칸" 항목(`luck-and-third-clock.md`).
     **남은 것**: 운끼리의 관계(`saeun × daeun` — 세운 칸이 자기를 감싼 대운을 놓는
     일이고 월운이 세운을 놓는 것과 같은 방향), 운의 12운성·12신살
     (`UNCOVERED_NOW_TOPICS`), 대운·세운·월운 **표 전체**의 발화.

**다음 세션이 고를 것**(2026-08-17 밤 기준).

1. **생성기**(위 8번) — 이제 **가장 값이 큰 것**이다. 손으로 채우는 동안 규칙이
   **열여덟** 생겼고 2026-08-17 하루에만 여덟이 늘었다(방향은 슬롯 / 낱말이 참인
   자리에서만 선다 / 문구는 화면이 쓰지 않는다 / 지우지 않고 걸러 낸다 / 계통을 고른
   값은 어느 계통인지 밝힌다 / 상대 표현은 좌표를 목록 앞에 한 번 / 명식 밖의 근거 /
   발화 없음 ≠ 침묵). **굳는 속도가 아직 안 떨어졌다**는 것이 여전히 미루는 근거다.
2. **운끼리의 관계**(`saeun × daeun`) — 세운 칸이 **자기를 감싼 대운**을 함께 놓고
   본다. 월운이 세운을 놓는 것과 같은 방향이고, 지금 그 사슬이 위에서 끊겨 있다
   (월운은 세운을 보는데 세운은 대운을 안 본다). `computeSaju` 에서 대운을 세운보다
   먼저 계산해야 하고 세운·월운 표의 관계 칸이 통째로 움직인다 — 골든이 크게 바뀐다.
   현재운이 그 공백을 문장으로 고지하고 있어(`now.coverage`) 채우면 그 문장이 또
   좁아지거나 사라진다.
3. **`verify` 에 e2e 를 넣을지** — 안 돌려서 스펙이 통째로 깨진 채 두 커밋을 지났다
   (c851b80). 넣으면 `verify` 가 느려지고(브라우저 띄운다) 안 넣으면 또 썩는다.

**열린 결정**: 대운 교체 시점을 생일로 잡은 것(정수 만 나이)과 대운수 정확값
(`startAgeExact`)으로 잡는 계통이 **다섯 달** 어긋난다. 문장이 반올림만 밝히고 생일
경계는 안 밝히므로 ① 문장에 한 마디 더 붙이거나 ② 정확값으로 교체 시점을 잡는다
(②는 `startAge` 를 쓰는 표·골든·테스트가 다 움직인다). 사용자 결정 대기.

작은 것들: 관계 표의 `두 글자` 표시가 합·형에서 뜻이 갈리는 것(지금은 한 낱말),
`combinedFormations` 를 따로 모은 목록(사실은 이미 행으로 발화한다 — 화면의 질문),
`DEFAULT_QUERY.saeunFrom = 2026` 이 하드코딩이라 해가 바뀌면 썩는다(하이드레이션
때문에 `Date.now()` 를 못 쓴다고 주석이 이미 적어 뒀다).
