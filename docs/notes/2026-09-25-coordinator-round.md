# 조율자 라운드 — CI 복구 · G-58 ㉢ · G-59 · G-23 ⑥ (2026-09-24 밤 ~ 09-25)

> 지금은: 틈의 상태는 `docs/product/gaps.md`, 사람 할 일은 이슈 #218, 일하는 법은 `docs/agents/delegation.md`.

UI 세션이 따로 도는 동안 조율자 한 세션이 워크트리 에이전트 넷을 나란히 띄웠다. 모두 main 에 직접 푸시했고
라운드 끝에 main = origin = `36824cf`, CI 초록, 워크트리 · 가지는 main 하나만 남겼다.

| 일 | 커밋 | 한 것 |
| --- | --- | --- |
| CI 복구(#219) | `9576b76` `39fb12e` `87e0538` | `check-discovery` 의 낡은 기대 둘, 머지 전 `fast` 차선에 `npm run build`, pgTAP 46 의 반출 배치 상한 |
| G-58 ㉢ | `7ede2cb` | 공유 그림 셋에 로고 · 「점점」 — 상수를 읽어 JPEG 로 굽는다(`scripts/brand-share-images.mjs`) |
| G-59 | `ef17af8` | `my_readings` 가 표지 두 일간을 준다. 인연 궁합은 동의 당시 사본. 운영 `db push` 2026-09-24 23:45 |
| G-23 ⑥ | `6fbbb3b` | `POST /api/portone/webhook` — 서명 · 금액 재조회 · `approve_reading_order`. 샌드박스 결제 전까지 안 닫힘 |
| UI(사용자) | `e8e416f` | 매칭 넘김 · 지도 보완 기운 · 관계 지도 강조 · 풀이 토글 걷기 · 지나친 인연 종이색 |
| 뒷정리 | `d1d1416` `36824cf` | 점수 검사를 첫 카드 안으로 좁힘(나란히 돌던 워크트리의 판) · 걷은 토글에 맞춘 e2e(#220) |

## 배운 것

- **화면 세션의 보고에 e2e 가 없으면 머지 전에 돌린다.** 「단위 · 타입 · 린트 · 빌드 통과」만 믿고 머지했다가 풀이 토글을
  찾는 e2e 넷이 main 을 붉혔다(#220). 화면에서 무엇을 걷었으면 그것을 찾는 시험부터 grep 한다.
- **에이전트를 띄우기 전에 나란히 도는 워크트리가 무엇을 고치는지 본다.** `discovery-checks` 워크트리가 같은 CI 실패를
  고치던 것을 모르고 맡겨 같은 일이 두 번 났다. 걷기 전에는 미커밋 차이를 main 과 견준다 — 이번에는 저쪽이 더 좁았다.
- 3000 에 사용자 dev 서버가 있을 때 e2e 는 임시 워크트리(`cp -c -R node_modules`, `.env.development.local` 은 빼고) +
  `PLAYWRIGHT_PORT` 로 돌렸다(delegation.md 함정 표 그대로).

## 남은 것 (사람의 답을 기다린다)

1. 공유 그림 셋의 머리글이 같아지고 대체 글이 `점점 — 나와 사람 사이를 이해하는 사주` 로 바뀐 것
2. ~~표지 색~~ **닫았다(2026-09-25, `a927b66` · `f5bdcfb` · `b067fec` · `0a13056`)** — 딱지는 「수정 전」(「이전 명식」은 안 쓴다), 안내 줄 「출생 정보를 수정하기 전에 만든 풀이입니다.」, 단추는 「사주풀이 다시 받기」 그대로. 수정 전 글을 보는 동안 책장 표지 · 글 표지 · 머리 딱지가 모두 풀이 당시 일간이다. 운영 `db push` 두 번(`20261023090000` · `20261024090000`). Production 배포 확인은 사람이 한다
3. 판매를 여는 날: 남길 거래 번호(`transactionId` / `pgTxId`) · 닫힌 주문에 돈이 들어올 때 `notify_ops` · 제공자 `portone` 고정
4. `fast` 차선에 build 를 더한 뒤 PR 시간을 한 번 잰다
5. G-26 · G-57 이메일은 공급자 결정(#218 의 6) 뒤. G-58 ㉡ 약관 글자는 다음 판에서
6. Production 이 `36824cf` 인지 확인하고 공유 그림 · 풀이 책장 표지를 눈으로 본다
