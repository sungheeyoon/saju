# 밤 리뷰 라운드 — 2026-09-25 밤 ~ 09-26 새벽

> 요구사항이 아니다. 운영자가 자는 동안 조율자 + 워크트리 에이전트 다섯(A 매칭 화면 · B 풀이/궁합/홈/공용 UI ·
> C 도메인 로직 · D 아키텍처 제약 · E DB 감사)이 `ff1a17b~1..3c4bca1`(커밋 111)을 시니어 리뷰로 다시 본 기록.
> 코드와 어긋나면 코드가 맞다. **운영 배포 · `db push` · 새 마이그레이션은 하지 않았다** — Production 은 여전히 `f631244`.

## 머지한 것

| PR | 영역 | 한 줄 |
|---|---|---|
| #238 | D | DB 결과를 문장으로 버리는 자리를 잠금(9곳 → 지문, 뒤에 0) · 복구기가 못 닫은 일감을 닫은 것으로 세던 버그 · `app/ui` 층 방향 잠금 |
| #239 | D | PortOne 결제 단건 조회 10초 한도 — 전에는 300초까지 붙들림 |
| #240 | C | 운영에 안 쓰는 세기 후보 표 · 안 읽는 `withheld` 걷음, 비교기 v2 행 = 운영 셈 200쌍 잠금(점수 지문 불변) |
| #241 | A | 사진 순서를 바꾸면 칸에 옛 사진이 1분 캐시되던 버그(`useOptimistic` + 서버 자리로 주소) |
| #242 | A | 매칭 모듈 순환 · 복제 셋 걷음(동작 그대로) |
| #244 | A | discovery 참여 실패 기록 · 소식에 예외 원문 노출 막음 |
| #245 · #247 · #249 · #250 · #251 | B | 시안 찌꺼기 토큰 · 궁합 결과 서버 컴포넌트화(두 명식이 화면 자료로 실리던 것) · 로그아웃 실패 시 판이 닫히던 버그 · 세션 구독 한 벌 · 그림자 · 아이콘 · 카드 · 오행 색 · 메뉴 닫기 · 줄인 움직임을 `app/ui` 로 |
| #246 | B | 풀이 뒤에서 받치는 DB 쓰기 여섯이 실패를 기록 |
| #243 | E | pgTAP 62 — 카드 점수 60갑자 대칭 · 사진 자리 섞은 200 걸음 불변식 |
| #253 · #254 · #255 | F(뒷정리) | 시안 별칭 `--card` · `--line` 걷음 · `console.error` 수를 잰 값(19곳 12파일)으로 · 계정 관리 · 가입 화면의 로그아웃 실패를 `useSignOut` 한 벌로 말함 · 풀이 뒤 사이를 바꾸면 사이 줄이 지금 글과 다음 풀이의 사이를 갈라 말함(딱지와 어긋나던 것) |
| `5d8ce58` | 조율자 | #247 이 문구를 바꾼 뒤 flow `check-reading` 이 옛 문장을 찾아 main 이 붉었다 — 고침, main verify 전 차선 초록 |

바꾼 문구: 궁합 사이 줄(사이가 갈릴 때만) 「**가족** 사이로 읽어 드립니다. …」→「지금 글과 점수는 **연인·배우자** 사이로 읽었습니다. 다음 풀이는 **가족** 사이로 읽어 드립니다. …」, 소식 「요청함을 읽지 못했습니다 — {예외 원문}」→ 우리 문장, 궁합 고르기 「점수에는 쓰지 않습니다.」→「점수의 기준도 이 답을 따릅니다.」(옛 문장은 ADR 0113 과 어긋난 거짓).

## 운영자가 정할 것

1. **DB 붉은 발견 셋(E) — 마이그레이션 초안 `2026-09-26-night-review/fix-draft.sql`, 붉은 시험 `red-findings.test.sql`.**
   ① 카드 점수 SQL 이 numeric 나눗셈 절단으로 x.5 를 내려 TS(풀이 기준점)보다 1점 낮은 쌍 — 79,800 쌍 중 100(8글자 × 6글자 쌍의 0.33%) ② `clear_my_photo` 가 계정 행을 안 잠가 올리기와 겹치면 4번 자리 하나만 남음(대표 사진 빔) ③ `remove_my_photo(2)` 두 번이 다른 장까지 지움(두 탭 · 재시도). 초안은 로컬에서 셋 다 초록 · 불일치 100 → 0. 받으면 61 의 권한 검사와 앱(`p_version` 넘김)이 함께 바뀐다. 운영에 벌어진 자리가 있는지 확인 SQL:
   `select user_id from profile_photo group by user_id having max(position) <> count(*)`
   **받음 · 고침(2026-09-26, 에이전트 G)** — #256 마이그레이션 `20261027090000_the_card_score_rounds_like_the_reading_and_a_photo_is_named_by_its_version`,
   `db push` 2026-09-26 11:04 KST(remote 목록에 섬, 발행 키 호출이 42501 — 캐시가 새 서명을 봄) · #257 앱이 `p_version` 을 넘김.
   초안과 다른 점: ① 아홉째 자리 맞추기는 TS 도 부동소수로 x.5 를 내리는 쌍에서 거꾸로 갈려(79,800 쌍 중 1), SQL 이 TS 의
   연산 순서를 `double precision` 으로 따른다 — 무작위 두 벌 79,800 쌍씩 불일치 154 · 118 → 0, 반올림 전 수까지 같은 비트.
   TS 쪽 `toFixed(9)` 방어는 기존 TS 결과를 1 · 354 쌍 바꿔 넣지 않았다. 저장된 카드 점수는 없어 다시 세울 것이 없다.
   ② 판본은 `profile_photo_version()` 한 식이고 `my_photos()` 도 그것을 부른다. 운영 집계: 벌어진 사진 자리 사용자 **0**.
2. **풀이 점수 폭** — 셋으로 나뉜다: 항목 조정 ±15(`READING_POLICY.scoreAdjustment`) · 모델 재량 ±10(`scoreDiscretion`) · 검사가 막는 최종 폭 ±25(`check.ts` 가 둘의 합). ±15 는 v1 분포(50~77)에 맞춘 값이고 v2 는 연인 36~74 · 일반 35~84. 다시 잴지 — 실호출 분포를 보기 전에는 수치를 안 바꾸는 것을 권한다(외부 리뷰 2026-09-26).
3. **`needComplementOf`(ADR 0112, 287줄)** — 시험 외 부르는 곳 없음, 점수는 `needComplementSymmetric`. 시험에서만 쓰는 것은 손대지 않는다(`delegation.md` 「죽은 코드는 셋으로 가른다」) — 걷으려면 ADR 0112 실험을 끝내는 결정이 따로 선다.
4. **남의 카드 사진 캐시 `max-age=60`** — 순서를 바꾸면 보는 쪽에 최대 1분 옛 사진. 판본 주소로 바꿀지(DB · 라우트까지).
5. 그림자를 다섯 단으로 모으며 1~4px 달라진 자리(주 단추 · 책장 표지 · 하단 독 · 궁합 고르기 원)를 눈으로 한 번.

## 안 고친 것 (까닭과 함께)

- 화면 안의 옛 DB 호출 셋(`people/page.tsx` 2 · `matching/page.tsx` 1) — 잠긴 옛 자리, 문으로 옮기는 것은 큰 일.
- 「사진 지우기」 여섯 단추의 보조기기 이름이 같다 — 승인된 문구라 둠.
- cron 비밀 비교가 `!==`(timing-safe 아님) — 실익 낮음.
- v2 카드 점수 SQL 이 한 쌍 0.44ms(v1 균형의 약 60배) — 참여자 1만이면 새로 세울 때 약 4.4초. 지금 규모는 문제 없음.
- CI 가 Docker Hub 한도(`toomanyrequests`)에 걸린 날 Supabase 가 덜 떠 authed 차선이 붉을 수 있다 — 2026-09-26 정기 실행, 재실행으로 초록.
- 흔들리는 e2e 셋: `signed-in.spec.ts:981` 사람 더하기 워커 셋에서 한 번, `match.spec.ts:805` 첫 실행 시간 초과 한 번, 사진 끌기가 하이드레이션 전 누름으로 한 번.

## 정한 것 (2026-09-26)

- 카드 점수는 TS 의 부동소수 결과를 따른다(#256) — `v2-beta` 안에서는 카드와 풀이 기준점이 같은 값을 내는 것이 계약이다. 참값 반올림으로 바꾸려면 새 판본(`v3`)으로 명시적으로 넘어간다.
- `needComplementOf` 는 둔다. 남의 사진 1분 캐시는 이번에는 둔다. 풀이 점수 폭은 실호출 분포를 본 뒤에 다시 잰다.
