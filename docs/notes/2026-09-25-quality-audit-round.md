# 품질 점검 라운드 — 잠금 시험 · 변이 · 읽기 오류 · 화면 과녁 (2026-09-25)

> 지금은: 틈의 상태는 `docs/product/gaps.md`, 무엇을 돌리는가는 `docs/agents/test-map.md`, 일하는 법은
> `docs/agents/delegation.md`.

조율자 한 세션이 품질 점검을 두 차례 돌리고, 차례마다 워크트리 에이전트를 나란히 띄워 PR 로 머지했다.
라운드 끝에 main = `aa6f0a3`.

## 한 것

| PR | 머지 | 무엇이 참이 됐나 |
| --- | --- | --- |
| #221 | `5e2c92e` | 잠금 시험이 `error` 를 안 꺼내는 DB 결과 · 타입 자리의 `import()` · 확장자 붙은 관문 · `as never` / `any as` · `@ts-expect-error` 를 잡는다 |
| #222 | `139f65c` | 변이를 견디던 네 자리 — 합쳐 이룬 궁합 · 크론 두 주소의 자격 · 수정 전 풀이의 딱지와 표지 · 시험 지도의 수 — 를 시험이 본다 |
| #223 | `7e57778` | 여러 화면이 같은 글자로 적던 모양과 「수정 전」 딱지가 상수 하나에 산다 |
| #224 | `770dee4` | 베타 일정은 `app` 의 문이 읽고 `src/lib/consent` 는 DB 를 모른다 |
| #225 | `03b27e5` | 잡은 오류의 문장은 이름이 무엇이든 화면에 안 선다 — 공유 버튼 · 프로필 사진은 우리 문장만 세운다 |
| #226 | `4dd699d` | CI 가 표준시 표를 뽑은 Node 24.12.0(tzdb 2025b)으로 돌고, 판본이 어긋나면 대조를 건너뛰지 않고 붉는다 |
| #227 | `6fb2ed5` | PRD 가 코드에 맞는다 — 한 사람 풀이 화면에는 `사주` · `사주풀이` 탭이 없다(e8e416f 가 걷었다) |
| #228 | `7398c9a` | 못 읽은 DB 결과를 「없음」으로 세우지 않는다 — `error` 를 안 꺼내던 열두 자리. 서버 페이지 함수 넷을 vitest 가 부른다 |
| #229 | `aa6f0a3` | 바탕 빛은 한 번만 깔리고, 초점 테두리는 `outline-none` 에 안 눌리며, 작던 과녁 아홉 갈래가 44px 이다. 재는 도우미 `e2e/target.ts` |

- **보고 정정.** 조율자의 보고는 둘째 차례를 「여섯 가지」라 불렀지만 PR 은 다섯(#225~#229)이다. 화면 쪽 항목들은 #229 하나로 묶였다.
- **화면 기준선 도구는 저장소에 없다.** #229 에서 전후를 견준 시각 기준선 도구는 임시(scratch) 자리에서 돌렸고 커밋하지 않았다.
  같은 것을 다시 재려면 새로 짓거나 `e2e/target.ts` 의 단언으로 대신한다.
- 뒤따른 문서 정리(ADR 0055 의 후속 머리, PRD §0.2 의 「화면 정리」 줄, 시험 지도의 수)는 이 노트와 한 PR 이다.

## 사람이 정한 것

- 점검이 낸 후속 여섯을 **전부 고친다** — 미루거나 대장에만 적지 않는다.

## 남은 것

1. **던지는 서버 액션은 Next 의 일반 영어 문장을 보일 수 있다.** 액션이 던지면 운영 빌드는 원문을 가리고 Next 의 기본 문장을
   내보낸다. 고치는 PR 이 나란히 **진행 중**이다.
2. **Vercel 의 하루 배포 한도에 두 번 걸렸다.** Production 은 2026-09-25 07:51 KST 에
   `aa6f0a3` 까지 올라갔다 — 조율자가 CLI 로 `vercel deploy --prod` 를 불렀고, 사람이 그 호출을 거절했지만
   거절이 닿기 전에 배포가 Vercel 에 이미 만들어져 끝까지 돌았다. **운영 배포는 사람이 답한 뒤에만 부르고,
   거절된 호출도 밖에 닿았는지 먼저 확인한다.** 머지가 몰리는 날은 한도를 먼저 센다.
