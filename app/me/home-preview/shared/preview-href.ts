/**
 * 미리보기 안의 링크가 **가리키는 곳만 보이고 가지는 않게** 한다.
 *
 * 가짜 사람에게는 열 화면이 없다. 실제 주소로 보내면 「사람을 찾을 수 없음」이 서고, 비교하던 시안을 잃는다.
 * `#` 뒤에 실어 두면 누른 뒤 주소창에 목적지가 보여 클릭 수를 셀 수 있고, 화면은 그대로다.
 */
export function previewHref(to: string): string {
  return `#${to}`;
}
