/**
 * **서비스 이름은 여기 한 곳에서 온다**(G-58).
 *
 * 2026-09-24 에 재어 보니 서비스 이름으로 쓰는 「만세력」이 `app/` 의 27 파일에 따로 적혀 있었다 —
 * 뿌리 메타데이터 · 머리글 · 공유 미리보기 · 화면 제목 뒤의 「— 만세력」 23 자리. 기능 이름(만세력 =
 * 명식 보기, 「만세력 엔진」)과 글자가 같아서, 이름을 바꾸는 날 어느 쪽이 서비스인지 사람이 골라야 했다.
 * 그래서 서비스 이름으로 쓰는 자리는 전부 이 상수를 읽고, 기능 이름은 글자로 남는다.
 *
 * 이름 「점점」은 점으로 만든 관계 지도 — 나를 가운데 두고 둘레에 사람이 점으로 앉는 그림 — 와 맞고, 점술의
 * 「점」도 은근히 품는다(운영자 결정 2026-09-24). 로고(`app/ui/logo.tsx`)가 같은 그림 — 가운데 큰 점(나) ·
 * 점선 궤도 · 궤도 위에서 점점 커지는 작은 점 둘 — 을 그린다.
 *
 * 처리방침 · 약관 본문의 서비스 이름은 여기서 안 바뀐다 — 고지한 판의 글자라 판을 올릴 때 사람이 고친다.
 */
export const SERVICE_NAME = '점점';

/** 이름 옆에 서는 짧은 소개 한 줄 — 뿌리 메타데이터의 제목과 미리보기 그림의 대체 글에 쓴다 */
export const SERVICE_TAGLINE = '나와 사람 사이를 이해하는 사주';

/** 화면 제목 뒤에 붙는 모양 — `app/layout.tsx` 의 제목 틀이 쓴다(`궁합 — 점점`) */
export const SERVICE_TITLE_TEMPLATE = `%s — ${SERVICE_NAME}`;
