import { READING_POLICY } from './policy';

/**
 * 현재 결과가 사람에게 하는 **말.**
 *
 * `src/lib/consent` · `src/lib/discovery` 와 같은 자리에 선다 — 정책이 문장을 들고
 * 화면은 세우기만 한다. 세 화면(자기 풀이·비공개 궁합·공유 궁합)이 같은 것을 말해야
 * 하는데 화면마다 적으면 한 곳만 고쳐지고, 그때 사용자가 읽은 약속과 실제 동작이 갈린다.
 */

/** 아직 만들지 않았을 때 */
export const READING_NONE_NOTE =
  '아직 만들어 둔 사주풀이가 없습니다. 아래 버튼을 누르면 지금 저장된 입력으로 한 번 만듭니다.';

/*
 * **「누가 썼는가」는 한 사람짜리 화면에서 내렸다.**
 *
 * 「풀이 글과 점수는 언어 모델이 씁니다」가 만드는 버튼 옆에 한 줄로 서 있었다. 버튼
 * 옆에 넉 줄이 쌓이면서 그 덩어리가 통째로 안 읽히게 됐고, 남길 하나를 고를 때
 * **무엇을 안 넘기는가**(`READING_REDACTION_NOTE`)를 골랐다 — 누를지 정하는 시점에
 * 사용자가 실제로 쓰는 사실이 그쪽이다.
 *
 * 두 층이 **한 화면에 나란히 설 때**는 여전히 적는다. 공유 결과 화면이 그렇고, 그
 * 문장은 `MATCH_RESULT_ENGINE_NOTE` 가 든다.
 */

/**
 * 새로 만들면 지금 것은 사라진다 — **누르는 그때 읽힌다.**
 *
 * 만드는 버튼 옆에 늘 적혀 있었다. 늘 서 있는 경고는 **누르지 않을 사람에게** 하는
 * 말이고, 정작 누르는 사람은 그 자리를 배경으로 읽고 지나간다. 이제 이 문장은
 * 「다시 풀이받기」를 누른 뒤 뜨는 확인 창이 든다(`panel.tsx`).
 *
 * 여기 함께 있던 「화면을 다시 열어도 이 풀이는 그대로입니다」는 화면에서 내렸다.
 * 참이지만 이 화면에서 **한 번도 틀린 적이 없는** 사실이라, 옆에 선 「이전 출생
 * 정보로 썼습니다」처럼 실제로 갈리는 한 줄까지 같이 안 읽히게 만들고 있었다.
 */
export const READING_REPLACES_NOTE =
  '새로 만들면 지금 풀이와 점수를 대신합니다. 이전 것은 남기지 않습니다.';

/**
 * 점수가 무엇인지 — **검증된 사실이 아니라 실험 결과다.**
 *
 * 내부 엔진·프롬프트·모델 판본은 말하지 않는다(`prd-archive`). 사용자가 알아야 하는 것은
 * 「이 숫자를 얼마나 믿을 것인가」이지 그것을 만든 부품의 이름이 아니다.
 */
export const READING_SCORE_NOTE = `이 점수는 실험 중인 풀이가 붙인 값입니다(${READING_POLICY.scoreRange.min}~${READING_POLICY.scoreRange.max}). 궁합의 정답이나 관계의 좋고 나쁨이 아닙니다.`;

/** 만든 뒤에 입력이 바뀌었다 — 고장이 아니라 그렇게 하기로 한 것이다 */
export const READING_STALE_NOTE =
  '이 풀이는 이전 출생 정보로 썼습니다. 지금 저장된 입력으로 보려면 새로 만들어 주세요.';

/** 공유 결과는 **매인 판본**으로 난다 — 뒤의 수정이 이 글을 흔들지 않는다 */
export const READING_PINNED_NOTE =
  '이 풀이는 두 분이 동의한 그때의 입력으로 썼습니다. 어느 쪽이 나중에 입력을 고쳐도 이 결과는 움직이지 않습니다.';

/** 지난 시도가 끝나지 못했다 */
export const READING_FAILED_NOTE =
  '지난번 만들기가 끝나지 못했습니다. 지금 보이는 풀이는 그 전에 성공한 결과입니다.';

/**
 * 두 사람 글에서 「첫 번째 분」이 누구인가.
 *
 * 공유 결과는 **양쪽이 같은 글 하나**를 읽는다(용어집: 중립 문체 하나). 그래서 글
 * 안에서 사람을 부르는 말이 보는 사람마다 뒤집힐 수 없고, 대신 화면이 자기 자리를
 * 한 줄로 알려 준다. 뒤집어 그리면 두 사람이 서로 다른 글을 읽는 것이 된다.
 */
export const readingOrderNote = (viewerIsFirst: boolean): string =>
  viewerIsFirst
    ? '아래 글에서 「첫 번째 분」이 나이고 「두 번째 분」이 상대입니다.'
    : '아래 글에서 「첫 번째 분」이 상대이고 「두 번째 분」이 나입니다.';

/** 넘기지 않은 것 — 근거 보기가 무엇을 보장하는지 한 줄로 */
export const READING_REDACTION_NOTE =
  '풀이를 만들 때 정확한 생년월일시와 출생지는 넘기지 않습니다.';

/**
 * 기다리는 동안 화면이 말해야 하는 것 — **얼마나 걸리는지.**
 *
 * 앞 문구는 「보통 1분 안에 완성됩니다」였다. 우리는 그 값을 **잰 적이 없다.** 재지
 * 않은 숫자를 화면이 약속으로 말하면, 1분이 지나는 순간 사용자는 고장 났다고 읽는다.
 *
 * 대신 **우리가 아는 값**을 말한다. 상한은 우리가 정한 것이라(`GENERATION.settings.timeout`)
 * 검증이 필요 없는 사실이고, 「길면 여기까지」는 「보통 이만큼」보다 기다림에 더 쓸모가 있다.
 * 값을 받아 쓰는 것은 상한이 바뀌는 날 문구가 조용히 거짓이 되지 않게 하려는 것이다.
 */
export const readingWaitNote = (timeoutMs: number): string =>
  `길면 ${Math.round(timeoutMs / 60_000)}분까지 걸립니다.`;

/**
 * 나가도 되는가 — **이제 정말로 된다.**
 *
 * 앞 문구는 「다만 새로고침하거나 탭을 닫으면 중단될 수 있습니다」로 닫았다. 참이었다 —
 * 생성이 누른 그 요청 안에서 돌았으므로 요청이 끊기면 만들던 것도 끊겼다.
 *
 * 만드는 일을 응답 뒤로 옮기고 나서 그 문장이 거짓이 됐다(`beginReading`). 응답은
 * 이미 나갔으니 브라우저가 끊을 것이 없고, 화면은 도는 시도를 보고 기다리는 모습으로
 * 다시 열린다. **경고를 지우는 것이 이 변경의 결과다** — 남겨 두면 하지 않아도 되는
 * 걱정을 시키고, 사용자는 그 화면 앞에 붙들려 앉아 있게 된다.
 */
export const READING_LEAVE_SAFE_NOTE =
  '이 화면을 벗어나거나 새로고침해도 계속 만들어집니다. 다시 열면 진행 상태가 그대로 보여요.';

/**
 * 이미 도는 시도가 있다 — **누른 사람에게 아무 말도 안 하던 자리.**
 *
 * 한 대상에 도는 시도는 하나다(`start_reading_run`). 끊긴 시도가 남아 있거나 공유
 * 궁합에서 상대가 먼저 눌렀으면 두 번째 누름은 아무것도 시작하지 않는데, 화면은 그것을
 * **성공으로 받아** 조용히 예전 글을 다시 세웠다. 누른 사람에게는 「눌렀는데 그대로」다.
 *
 * 실패가 아니므로 실패라고 말하지 않는다. 지금 무슨 일이 벌어지고 있는지와, 그래서
 * 무엇을 하면 되는지를 말한다.
 */
export const READING_ALREADY_RUNNING_NOTE =
  '이미 만들고 있는 풀이가 있어 그대로 기다립니다. 완성되면 이 화면에 나타납니다.';

/**
 * 남은 풀이권 — **「토큰」이라고 부르지 않는다.**
 *
 * 모델의 토큰과 같은 낱말이 되면 사용자는 글자 수를 세는 무언가로 읽고, 긴 풀이가
 * 더 비싸다고 믿게 된다. 여기서 세는 것은 **만드는 횟수** 하나뿐이다.
 *
 * 숫자만 세운다. 「5번 중 3번」은 남은 것과 준 것을 함께 보여 주므로, 사용자가 자기가
 * 얼마나 썼는지를 따로 세지 않아도 된다.
 */
export const readingCreditsLabel = ({
  limit,
  available,
}: {
  limit: number;
  available: number;
}): string => `풀이권 ${limit}번 중 ${available}번 남음`;

/**
 * 숫자 옆에 붙는 한 줄 — **없을 때가 기본이다.**
 *
 * 늘 무언가를 적어 두면 그 줄은 곧 안 읽힌다. 말할 것이 실제로 있을 때만 선다.
 *
 * ## 만들고 있을 때 먼저 말한다
 *
 * 도는 시도가 한 자리를 잡고 있어서 잔액이 하나 줄어 보인다. 그 이유를 말하지 않으면
 * 「누르지도 않았는데 하나가 사라졌다」로 읽힌다. 그리고 그 자리에서 **실패하면
 * 돌아온다**는 것도 함께 말한다 — 만드는 중인 지금이 그 걱정을 하는 시점이다.
 *
 * ## 다 썼을 때는 아무 말도 안 한다
 *
 * 「새로 만들 수는 없지만 이미 만든 풀이는 언제든 다시 볼 수 있어요」가 서 있었다. 다시
 * 보기가 닫힌 적이 한 번도 없으므로 **그 자리에서 새로 알려 주는 것이 없다** — 잔액은
 * 머리글이 이미 들고 있고, 못 누른다는 것은 닫힌 버튼이 말한다.
 *
 * 그래서 이 함수는 이제 **잔액을 묻지 않는다.** 남은 갈래가 도는 시도 하나뿐이다.
 */
export const readingCreditsNote = ({
  reserved,
}: {
  reserved: number;
}): string | null => {
  if (reserved > 0) {
    return '지금 만들고 있는 하나가 한 번을 쓰고 있어요. 실패하면 그 한 번은 다시 돌아옵니다.';
  }
  return null;
};
