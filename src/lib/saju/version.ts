/**
 * 이 엔진이 내는 **여덟 글자의 판**.
 *
 * 스냅샷은 사본이라 엔진이 바뀌면 낡는다. 막을 수는 없고 **알아볼 수는 있다** — 저장된
 * 여덟 글자 옆에 이 값을 함께 적어 두면 「지금 엔진이 낸 것인가」를 물을 수 있다
 * (ADR 0071). 판본 id 로 재던 시절에는 그 물음 자체가 불가능했다.
 *
 * **배포마다 올리는 값이 아니다.** 올리는 것은 같은 입력에서 **다른 여덟 글자**가 나올 수
 * 있게 된 때뿐이다 — 절기·자시·경도·달력 변환이 바뀐 때.
 *
 * **언제 올릴지는 시험이 묻는다.** 여덟 글자를 바꾸는 변경은 `golden.test.ts`(명식 30벌)를
 * 반드시 깨뜨린다. 그 빨간불이 곧 「판을 올려라」이고, 사람이 기억할 것이 아니다.
 *
 * 올린 뒤에는 **일괄 백필이 따라야 한다**(ADR 0071). 남의 Person 은 그 사람 세션만 고칠 수
 * 있는 것이 아니라 아무 앱 세션도 못 고친다 — RLS 가 남의 입력을 안 열어 주므로, 다시
 * 세는 일은 열쇠를 든 운영 경로에만 있다.
 */
export const CHART_ENGINE_VERSION = 'chart-2026-09-24';
