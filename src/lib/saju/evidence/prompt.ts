import { relationBlock, relationSentence, type Relation } from '../../people';
import {
  BRANCH_INFO,
  ELEMENT_KO,
  SEASON_KO,
  STEM_INFO,
  type Branch,
  type Stem,
} from '../constants';
import {
  CLAIM_STRENGTH_KO,
  CLAIM_STRENGTH_ORDER,
  type ClaimStrength,
} from '../text/policy';
import { EVIDENCE_CONTRACT, type ChartEvidence } from '.';

/**
 * 자료와 함께 넘길 **프롬프트.**
 *
 * `evidenceOf` 가 계약을 값으로 싣고 나가는 것까지는 했는데, 받는 쪽이 모델이면
 * 계약이 자료 안에 있다는 것만으로는 지켜지지 않는다. **모델은 읽으라고 하지 않으면
 * 읽지 않고, 읽어도 따르라고 하지 않으면 따르지 않는다.** 그 한 겹이 여기다.
 *
 * ## 상한은 여기서 입을 막지 않는다
 *
 * 처음에 이 파일은 L3 문장 계약을 그대로 모델에게 옮겼다 — 점수 금지, 길흉 금지,
 * 후보는 확정 금지. **그러면 넘길 이유가 없다.** 그 규율은 *우리가 문장을 낼 때*의
 * 것이고, 우리 문장은 이미 `corpus.ts` 가 계약에 걸려서 낸다. 자료를 밖으로 넘기는
 * 까닭은 우리가 안 하는 것을 시켜 보려는 것이다.
 *
 * 그래서 사다리의 구실이 바뀐다. **말하지 말라는 눈금이 아니라 어느 층에서 한 말인지
 * 붙이는 딱지다.** 근거가 단단한 것은 단정하고, 얕은 것은 얕다고 밝히고 끝까지 간다.
 * 층을 밝히지 않은 채 다 단정하는 것과, 층에 막혀 아무 말도 못 하는 것 사이가 이
 * 파일이 겨누는 자리다.
 *
 * 딱 하나만 여전히 금지한다 — **없는 사실을 지어내는 것.** 이것은 조심성의 문제가
 * 아니라 그냥 틀린 것이다. 자료에 없는 글자를 있다고 하면 그 위의 해석은 전부 남의
 * 명식 이야기가 된다.
 *
 * ## 왜 다섯인가
 *
 * 셋은 **해석을 시키는 것**이고(`reading`·`now`·`compat`), 하나는 **옛 계약 그대로
 * 조이는 것**이고(`strict`), 하나는 **나온 글을 되짚는 것**이다(`audit`). 넷째가
 * 남아 있는 것은 견줄 짝이 있어야 실험이기 때문이다 — 조인 것과 푼 것을 같은 자료로
 * 돌려 봐야 무엇이 상한 덕이고 무엇이 모델 덕인지 갈린다.
 *
 * ## 계약을 되풀이하지 않고 가리킨다
 *
 * 사다리도 규칙 묶음 이름도 손으로 타이핑하지 않는다. `CLAIM_STRENGTH_ORDER` 와
 * `EVIDENCE_CONTRACT` 에서 지어서, 사다리에 칸이 하나 생기면 프롬프트가 저절로
 * 따라오고 그 칸에 말투를 안 적었으면 **타입이 먼저 빨개진다**.
 */

export type PromptKind = 'reading' | 'now' | 'compat' | 'strict' | 'audit';

export const PROMPT_POLICY = {
  ruleSet: 'evidence-prompt-v1',
  /** 계약을 되풀이하지 않고 자료 안의 자리를 가리킨다 */
  contract: 'pointed-at-not-restated',
  /** 사다리는 손으로 적지 않는다 — 계약에서 짓는다 */
  ladder: 'generated-from-claim-strength-order',
  /**
   * 상한의 구실이 프롬프트마다 다르다.
   *
   * 해석용은 **딱지**로 쓰고 엄격용은 **눈금**으로 쓴다. 같은 사다리를 두 가지로
   * 쓰는 것이 이 파일의 요점이라 값으로 적는다 — 하나로 합치면 둘 중 하나가 사라진다.
   */
  ceiling: 'label-in-reading-limit-in-strict',
  /** 지어내는 것만 막는다 — 조심성이 아니라 참·거짓의 문제다 */
  hardRule: 'no-invented-facts',
  /** 자료는 들여쓰지 않고 붙인다 — 붙여 넣는 자리에서 들여쓰기는 값이 아니라 무게다 */
  payload: 'minified-json',
  /** 문장마다 층과 근거를 달게 한다 — 어디서 온 말인지가 눈으로 보여야 실험이 된다 */
  output: 'every-sentence-carries-tier-and-path',
} as const;

/**
 * 사다리 칸마다 **어떻게 말할 것인가** — 해석용.
 *
 * 어느 칸에서도 「말하지 마라」가 없다. 가장 낮은 칸조차 말할 길을 준다 — 다만
 * 그것이 자료에서 온 것이 아님을 먼저 밝히게 한다.
 */
const TIER_RULE: Record<ClaimStrength, string> = {
  fact:
    '자료가 그대로 말한다. **적힌 사실 자체는 단정해서 써라** — 「일간은 庚이다」를 흐릴 이유가 없다. 그 사실에서 해석으로 넘어간 결론의 강도는 아래 규칙을 따른다.',
  derived:
    '세어 보면 나온다. 「…로 세면 …이다」로 쓰고, 세는 법이 달라지면 값도 달라진다는 것을 한 번은 밝혀라.',
  candidate:
    '갈래가 여럿인데 이 엔진은 하나만 봤다. 「…쪽으로 본다면」으로 열되 **끝까지 해석해라.** 여기서 멈추면 넘긴 보람이 없다.',
  reference:
    '고전 표에서 옮긴 것이다. 표가 뭐라 적었는지 밝히고 그 위에 네 읽기를 이어 붙여라 — 표와 네 읽기가 구별되게만 써라.',
  silent:
    '자료가 이 방향으로는 아무것도 못 준다(대개 시주를 몰라서다). 그래도 말하고 싶으면 **「자료가 뒷받침하지 않는 추측」이라고 먼저 적고** 말해라.',
};

/**
 * 같은 사다리를 **눈금으로** 쓸 때 — 엄격용.
 *
 * 우리 L3 가 문장을 낼 때 지키는 것과 같은 규율이다. 견줄 짝으로 남겨 둔다.
 */
const CEILING_RULE: Record<ClaimStrength, string> = {
  fact: '단정해도 된다. 「…이다」로 쓴다.',
  derived: '무엇을 어떻게 세어 나온 값인지 함께 적는다. 「…로 세면 …이다」.',
  candidate: '「후보」라는 말을 반드시 넣는다. 확정하지 마라.',
  reference: '「고전 표에는 …라고 적혀 있다」까지만. 그 표가 이 명식에 맞는다고 말하지 마라.',
  silent: '그 방향으로는 한 글자도 쓰지 마라.',
};

/** 사다리를 프롬프트에 적을 꼴로 — 센 것부터 내려온다 */
const ladderLines = (rule: Record<ClaimStrength, string>): string =>
  [...CLAIM_STRENGTH_ORDER]
    .reverse()
    .map(
      (strength) =>
        `- \`${strength}\` ${CLAIM_STRENGTH_KO[strength]} — ${rule[strength]}`,
    )
    .join('\n');

/**
 * 해석용 프롬프트에 늘 붙는 머리.
 *
 * 자료가 무엇인지, 무엇이 딱 하나 금지인지, 층을 어떻게 달지 — 셋뿐이다. 짧은 것이
 * 목적이다. 규칙이 길면 모델이 규칙 지키기에 지면을 다 쓰고 해석을 안 한다.
 */
const readingRules = (): string => `## 이 자료가 무엇인가

만세력 엔진이 낸 **사실과 계산의 목록**이다(\`contract\`, ${EVIDENCE_CONTRACT.version}). 해석은 하나도 들어 있지 않다(\`contract.interpretation: "none"\`) — 그 자리를 채우라고 너에게 넘긴다.

관계의 참여자마다 \`chartId\` 가 있다: \`natal\`(원국)·\`decade:n\`(대운)·\`annual:연도\`(세운)·\`monthly:…\`(월운). 두 사람이면 \`natal:a\`·\`natal:b\` 로 갈린다. 운은 지금 도는 칸만 실리고 기준 시각은 \`viewedAt\` 이다.

**어느 칸의 관계인지 섞지 마라.** \`natal\` 의 관계는 타고난 구조이고 \`decade\`·\`annual\`·\`monthly\` 의 관계는 그 시기에 **새로 걸린 것**이다. 원국에 있던 충을 「올해 생긴 일」로 옮겨 적으면 그 문장은 해마다 참이 되어 아무것도 말하지 않고, 반대로 올해 걸린 것을 타고난 성향으로 적으면 내년에 거짓이 된다.

**운은 원국을 다시 쓰지 않는다.** 운에서 새로 걸린 관계는 그 시기의 조건으로만 쓰고, 원국의 무엇과 만나 무엇을 드러내거나 누르는지로 이어라.

## 사실에 관한 단 하나의 금지

**없는 것을 지어내지 마라.** 자료에 없는 글자·관계·운을 있다고 하면, 그 위에 쌓은 해석은 전부 남의 명식 이야기가 된다. 이건 조심성의 문제가 아니라 참·거짓의 문제다. 자료가 안 주는 것을 말해야겠으면 「자료 밖」이라고 먼저 적어라.

그 밖에는 **막지 않는다.** 길흉도, 시기도, 조언도, 성격도 말해도 된다. 이 엔진이 스스로는 안 하는 것들이고, 그것을 시켜 보려고 넘기는 것이다.

여기서 「하나」는 **말할 수 있는 것의 경계**를 말한다. 아래의 말투·순서·출력 규칙은 무엇을 말해도 되는지가 아니라 **어떻게 쓸 것인지**라, 이것과 겨루지 않는다. 둘 다 지켜라.

## 근거의 층 — 입을 막는 눈금이 아니라 딱지다

값마다 \`claims\` 표에 상한이 적혀 있다(사람마다 한 벌씩, 「있다(\`presence\`)」와 「없다(\`absence\`)」가 따로). **전부 해석하되 어느 층에서 한 말인지 밝혀라.**

${ladderLines(TIER_RULE)}

두 방향이 갈리는 까닭은 시각을 모를 때 「없다」가 먼저 무너지기 때문이다 — 여덟 글자 중 둘이 빠졌는데 「金이 없다」고 하면 그냥 거짓이다.

**얕은 근거에서 멈추지 마라.** 얕으면 얕다고 적고 **그 위에서 계속 읽어라.** 「후보라서 말할 수 없다」는 이 자리에서 쓸 답이 아니다 — 그 말을 하라고 넘기는 것이 아니다.

다만 층이 얕은 자리에서는 **말투로 그것이 드러나야 한다.** 단단한 것은 「…이다」로, 얕은 것은 「…쪽으로 보인다」·「…라면 이렇게 읽힌다」로 쓴다.

**층은 해석 결론의 천장이다.** 자료에 적힌 사실 자체는 그대로 단정해 쓴다 — 거기에는 흐릴 것이 없다. 천장이 걸리는 것은 그 사실에서 **넘어간 결론**이고, 천장 아래에서 얼마나 세게 쓸지는 **근거가 몇 갈래로 모이는가**가 정한다. 층이 \`사실\` 이어도 결론을 받치는 근거가 하나뿐이면 단정하지 않는다. 둘이 다른 답을 가리키면 **낮은 쪽**을 따른다. 딱지(\`[사실 · analysis.elements]\` 같은 것)는 **본문에 달지 말고 맨 끝 근거 칸에 모은다** — 문장마다 붙으면 글이 죽는다.`;

/**
 * 말투와 뼈대 — **이것이 없으면 모델은 논문을 쓴다.**
 *
 * 프롬프트가 자료의 필드 이름을 가리키다 보니, 처음 나온 글이 「월지 亥수 정관의
 * 본기가 투출하여 형성된 정관격에 월간 辛금 편재가 관을 생하고」였다. 틀린 말은
 * 하나도 없었고 딱지도 제대로 달려 있었는데, 읽고 나면 **「그래서 뭔데?」**가 남았다.
 *
 * 두 가지가 빠져 있었다.
 *
 * 1. **읽는 사람이 사주를 모른다는 것.** 정관격·쟁합·원진은 처음 듣는 말이고,
 *    한자 괄호를 달아도 푼 것이 아니다.
 * 2. **「그래서」.** 사실을 적고 끝내면 그것은 자료를 우리말로 옮겨 적은 것이지
 *    해석이 아니다. 자료는 이미 그 사실을 들고 있다.
 *
 * 딱지도 옮긴다. 문장마다 `[사실 · charts.a.pillars]` 가 붙으면 글이 죽는데, 딱지를
 * 없앨 수는 없다 — 어디서 온 말인지가 이 실험의 절반이다. 그래서 **본문에서 빼고 맨
 * 끝에 모은다.** 검사는 그대로 되고 읽는 사람은 글을 읽는다.
 */
/**
 * **문장을 어떻게 닫을 것인가** — 말투를 한 벌로 못박는 자리.
 *
 * 앞의 규칙은 「`~해요`·`~있어요`·`~입니다`를 자연스럽게 섞어 리듬을 바꾼다」였다.
 * 시킨 대로 나왔고, 그래서 나온 글이 이랬다.
 *
 * > 느낄 수 있습니다. … 따라와요. … 받을 때도 있습니다. … 쉬워요.
 *
 * 리듬이 아니라 **여러 사람이 이어 쓴 것처럼** 읽힌다. 사람은 한 자리에서 말할 때
 * 말투를 바꾸지 않는다 — 바뀌면 듣는 쪽은 말하는 사람이 바뀐 줄 안다.
 *
 * 그렇다고 모든 문장을 `~해요`로 닫는 것이 통일은 아니다. 해요체 안에도 어미가
 * 여럿이고, 리듬은 **그 안에서** 만든다.
 *
 * 두 프롬프트 계열이 같은 것을 쓴다. 따로 적으면 우리가 부르는 글과 사용자가 복사해
 * 가는 글이 다른 말투로 나가고, 그때 어느 쪽이 이 제품의 목소리인지 알 수 없다.
 */
const REGISTER = `## 문장을 어떻게 닫을 것인가

**해요체 하나로 쓴다.** \`~습니다\`·\`~입니다\`와 섞지 마라 — 두 말투가 한 글에 섞이면 여러 사람이 이어 쓴 것처럼 읽히고, 눈앞에서 말해 주는 느낌이 사라진다.

해요체 안에서도 어미는 여럿이다. \`~해요\`·\`~이에요\`·\`~거예요\`·\`~더라고요\`·\`~죠\`·\`~볼까요\`를 섞어 리듬을 바꾼다. **모든 문장을 \`~해요\`로 닫는 것이 통일이 아니다.**

\`~한다\`·\`~이다\`·\`~간다\`·\`~하라\` 같은 해라체와 보고서 말투는 본문에 쓰지 않는다.`;

const VOICE = `## 어떻게 말할 것인가

읽는 사람은 **사주를 모른다.** 정관격도 쟁합도 원진도 처음 듣는 말이다. 이 글을 읽고 「그래서 뭔데?」가 남으면 실패다.

1. **용어는 그 자리에서 풀어라.** 「정관격」이라 쓸 거면 바로 뒤에 「정해진 틀과 규칙이 중심에 있는 구조」라고 붙여라. 못 풀 말은 아예 쓰지 마라. **한자를 괄호에 넣는 것은 푼 것이 아니다.**
2. **비유는 처음 한 번만.** 「한겨울 바다 위의 촛불」로 그림을 그렸으면 그다음부터는 실제 이야기로 가라. 비유를 되풀이하면 아무 말도 안 한 글이 된다.
3. **문단마다 「그래서」로 닫아라.** 이런 사실이 있다 → 그것이 이 사람 하루에 어떻게 나타나는가 → 그래서 무엇을 하면 되는가. 셋째 칸이 없는 문단은 지워라.
4. **조언은 손에 잡히게.** 「유연함이 핵심입니다」·「조율이 필요합니다」·「주의 깊게 살피세요」는 아무 말도 아니다. **언제, 어떤 상황에서, 무엇을 하라**고 적어라.
5. **숫자를 문장에 흩뿌리지 마라.** 「金이 33.12%」는 읽는 사람에게 아무 뜻이 없다. 「여덟 글자 중 셋이 쇠(金)다」처럼 셀 수 있는 말로 바꿔라.
6. **딱지를 본문에 달지 마라.** 맨 끝에 따로 모은다. 본문은 사람이 읽는 글이다.
7. 약점을 성격 흠으로 적지 마라. **어떤 조건에서 그렇게 되는지**와 **그때 어떻게 하는지**로 적어라.
8. **살(殺)은 저주가 아니라 조건이다.** 고신살·백호살·귀문관살처럼 이름이 무서운 것으로 겁주지 마라. 어떤 자리에서 그 모양이 나오고 **그때 무엇을 하면 되는지**로 적어라. 반대로 귀인이 있다고 다 잘된다고도 하지 마라.
9. **오행이 어느 시간대·방위·색·계절에 붙는지는 이 자료에 없다.** 널리 쓰이는 대응(木-동쪽-아침-푸름 …)에서 가져다 써도 되지만, 그 말을 쓴 **절의 근거 줄에** \`자료 밖\` 이라고 적어라(근거 칸은 절마다 한 줄이다). 가져다 쓰는 것은 되고 자료가 준 것처럼 말하는 것은 안 된다.`;


/**
 * 성격을 읽는 순서 — **신살이 성격을 정하지 않는다.**
 *
 * 처음 나온 글이 「천을귀인·문창귀인·학당귀인을 깔고 앉아 두뇌 회전이 빠르고 학습
 * 능력이 탁월합니다」였다. 딱지는 `[참고 · sinsal.stars]` 로 제대로 달려 있었는데,
 * 그 문장을 쓴 것은 자료가 아니라 **신살 이름**이다. 같은 신살을 가진 사람이
 * 수천만이고, 그래서 그 문장은 누구에게 붙여도 맞는다 — 뻔한 말은 여기서 나온다.
 *
 * 순서를 못박으면 오히려 반대가 된다. 앞의 여섯을 먼저 세우고 신살을 색으로만 쓰면
 * 그 사람에게만 맞는 이야기가 남고, **신살이 구조와 다른 방향을 가리키는 자리**에서는
 * 긴장이 드러난다. 그 긴장이 가장 그 사람다운 대목이다.
 *
 * 완충 표현에는 함정이 하나 있다. 근거가 하나면 「~경향이 있다」로 쓰라는 규칙이
 * **애매하게 말해도 된다는 허가**로 읽히면 글이 물러진다. 그래서 한 줄을 더 단다 —
 * 완충한 문장도 무엇이 어떤 상황에서 어떻게 나타나는지는 그대로 구체적이어야 하고,
 * 근거가 하나뿐이면 그 하나를 밝혀야 한다. 밝히면 완충한 문장이 오히려 더 말한다.
 *
 * 이 순서는 `claims` 의 상한과 **다른 축**이다. 상한은 「이 근거로 얼마나 세게 말해도
 * 되는가」이고 이것은 「같은 층 안에서 무엇을 먼저 보는가」다. 섞이면 둘 다 흐려진다.
 */
const PERSONALITY = `## 성격을 읽는 순서

**신살이 성격을 정하지 않는다.** 「천을귀인이 있어 머리가 좋습니다」 같은 문장은 자료가 아니라 신살 이름이 쓴 것이고, 같은 신살을 가진 사람이 수천만이다. 누구에게 붙여도 맞는 말이 거기서 나온다.

읽는 순서는 이렇다. **위가 먼저다.**

1. 일간과 월령 — 무엇이 어느 계절에 났는가
2. 일간의 강약과 통근 — 기댈 데가 있는가
3. 오행의 분포 — 무엇이 넘치고 무엇이 없는가
4. 십성의 구성과 강약 — 무엇이 이 사람을 움직이는가
5. 천간·지지의 구조와 형충회합 — 어디가 부딪히는가
6. 격국·용신 같은 별도 체계
7. 신살과 길성

앞의 것들로 성향을 **먼저 세운 뒤**, 신살은 거기에 색을 입히는 데만 쓴다.

- **신살 하나로 성격을 단정하지 않는다.** 길성도 마찬가지다 — 귀인이 있다고 잘났다고 쓰지 마라.
- **같은 신살이 여러 자리에 걸렸다고 그 성향이 몇 배가 되지 않는다.** 관계를 개수로 합산하지 않는 것과 같다.
- 신살이 앞의 구조와 **같은 방향**을 가리키면 「보강된다」·「두드러진다」까지 쓸 수 있다.
- **다른 방향**을 가리키면 신살로 앞을 뒤집지 말고, 둘이 함께 있는 **긴장**으로 읽어라. 이 자리에서 오히려 그 사람다운 이야기가 나온다.

성격은 세 층으로 나눠 쓴다.

- **기본 기질** — 일간·월령·강약
- **구체적 성향** — 오행·십성·구조
- **색깔** — 신살·길성

## 얼마나 세게 말할까

**층이 정한 천장 아래에서** 해석 결론의 강도를 정하는 것이 이것이다. 「~이다」라고 단정하는 것은 **서로 다른 근거 여럿이 같은 방향을 가리킬 때**뿐이다. 근거가 하나면 「~한 경향이 있다」·「~로 나타날 수 있다」로 쓴다. 층이 그보다 낮으면 층을 따른다 — 둘 중 **낮은 쪽**이 답이다.

다만 **완충은 불확실함의 표시이지 애매하게 말해도 된다는 허가가 아니다.** 「~경향이 있다」로 쓰더라도 무엇이 어떤 상황에서 어떻게 나타나는지는 그대로 구체적이어야 한다. 근거가 하나뿐이면 **그 하나가 무엇인지 밝혀라** — 밝히면 완충한 문장이 오히려 더 많은 것을 말한다.

이 순서는 \`claims\` 의 상한과 **다른 축**이다. 상한은 「이 근거로 얼마나 세게 말해도 되는가」이고 이것은 「같은 층 안에서 무엇을 먼저 보는가」다. 둘 다 지켜라.`;

/**
 * 해석용 셋이 공통으로 닫는 자리 — 딱지가 여기로 모인다.
 *
 * ## 경로만 적게 하면 **어디를 봤는지**만 남는다
 *
 * 앞판의 본보기는 `강점 — analysis.strength [유도]` 였다. 경로와 그 경로의 층뿐이다.
 * 실제로 나온 글이 그대로 그랬다 — 열세 줄 중 열이 `[사실]` 이었다. 해석 글에서는
 * 나올 수 없는 분포다.
 *
 * 딱지가 틀린 것이 아니다. `compatibility.tenGods` 는 정말로 사실이다. 틀린 것은
 * **그 딱지가 무엇에 붙었는가**다. 사다리는 「이 값을 얼마나 세게 말해도 되는가」인데
 * 모델은 그것을 **출처**에 달았고, 정작 문장이 주장한 것(「연락에서 답을 늦춘다」)은
 * 아무 딱지도 안 달고 지나갔다. 프롬프트가 스스로 「층은 해석 결론의 천장이다」라고
 * 적어 두고도 형식이 그것을 못 담았다.
 *
 * **가르쳐 준 예시가 곧 기본값이 된다.** 그래서 형식을 고친다.
 *
 * ## 새 축을 만들지 않고 **빈칸**을 만든다
 *
 * 결론에 붙일 사다리를 하나 더 세우고 싶었지만 그러면 축이 둘이 되고, 이 저장소가
 * 되풀이해 피한 것이 그것이다(`claims` 의 상한과 읽는 순서를 섞지 않는 것과 같다).
 *
 * 대신 **「넘어간 것」** 칸을 연다. 자료에서 결론까지 사이에 무엇을 더했는지 제 말로
 * 적게 하는 자리다. 새 눈금이 아니라 빈칸이라 사다리와 겨루지 않고, 비어 있으면 그
 * 문장은 자료를 옮긴 것이며 길면 그만큼 자료에서 멀다 — **검수하는 사람이 어디를 볼지
 * 그 칸의 길이가 알려 준다.**
 *
 * 관계에 이름을 대게 하는 것도 같은 까닭이다. 실제 출력에서 「부딪히는 지점」은
 * 관계를 이름으로 열거했는데(정계충·묘유충…) 「생활에서 반복될 장면」은
 * `compatibility.relations` 라고만 적었다. **이름을 못 대는 절은 그 관계에서 나온
 * 결론이 아닐 가능성이 크다.** 그 차이가 눈에 보이게 이름을 강제한다.
 */
const CLOSING = `### 근거 (검사용)

본문 절마다 한 줄씩. **세 칸이다.**

\`절 이름 — 결론 「…」 | 자료: 경로 [층] · 경로 [층] | 넘어간 것: …\`

- **결론** — 그 절이 **실제로 주장한 것** 한 마디. 절 제목을 되풀이하지 마라.
- **자료** — 그 결론을 받친 경로와 각 경로의 층. **관계를 들 때는 이름을 댄다** —
  \`compatibility.relations\` 가 아니라 \`relations 묘유충\` 이다. 이름을 못 대겠으면
  그것은 그 관계에서 나온 결론이 아니다.
- **넘어간 것** — 자료에서 결론까지 **사이에 무엇을 더했는가.** 자료를 우리말로 옮기기만
  했으면 \`없음\`. 통설·비유·생활 장면·조언을 얹었으면 그것을 적고, 자료에 아예 없는
  것에서 가져왔으면 \`자료 밖\` 이라고 함께 적어라.

**층은 자료 칸에만 단다.** 사다리는 「그 값을 얼마나 세게 말해도 되는가」이지 「내 결론이
얼마나 단단한가」가 아니다. 결론의 단단함은 **자료 칸의 가장 낮은 층**과 **넘어간 것 칸의
길이**가 함께 말한다.

\`넘어간 것\` 을 \`없음\` 으로 채우고 싶은 유혹을 이겨라. 절마다 \`없음\` 이면 그 글은
해석이 아니라 자료를 우리말로 옮긴 것이고, **그건 자료가 이미 하고 있다.** 반대로 모든
칸이 길면 자료를 거의 안 쓴 것이다. 어느 쪽이든 그 자리가 고칠 자리다.

본보기 둘 — 하나는 넘어간 것이 없고 하나는 있다.

\`오행의 치우침 — 결론 「토가 한 글자도 없다」 | 자료: analysis.elements.missing [사실] | 넘어간 것: 없음\`

\`생활에서 반복될 장면 — 결론 「연락 속도가 어긋나 서로를 오해한다」 | 자료: compatibility.tenGods.aSeesB 편관 [사실] · relations 묘유충 [사실] | 넘어간 것: 편관·편재를 「연락 습관」이라는 장면으로 옮긴 것 · 「밤에 연락하겠다」는 조언은 자료 밖\`

마지막에 **가장 흔들리는 것 셋**을 덧붙인다 — 무엇을 더 알면 단단해지는지까지.
\`contract.excluded\` 와 \`limitations\` 도 여기서 읽는다.

**이 절은 사용자에게 안 나간다**(화면이 \`### 근거\` 앞에서 끊는다). 그래서 여기 적는
것은 검수하는 사람에게 하는 말이고, 여기 적었다는 이유로 본문이 조용해지면 안 된다 —
읽는 사람이 **알아야 하는** 한계는 본문 규칙이 정한 대로 그 판단 옆에서 말한다.`;

const READING = `# 역할

너는 이 사람의 사주를 **끝까지 읽어 주는 사람**이다. 자료를 우리말로 옮겨 적지 마라 — 그건 자료가 이미 하고 있다.

${readingRules()}

${VOICE}

${REGISTER}

${PERSONALITY}

## 낼 것

열한 개 절. 소제목을 그대로 쓴다.

**1. 한 줄로** — 이 사람이 어떤 사람인지 한 문장. 비유를 쓸 거면 여기서만.

**2. 타고난 결** — 어떤 기질이고 그게 하루에 어떻게 나오는지. 너덧 문장.

**3. 잘하는 것 다섯** — 줄마다 「무엇을 잘한다 → 왜 그런가 → 어디에 쓰면 되는가」. **다섯을 채워라.** 셋에서 멈추면 뻔한 것만 남는다.

**4. 걸리는 것 넷** — 줄마다 「무엇이 문제다 → 언제 그렇게 되는가 → 그때 어떻게 하는가」.

**5. 일과 돈** — 어떤 판에서 힘이 나고 어떤 판에서 빠지는지. **직업 이름을 못박지 말고 조건으로 말해라** — 「엔지니어」가 아니라 「정해진 규칙 안에서 남이 못 찾는 오류를 찾는 일」이다. 돈이 들어오는 결(몸을 써서인지, 아는 것을 팔아서인지, 남의 판을 빌려서인지)도 함께.

**6. 사람 관계** — 되풀이되는 모양 하나, 어떤 사람과 편하고 어떤 사람이 힘든지, 그리고 그걸 아는 것만으로 달라지는 것 하나.

**7. 몸** — 어느 쪽이 먼저 지치는지와, 그래서 무엇을 먼저 챙길지. 병명을 진단하지 마라.

**8. 살림법 — 무엇을 늘리고 무엇을 줄일까**

**이 절이 이 글에서 가장 손에 잡히는 자리다.** 「~하면 좋습니다」로 끝내지 말고 **오늘부터 할 수 있는 것**을 적어라.

- **늘릴 기운 하나** — 억부 후보(\`analysis.eokbu\`)와 없는 오행(\`analysis.elements.missing\`), 조후(\`analysis.johu\`)를 함께 보고 고른다. 그 기운을 **일상에서 늘리는 법**을 서너 개. 시간대·장소·움직임·공부·물건·습관 중에서 고른다. 예를 들어 나무(木) 기운이면 「새로 배우고 배운 것을 적어 두는 일」이 그 기운을 쓰는 것이고, 불(火) 기운이면 「낮에 움직이고 사람 앞에 서는 일」이 그렇다.
- **줄일 것 하나** — 무엇을 덜어야 하는지와, 덜기 어려운 상황이 언제인지.
- **신살에서 나오는 살림법** — \`charts.a.sinsal.stars\` 와 \`twelveSpirits\` 를 읽어라. 역마가 걸렸으면 한자리에 묶이는 일보다 **움직임을 일의 일부로 만드는 것**이 낫고, 고신·과숙이면 혼자 있는 시간이 필요한 사람이라 억지로 어울리는 자리를 늘리면 오히려 닳는다. 화개면 파고드는 시간을 일과 안에 두는 것이 낫다. **걸린 것만 말해라** — 없는 신살을 끌어오지 마라.

**9. 지금** — 기준 시각을 밝히고, 지금 도는 대운·세운·월운이 무슨 때인지. **이번 달과 올해에 무엇을 밀어붙이고 무엇을 미루면 좋은지**까지. 「문서를 조심하라」 같은 말을 쓸 거면 왜 그런지 한 줄로 붙여라.

**10. 자주 묻는 것** — 사람들이 실제로 들고 오는 물음 중 **이 자료로 답할 수 있는 것** 넷을 골라 물음-답 꼴로. 두세 문장씩.

고를 만한 것: 지금이 움직일 때인가 기다릴 때인가 · 어떤 사람이 잘 맞나 · 옮기거나 혼자 해도 되나 · 공부나 자격이 도움이 되나 · 돈은 어떻게 모이나 · 이사나 먼 곳이 맞나 · 무엇을 먼저 고쳐야 하나.

**자료가 못 주는 물음은 골라 놓고 얼버무리지 말고**, 「이 자료로는 거기까지 못 간다」고 적은 뒤 **대신 볼 수 있는 것**을 말해라. 결혼·출산 같은 특정 사건의 날짜는 이 자료로 못 짚는다.

**11. 한 줄로 닫기**

그다음에 줄을 긋고:

${CLOSING}

본문 2200~3000자. 근거 칸은 분량에 안 넣는다.`;

const NOW = `# 역할

너는 이 사람의 **지금**을 읽어 주는 사람이다. 앞으로 몇 달을 어떻게 쓸지가 이 글의 목적이다.

${readingRules()}

${VOICE}

${REGISTER}

## 낼 것

**1. 언제 기준인가** — \`viewedAt\` 을 사람이 읽는 날짜로 적는다. 이 글은 하루 뒤에 읽으면 틀릴 수 있다.

**2. 지금은 어떤 시기인가** — 대운은 십 년짜리 흐름이다. 앞의 대운과 무엇이 달라졌는지, 이 십 년이 무엇을 하기 좋은 때인지.

**3. 올해** — 세운. 올해 들어온 글자가 원국의 무엇을 건드리는지, 그래서 올해가 어떤 해인지.

**4. 이번 달** — 월운. **가장 손에 잡히는 절이다.** 이번 달에 밀어붙일 것 하나, 미룰 것 하나를 적어라.

**5. 조심할 것 둘** — 무엇이, 언제, 왜 그런지. 대처까지. 「조심하세요」로 끝내면 안 쓴 것과 같다.

**6. 밀어붙일 것 둘** — 같은 꼴로.

**7. 이번 달 살림법** — 이 시기에 늘리면 좋은 기운 하나와 그것을 **일상에서 늘리는 법** 서넛. 시간대·장소·움직임·공부·습관 중에서. 지금 운이 데려온 글자(\`now.saeun.pillar\`·\`now.wolun.pillar\`)와 원국에 모자란 것(\`analysis.elements.missing\`)을 함께 보고 고른다. 오행이 어느 시간대·방위에 붙는지는 자료에 없으니 가져다 쓰되 근거에 \`자료 밖\` 이라고 적어라.

그다음에 줄을 긋고:

${CLOSING}

**틀이 다른 값이 섞여 있다.** \`contract.crossedFortunes\` 를 읽어라 — \`now.saeun.relations\` 는 한 해 전체를 견준 것이라 지금 안 도는 대운과 걸린 것이 섞일 수 있고, \`now.relations\` 는 한 순간이라 지금 도는 대운만 남긴 것이다. \`daeunApproximate\` 가 참이면 시각을 몰라 대운수가 두어 달 흔들려 지금 대운이 다른 칸일 수 있다 — 그러면 그렇다고 적어라.

본문 1200~1600자.`;

/**
 * **관계 문단이 들어갈 자리.**
 *
 * 이 프롬프트는 정적인 문자열이라 몸통을 미리 지어 둔다(`BODY`). 관계는 사람마다
 * 다르므로 복사할 때 끼운다 — 자리만 표시해 두고 `promptHeadOf` 가 채운다.
 *
 * **자리를 비워 두지 않는 것이 요점이다.** 안 고른 사람에게도 「무슨 사이인지 모른다」가
 * 실려야 한다. 비우면 모르는 것과 안 물어본 것이 같은 침묵이 되고, 모델은 그 침묵을
 * 연애로 읽는다.
 */
const RELATION_HERE = '{{relation}}';

const COMPAT = `# 역할

너는 두 사람의 사주를 맞대어 읽어 주는 사람이다. 「잘 맞는다/안 맞는다」로 끝내지 마라 — **어디서 맞고 어디서 부딪히며 그때 어떻게 하는지**가 이 글의 목적이다.

${readingRules()}

${VOICE}

${REGISTER}

${PERSONALITY}

두 사람을 부를 이름이 자료에 없다. \`charts.a\` 를 「첫 번째 분」, \`charts.b\` 를 「두 번째 분」이라 부르고, **누구 이야기인지 문장마다 분명히 하라.**

${RELATION_HERE}

## 낼 것

**1. 한 줄로** — 이 둘이 만나면 어떤 그림인지 한 문장.

**2. 서로에게 무엇인가** — 첫 번째 분이 두 번째 분을 보는 자리와 그 반대가 다르다(\`tenGods.aSeesB\`·\`bSeesA\`). **한쪽이 다른 쪽을 뜻하지 않는다** — 짝사랑 모양이 흔하다. 그것이 일상에서 어떻게 보이는지까지.

**3. 잘 맞는 지점 셋** — 줄마다 「어디서 맞는가 → 왜 그런가 → 그래서 무엇이 쉬워지는가」.

**4. 부딪히는 지점 셋** — 줄마다 「어디서 부딪히는가 → 어떤 상황에서 터지는가 → 그때 어떻게 하는가」.

**5. 둘이 만나야 생기는 것** — \`combinedFormations\`. 혼자서는 못 이루고 둘이 모여야 서는 구조다. 궁합의 본론이 여기다.

**6. 점수** — 아래를 따른다.

그다음에 줄을 긋고:

${CLOSING}

## 점수를 어떻게 낼 것인가

**내 봐라.** 이 엔진은 궁합을 점수로 내지 않지만(\`contract.scoring: "${EVIDENCE_CONTRACT.scoring}"\`, \`${EVIDENCE_CONTRACT.ruleSets.compatibility}\`), 그것은 우리가 배점의 근거를 못 찾아서지 네가 못 하리라는 뜻이 아니다.

다만 **한 덩어리 숫자로 내지 마라.** 항목으로 쪼개고(예: 서로 채워 주는 정도 / 부딪히는 정도 / 함께 이루는 것), 각 항목이 자료의 어느 경로에서 나왔는지 적어라. 마지막에 한 줄 — **이 배점은 자료에 없고 네가 만든 것이다.** 그 사실을 숨기지 마라.

본문 1200~1800자.`;

const STRICT = `# 역할

너는 만세력 자료를 **근거보다 세게 말하지 않고** 옮기는 사람이다. 이 프롬프트는 앞의 것들과 견주려고 있다 — 같은 자료를 조여서 읽으면 무엇이 남는지 보는 자리다.

## 반드시 지킬 것

이 자료는 스스로 계약을 들고 나온다(\`contract\`, ${EVIDENCE_CONTRACT.version}).

1. **자료 밖의 것을 말하지 않는다.** 사주 지식으로 빈칸을 메우지 마라. 없으면 「이 자료로는 알 수 없다」고 적는다.
2. **근거마다 상한이 다르다.** \`claims\` 의 「있다」·「없다」 상한을 넘지 마라.
3. **점수도 등급도 만들지 않는다**(\`contract.scoring: "${EVIDENCE_CONTRACT.scoring}"\`). 오행 비율(\`analysis.elements.ratios\`)은 지장간을 사령 일수로 펼친 **엔진 내부 점수**이지 세력이 그만큼이라는 뜻이 아니다.
4. **길흉을 말하지 않는다.**
5. **용신을 확정하지 않는다.** \`analysis.eokbu\` 는 억부만 본 후보다. 「기신은 金입니다」가 아니라 「억부 후보를 용신으로 놓으면 金이 기신 자리에 온다」이다.
6. **합이 되었다고 말하지 않는다.** 관계 목록은 「모였다」까지고 화(化)는 \`analysis.transformation\` 이 세 등급으로만 낸다.
7. **운은 지금 도는 칸뿐이다**(\`contract.fortune: "${EVIDENCE_CONTRACT.fortune}"\`). \`daeunAbsence: "beyond-table"\` 은 우리 표가 짧아서이지 그 사람에게 대운이 없다는 뜻이 아니다.
8. **누구의, 어느 판의 글자인지 밝힌다**(\`chartId\`).

## 강도 사다리

${ladderLines(CEILING_RULE)}

## 낼 것

네 부분. 문장 끝마다 \`[강도 · 근거경로]\` 를 단다.

1. **여덟 글자가 그대로 말하는 것** — \`fact\` 만.
2. **세어 보면 나오는 것** — \`derived\`.
3. **후보에 그치는 것** — \`candidate\`·\`reference\`.
4. **이 자료가 답하지 못하는 것** — \`contract.excluded\`·\`limitations\`, 그리고 **네가 말하고 싶었으나 상한에 막혀 못 쓴 것.**

전체 900자 안쪽.`;

const AUDIT = `# 역할

아래에 만세력 자료와, 그 자료를 읽고 **어떤 모델이 쓴 해설**이 있다. 너는 그 해설을 되짚는다.

찾을 것은 「틀린 곳」이 아니라 **「어디서 온 말인가」**다. 해석은 있어도 된다 — 해석인 줄 모르게 적힌 것이 문제다.

## 가를 것

문장마다 셋 중 하나로 가른다.

- **자료** — 자료의 값이 그대로 말한다. 근거 경로를 적는다.
- **읽기** — 자료 위에 세운 해석이다. 어느 값에서 출발했는지 적는다.
- **자료 밖** — 자료에 없는 것을 말했다. 모델이 제 지식에서 가져왔거나 지어낸 것이다.

## 특히 볼 것

1. 자료에 **없는 글자·관계·운**을 있다고 한 곳 — 이건 해석이 아니라 오류다.
2. \`claims\` 의 상한이 얕은데(\`candidate\`·\`reference\`) 단정으로 적힌 곳.
3. 「있다」와 「없다」를 같은 세기로 쓴 곳 — 시각을 모르면 「없다」가 먼저 무너진다.
4. 누구의 글자인지, 어느 계산판(\`chartId\`)의 글자인지 흐린 곳.
5. 기준 시각(\`viewedAt\`)을 안 밝히고 운을 말한 곳.
6. 점수·등급을 냈으면 **그 배점이 어디서 왔는지 적혀 있는가.** 만든 것을 만들었다고 적었으면 통과다.

## 낼 것

표 하나. | 해설의 문장 | 자료 / 읽기 / 자료 밖 | 근거 경로 | 한 줄 평 |

마지막에 두 줄로 닫는다 — **자료가 실제로 떠받친 비율**이 얼마쯤인지, 그리고 **가장 위험한 한 문장**이 무엇인지.

**없는 문제를 만들지 마라.** 해석을 해석이라고 밝히고 쓴 문장은 문제가 아니다.

## 검사할 해설

<<< 여기에 검사할 해설을 붙여 넣으세요 >>>`;

/**
 * 프롬프트를 이루는 **조각** — Reading 파이프라인이 같은 것을 쓴다.
 *
 * 9단계의 프롬프트를 따로 쓰면 말투와 규율이 두 벌이 되고, 둘은 갈린다 — 갈렸을 때
 * 느슨한 쪽은 언제나 사용자에게 나가는 쪽이다. 조각을 내주고 **낼 것**만 각자 적는다.
 */
export const PROMPT_PARTS = {
  /** 자료가 무엇이고 무엇이 금지이며 층을 어떻게 다는가 */
  rules: readingRules(),
  /** 말투와 뼈대 */
  voice: VOICE,
  /** 문장을 어떻게 닫는가 — 말투가 한 벌이어야 한 사람이 말하는 글이 된다 */
  register: REGISTER,
  /** 성격을 읽는 순서 — 신살이 성격을 정하지 않는다 */
  personality: PERSONALITY,
  /** 딱지가 모이는 끝자리 */
  closing: CLOSING,
} as const;

const BODY: Record<PromptKind, string> = {
  reading: READING,
  now: NOW,
  compat: COMPAT,
  strict: STRICT,
  audit: AUDIT,
};

/** 화면이 고르게 할 목록 — 문구는 여기 있고 화면은 자리만 안다 */
export const PROMPTS: readonly {
  kind: PromptKind;
  label: string;
  hint: string;
  /** 두 사람일 때만 뜻이 있는가 */
  needsTwo: boolean;
}[] = [
  {
    kind: 'reading',
    label: '전부 해석',
    hint: '사주를 모르는 사람이 읽고 「그래서 뭘 하면 되나」가 남게 — 강점·주의·지금까지',
    needsTwo: false,
  },
  {
    kind: 'now',
    label: '지금 도는 운',
    hint: '이번 달에 밀어붙일 것 하나, 미룰 것 하나까지',
    needsTwo: false,
  },
  {
    kind: 'compat',
    label: '궁합 · 점수',
    hint: '어디서 맞고 어디서 부딪히는지 · 점수는 쪼개서 만든 것이라 밝히게',
    needsTwo: true,
  },
  {
    kind: 'strict',
    label: '상한 지키기',
    hint: '조여서 읽으면 무엇이 남는가 — 견줄 짝',
    needsTwo: false,
  },
  {
    kind: 'audit',
    label: '해설 되짚기',
    hint: '나온 글의 어느 문장이 자료에서 왔는지 가른다',
    needsTwo: false,
  },
];

/** 자료 없이 프롬프트만 — 화면이 미리 보이는 자리에 쓴다 */
export const promptBodyOf = (kind: PromptKind): string => BODY[kind];

/**
 * 한눈에 보이는 머리 — **새 값이 아니라 아래 자료에서 뽑은 것.**
 *
 * 여덟 글자를 읽으려고 36KB 짜리 JSON 을 뒤지게 하지 않는다. 사람이 붙여 넣기 전에
 * 「이 사람 맞나」를 눈으로 확인하는 자리이기도 하고, 모델에게는 긴 자료를 읽기 전에
 * 좌표를 주는 자리다.
 *
 * **다시 세지 않는다.** 전부 `evidence` 의 필드를 읽어서 적는다 — 여기서 간지나 절기를
 * 새로 구하면 머리와 자료가 언젠가 어긋나고, 어긋난 날 어느 쪽이 맞는지 알 수 없다.
 * 화면의 운과 문장의 운을 한 곳에서 낸 것과 같은 규율이다.
 *
 * **사람을 이름으로 부르지 않는다.** `charts.a`·`charts.b` 라고 적는 것은 무뚝뚝해서가
 * 아니라, 이 머리가 하는 말이 사람에 대한 것이 아니라 **자료의 어느 자리**에 대한
 * 것이기 때문이다. 모델이 아래 JSON 에서 찾아갈 이름도 그것이다.
 */
/**
 * 머리가 실제로 읽는 자리만 — **잘린 자료도 같은 머리를 쓴다.**
 *
 * `ChartEvidence` 를 그대로 받으면 모델에 넘길 자료(`RedactedEvidence`·
 * `SharedEvidence`)가 이 함수를 못 부른다. 그러면 머리를 짓는 자리가 둘이 되고,
 * 둘은 언젠가 갈린다 — 갈리면 「한눈에」가 아래 자료와 다른 말을 하게 된다.
 */
export type SummarizedChart = {
  pillars: Pick<ChartEvidence['pillars'], 'year' | 'month' | 'day' | 'hour' | 'dayMaster'> & {
    meta: Pick<ChartEvidence['pillars']['meta'], 'monthTerm' | 'sajuYear'>;
  };
  /** 운을 아예 안 싣는 자료도 있다(Match 동의 범위 밖) — 그때는 이 키가 없다 */
  now?: ChartEvidence['now'] | null;
};

/** 머리를 지을 수 있는 자료 — `Evidence` 도, 잘린 것도 이 모양을 만족한다 */
export type Summarizable = {
  viewedAt: string;
  charts: { a: SummarizedChart; b: SummarizedChart | null };
};

/**
 * 한 기둥을 **사람이 읽는 꼴로** — 한글 이름과 두 글자의 오행.
 *
 * 자료의 `pillars` 는 간지와 한글 이름만 들고 오행은 안 든다. 그러면 「인목은 나무다」를
 * 모델이 제 기억에서 꺼내 쓰게 되고, 그것은 이 프롬프트가 딱 하나 금지한 것(자료 밖을
 * 자료인 척하기)의 경계에 선다. 오행은 지지·천간에 붙은 **정의**라 새로 재는 값이 아니니,
 * 상수표에서 읽어 머리에 실어 준다 — 그러면 글이 자료를 읽고 쓴 것이 된다.
 *
 * 여기서는 **`ELEMENT_KO` 로 적는다.** 이 줄이 하는 일은 庚 을 金 으로 옮기는 수고를
 * 덜어 주는 것까지이고, 「금이냐 쇠냐」는 문장마다 다르므로 그 선택은 프롬프트가 두 벌을
 * 다 내주고 글 쓰는 쪽이 한다. 머리가 한 벌을 먼저 적으면 그것이 그대로 베껴진다.
 */
const plainPillar = (pillar: { stem: string; branch: string; ko: string }): string => {
  const stemElement = STEM_INFO[pillar.stem as Stem].element;
  const branchElement = BRANCH_INFO[pillar.branch as Branch].element;

  return `${pillar.ko}(${ELEMENT_KO[stemElement]}·${ELEMENT_KO[branchElement]})`;
};

function chartSummary(key: 'charts.a' | 'charts.b', chart: SummarizedChart): string {
  const { pillars, now } = chart;
  const { year, month, day, hour, dayMaster, meta } = pillars;

  const stem = STEM_INFO[dayMaster as Stem];
  const monthBranch = BRANCH_INFO[month.branch as Branch];

  const eight = [
    `년 ${year.name}`,
    `월 ${month.name}`,
    `일 ${day.name}`,
    hour === null ? '시 —(시간 미상)' : `시 ${hour.name}`,
  ].join(' · ');

  const plain = [
    `년 ${plainPillar(year)}`,
    `월 ${plainPillar(month)}`,
    `일 ${plainPillar(day)}`,
    hour === null ? '시 —(시간 미상)' : `시 ${plainPillar(hour)}`,
  ].join(' · ');

  const lines = [
    `\`${key}\``,
    `- 여덟 글자  ${eight}`,
    `- 한글과 오행  ${plain}`,
    `- 일간 ${dayMaster}(${stem.yinYang === '陽' ? '양' : '음'}·${stem.element}) · 월지 ${month.branch}(${SEASON_KO[monthBranch.season]}) · 절입 ${meta.monthTerm.name} · 사주년 ${meta.sajuYear}`,
  ];

  // 운이 안 실린 자료도 있다. 없는 것을 지어 적지 않고 줄을 빼기만 한다.
  if (now !== null && now !== undefined) {
    const daeun =
      now.daeun === null
        ? `대운 없음(${now.daeunAbsence})`
        : `대운 ${now.daeun.index} ${now.daeun.pillar.name}(만 ${now.daeun.startAge}→${now.daeun.endAge}세)`;

    const daeunPlain =
      now.daeun === null ? '대운 —' : `대운 ${plainPillar(now.daeun.pillar)}`;

    lines.push(
      `- 지금 만 ${now.age}세 — ${daeun} · 세운 ${now.saeun.pillar.name}(${now.saeun.year}) · 월운 ${now.wolun.pillar.name}(${now.wolun.startTerm.name})`,
      `- 운의 한글과 오행  ${daeunPlain} · 세운 ${plainPillar(now.saeun.pillar)} · 월운 ${plainPillar(now.wolun.pillar)}`,
    );
  }

  return lines.join('\n');
}

/** 자료 앞에 놓는 머리 전체 */
function summaryOf(evidence: Summarizable): string {
  const lines = [chartSummary('charts.a', evidence.charts.a)];
  if (evidence.charts.b !== null) lines.push(chartSummary('charts.b', evidence.charts.b));

  return `## 한눈에

아래 자료에서 뽑은 것이다 — **새로 더한 값이 아니다.** 어긋나 보이면 자료 쪽이 맞다.

${lines.join('\n\n')}

기준 시각 \`viewedAt\` = ${evidence.viewedAt}`;
}

/**
 * 프롬프트와 자료를 한 덩어리로 — **이대로 붙여 넣으면 된다.**
 *
 * 자료를 뒤에 놓는다. 앞에 놓으면 긴 JSON 을 다 읽고 나서야 규칙을 만나고, 그때는
 * 이미 읽는 방식이 정해져 있다. 규칙이 먼저다.
 *
 * **한눈에 보이는 머리는 역할 바로 뒤에 선다.** 규칙보다도 앞이다 — 무엇을 읽는지
 * 모르는 채 어떻게 읽을지부터 듣게 하지 않는다. 붙여 넣는 사람도 첫 줄에서 「이 사람
 * 맞나」를 확인한다.
 *
 * 자리를 찾는 법이 암묵이라 테스트가 잠근다. 프롬프트는 전부 `# 역할` 문단으로 열고
 * 그다음이 `## ` 로 시작하는 절이다 — 그 경계에 끼운다.
 *
 * 들여쓰지 않는다. 붙여 넣는 자리에서 들여쓰기는 읽기 좋음이 아니라 **무게**다 —
 * 두 사람짜리가 들여쓰면 460KB 이고 안 들여쓰면 76KB 다.
 */
export function promptWithEvidence(
  kind: PromptKind,
  evidence: Summarizable,
  relation: Relation | null = null,
): string {
  return `${promptHeadOf(kind, evidence, relation)}

## 자료 (${EVIDENCE_CONTRACT.version})

\`\`\`json
${JSON.stringify(evidence)}
\`\`\``;
}

/**
 * 자료를 뺀 나머지 — **화면 미리 보기가 실제로 보낼 것을 보이게 한다.**
 *
 * 미리 보기가 `promptBodyOf` 를 보이면 머리가 빠진 글을 보여 주게 되고, 그러면
 * 「보내기 전에 무엇을 보내는지 본다」는 그 칸의 구실이 절반만 지켜진다. 자료만
 * 잘라 낸다 — 잘린 자리가 어디인지는 화면이 한 줄로 적는다.
 */
export function promptHeadOf(
  kind: PromptKind,
  evidence: Summarizable,
  /**
   * 두 사람이 무슨 사이인가 — **궁합 프롬프트에만 뜻이 있다.**
   *
   * 익명 화면은 우리가 모델을 안 부르지만 **프롬프트는 나간다.** 사용자가 복사해
   * 붙여 넣는 그 글에도 같은 구멍이 있었다 — 두 사람이 무슨 사이인지 안 적혀 있어서
   * 모델이 관계의 일반적 가능성으로, 사실상 연애로 읽는다.
   */
  relation: Relation | null = null,
): string {
  const body = BODY[kind].replace(RELATION_HERE, relationBlock(relationSentence(relation)));

  return withSummary(body, evidence);
}

/**
 * 아무 몸통에나 머리를 끼운다 — **9단계 프롬프트도 같은 자리에 끼운다.**
 *
 * 자리를 찾는 법이 암묵이라 테스트가 잠근다. 프롬프트는 전부 `# 역할` 문단으로 열고
 * 그다음이 `## ` 로 시작하는 절이다 — 그 경계에 끼운다.
 */
export function withSummary(body: string, evidence: Summarizable): string {
  const [role, ...rest] = body.split(/\n\n(?=## )/);

  return [role, summaryOf(evidence), ...rest].join('\n\n');
}
